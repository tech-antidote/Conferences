---
title: "SCCM - The tree that always bears bad fruits"
speakers: ["Mehdi 'kalimer0x00' Elyassa"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - SCCM - The tree that always bears bad fruits - Mehdi 'kalimer0x00' Elyassa.eng.txt"
sha256: "145ec683995771b508b0e59a62f015a06763ceecef8464b431b4b96c88d9cf2c"
duration_seconds: 2330
words: 5531
text_chars: 30671
redacted_secrets: 0
converted_at: "2026-08-12T06:24:14Z"
---

# SCCM - The tree that always bears bad fruits

**Speakers:** Mehdi 'kalimer0x00' Elyassa  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (38 min, 5,531 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


All right, welcome to the last track of track four of DEF CON 33. And he's going to be talking to us a little bit of his research into SCCM security. Hi, everyone. I'm super glad to be here. And thank you for staying till the end. So today in this last session, I will walk you through my research on SCCM. So let me first introduce myself. My name is Mehdi Eliassar, and I work as a red teamer at Synactive, which is an offensive security company based in Paris, in France. So I've been working in IT security for eight years now, and I started as a pen tester. And now I do red teaming full-time. And I mainly focus on breaking web applications and also on Active Directory stuff. So this talk will cover the following topics. First, I will present and describe some key internals of SCCM, mainly to give you a grasp on how it works under the hood. Then I will cover the CVs I've discovered in SCCM.

Then I will introduce some post-exploitation techniques that I've been using throughout my engagements, but also these techniques that led to the creation of a tool that I will present here. And finally, based on the knowledge that we've got about the internals of SCCM, I will share with you some techniques to backdoor an instance. So let's start with the SCCM internals. For those who don't know what SCCM actually is, it was renamed recently to Microsoft Configuration Manager. So it's an IT asset management solution edited by Microsoft. It has been around for a long time now, and it is still widely used and deployed within organizations. And so to manage devices, SCCM installs an agent on the clients, and it provides code execution capabilities. And for us, on the offensive side, this makes it Microsoft's native C2, and it's a prime target during our engagements. And today, there are many attack techniques to take over an SCCM instance, and they primarily arise from misconfiguration or a lack of hardening.

But they were widely documented until now, so I won't cover them in this talk. So let's start with the first internal, and the first concept to introduce this part is the client communication modes. So you have two of them. So SCCM client mainly talks over HTTP with the management point. So you have on the management point, you can configure it either with enhanced HTTP mode. So this is mainly using plain HTTP transport mode. Or you have HTTPS mode, which is advised by Microsoft, which requires an internal PKI to issue certificates such as ADCS.


## [03:06]


And this is done to make the clients do mutual TLS with the management point services. So this is what makes the clients present a certificate to talk with the management point. So sometimes in an engagement, you need that certificate, and it can be a bit painful to exploit SCCM. Now let's take a look at how SCCM works under the hood. Most services are web applications hosted on IIS. From what I've seen, it's a mix of old and new technologies that are glued together sometimes. You have mainly ASAPI modules alongside .NET applications. And you also have COM that is heavily used between components, often with deep chains of COM codes. So when you do reverse engineering, it can be hard to follow the code flow. And as I have limited time, I will focus only on the services that I tied to the management point. And let's start with the SMS management point service. So on the right side, this is a snippet of a dump I did on the IIS web configuration.

So this is only for the SMS management point service, but we'll get other snippets for other services. And you can observe here that there are three ASAPI modules in use. One to serve policies to clients, another to serve scripts to clients, and the last one to serve location services information to clients. So this service has four different entry points. You have the standard one that is named SMS underscore MP, and other ones with the same modules, but different authentication methods, such as Windows Auth, Alt-Auth, and Token-Auth. But also you can observe on the right side in the configuration on IIS that anonymous authentication is enabled on three entry points out of four. So that's weird. And as I said previously, when the management point is configured in HTTPS mode, a mutual TLS is required from the client to access the services. This is the theory, but in practice, you can bypass this.

To do so, you have to use the SMS MP, Token-Auth, or Alt-Auth entry points. And here, there is no need for a client certificate. As you can see in the snippet below, the first query, we get a 403 error from the server asking for a client certificate. But if you hit it with the other entry points, you get 200, and you can reach the services. What can you do with this? For example, on the service, you can hit the method MP list on the get-auth module to get a full list of management points within a site.


## [06:09]


There are two interesting fields. You have the version with the build number of SCCM and also the SSL state, which indicates if HTTPS mode is enabled on the management point or not. You also have another method, which is MP list one. You give it a parameter of the site code so you can enumerate management points from other sites within the same hierarchy. So you can do with this unauthenticated reconnaissance on HTTP, but also on HTTPS if you use those TLS bypass, mutual TLS bypass that I showed you earlier. Let me now cover another key service, which is CCM messaging. This web app has a single ASAPI module with entry point named in a similar way than the ones we've seen before. And here, too, anonymous authentication is enabled on three entry points. So in practice, CCM messaging is an HTTP-based communication protocol that uses a custom method, CCM underscore post. And this protocol is mainly used by the clients to discuss with the management points, but also it's used by other site servers, such as management points or between internal services.

In this snippet below, you have the structure of a message. There is two main parts in a multi-part body, the XML header and the request, which is an XML document. So in the header, sometimes you get a message signature that is included and it proves the device identity. So this is how authentication is done here in this protocol. And under the hood again, the ISAPI module serves as a single entry point that dispatches the messages to the right service handler. So it does so by checking the value of the target and point field in the header part in the system message. And once again, everything is done through COM as the service handler are COM classes. They are configured within the WMI database as instances of the CCM service and point configuration class, and these objects can be found in two distinct namespaces. So the first one is where the configuration is persisted between reboots. And on this class, you have two interesting fields, the CO class one, which is the cellist ID of the COM service handler to call, and also the visibility field, which is super important as it indicates if a device identity is needed to interact with this service.

So this is where the authorization level is defined. And as you can see in the dump below, the MP services are configured with either all or client signed visibility. And I will circle back to this knowledge at the end of this talk for the persistence part.


## [09:14]


And once again, when HTTPS mode is enabled, there is also a trick to bypass mutual TLS for CCM messaging. So you need to use CCM system alt-auth entry point to do so. Unfortunately, the other entry point with the token auth doesn't work at this time. And as the alt-auth was introduced in recent version of ECCM 2403, this bypass won't work in older version of ECCM. So how can you leverage this bypass to do some nasty thing on ECCM? In fact, you can loot NIA credentials without having Windows credentials on the network. So there is a well-known technique to harvest NIA credential is to register fake clients when you got yourself a Windows credential for a machine account. That's needed. So when you do that registration, the SSM client is approved. And this is mandatory to pull policies. However, with this mutual TLS bypass, if you register a client with a domain credentials, it is automatically marked as approved.

Thus, this gives you the ability to loot NIA credentials unauthenticated. So I plan on updating the tool SSCM secrets, which is available on GitHub already. So I will do a pull request in the next day, right after the DevCon. So I've discovered this while preparing this talk. All right. Another service that is interesting is the one that is named SMS Provider. It acts as an abstraction layer for read and write operation on the database. So this is a completely different role. It creates a specific WMI namespace named SMS underscore this three-letter site code. And access control is managed through airbag, withdrawals that are defined in the database. And there is also a local Windows group, SMS admin that is created, and it is granted access permission to the WMI namespace. So this group automatically includes any user or group assigned an airbag role, as well as machine accounts of management points servers.

So this gives you the ability to do some relaying. And on top of that, the SMS Provider exposes the Admin Service REST API. So this API offers two routes, one for the OData protocol, and another one for WMI, supporting GET and POST operation on over 700 classes. And this API is a .NET application built with Owen, and there is no way to configure or enable EPA. So this makes it vulnerable by default, by design, to relay attacks. So you can't fix that. And let me finish with the Tenant Attach concept.


## [12:18]


So for a hybrid management approach, an SCCM site can be configured with Tenant Attach. Basically, the feature allows the on-premise SCCM instance to connect to Intune. And when enabled, the SCCM instance uploads service device information to Intune. But also from the Intune web console, you can run some actions on those on-premise device, such as asking for a device restart, but also installing application or running scripts. And when this feature is enabled, there is a principle named config-manager-svc-underscore-guid that is created on your tenant. So it's an intra-ID application, granted the following permission on the tenant. So we have read and write collection data on the configuration manager microservice API, which is an undocumented API. But also we have directory read-all granted on the graph API. So this is an account that you can extract from the on-premise to perform reconnaissance on the tenant.

And I'll get back later to this account and show you how you can recover its credentials from the database. All right, let's talk a bit about zero days. So now I'm gonna walk you through how I identified vulnerabilities in SCCM. I will mostly detail the methodology I applied so you can see exactly how the issues were discovered. So I got two of them today. So the first one, it's CVE-2024. It's an unauthenticated SQL injection that I discussed earlier this year. So it affects SCCM messaging, the MP location manager handler. And as this handler is configured with visibility at all, so there is no need to prove the device identity which makes the exploitation unauthenticated. And usually the management point and site server roles are installed on the same server. So as the site server has the sysadmin role on the database, this issue allows you to instantly take over the database and all the sites.

And to mitigate the impact, Microsoft advises to install the roles on distinct servers. So the code is already available on our GitHub. All right, so to discover this issue, I kick-started my research by extracting strings from all the libraries used by SCCM. And some SQL format string patterns stand out right away. Also, there were method signatures that show up in the debugging messages that are left in the code of SCCM, which reveal the internal code structure. Among those SQL patterns in the location manager library, precisely, there are some format strings used by the service to execute stored procedures, like the one calling MP get machine ID, which is highlighted here in the snippet.


## [15:23]


So looking at the code next with the compiler like IDA, we can confirm that the input is formatted into the SQL string without escaping. So here comes the question, how to reach this flawed method? And thanks to the call tree overview plugin in IDA, you can observe that there are multiple paths to the flawed method, but one clearly stands out, the one highlighted here. It stands out as the simplest route to reach the code. So this is the one I took for the exploit. There is less hassle and less condition to reach the code. And following that shortest path from the CTO, I landed in the code location shown in this screenshot. And once again, the debugging strings are helpful, as they indicate that the get machine ID is called right before an update SF method. So this is helpful next to choose which requests to pick up. So this is an extract of the partial request body, in which there are at least 20 names of XML document that are processed by this service that you can reach.

And with the information that you, the hint that I got from the debug string, I could quickly choose the update SF request as the one to use to reach the flawed code. And here is the final CCM message to exploit the injection. So on the left side, you have the header part where the injection point is located in the source ID field. So this is the injection point is in the header, which isn't even signed by the client. And on the right side, you have a minimal update SF request filled with the placeholder values just so the exploit can work. So that's the exploitation. All right, so when the patch was released, I confirmed the issue was fixed because they now use prepared statements. However, I've also noticed that the flawed code is still there and it's now gated by an if condition, which was raising questions about the purpose of that. And digging a bit further, I concluded that the if condition checks if a registry value is set on the system.

So it's named disable additional validations. So this is clearly a fail-safe to bypass that patch. So when you exploit the system and you put that value, you will get the vulnerability again. And I think the main reason is that the first version of the patch that was released by Microsoft was causing serious trouble to the customers. So they left that if condition as a rollback for customers that are complaining about the patch. All right, let's talk now about the other CVE 2025. It's a not authenticated SQL injection in the SMS provider component.


## [18:27]


It's the admin service. Microsoft released a patch in July 2025 to address this flow. So this vulnerability can be exploited using any SCCM airbag role, even the read-only one. We'll see later why. And I am releasing the proof of concept for this issue right after this talk. So to discover this bug, I reviewed the MOV file for the SMS provider namespace. I looked mostly for patterns where methods had parameters that appear numeric but are defined as strings in the WMI side. So this mismatch between the expected type and the one defined into WMI enables potential injection. So this, I identified the method update classic deployment as a good candidate, as its offer ID parameter seems numeric, but it's defined as a string in the WMI MOV. So you have its definition here in the snippet. All right, so looking into the code behind of update classic deployment, I noticed that the SQL injection occurs in the permission check step.

So that's why a user with any RBAC role, even the read-only one, can exploit this injection. And from my point of view, there are two ways to exploit this issue. First is obtaining credentials for a user having any SSCM administrative role. But this is not that easy on an engagement. Otherwise, you can perform authentication relay against the admin service API on which EPA protection can't be required. So depending on the topology of the deployment you got, you have multiple scenarios. First, if the SMS provider role is installed on a dedicated server or not on the management points, you can relay the management point to the admin service and exploit the injection. If there are multiple management points deployed on a site, you can relay one management point to another management point hosting the SMS provider role. And if there is a single management point in a standalone mode with also the SMS provider role on the same server, you can try some self-relay attacks if it isn't patched against it.

So remember, there is a CVE for NLM reflection that was disclosed in June. So you can use that to exploit. All right, so I have other unresolved issues with Microsoft, but I cannot cover them in this session. But stay tuned, I will share the details at the appropriate time when I got the disclosure is okay. Let me cover now the post-exploitation phase, when you got yourself an admin access.


## [21:29]


Let's see what you can do from the database, for example. So in SCCM, there is the create and run script feature. It's super interesting as it allows you to run PowerShell on a single device. The execution is done as system, and the process is spawned by the CCM exec process, which is highly trusted binary. And to exploit this feature, actual public tools leverage the admin service API of the SMS provider to do so. So there are some downsides to doing that, especially during a red team. First, all actions are logged. Then there is also a double approval mechanism by default. So you need two accounts to exploit this. One to create the script and another one to approve it. And finally, you can't properly clean exploitation artifacts behind yourself. So I asked myself if it was possible to trigger the script execution feature directly from the database access. And the answer is yes. Doing so bypasses the double approval process, and it leaves minimal logging press behind you.

And here is how to do so. We have some steps to do so. First, you'll have to create an entry in the scripts table. There are some columns that are interesting. For example, the script type set to zero is for PowerShell. You have the approver set. You directly set it to three, so to mark the script as already approved. You have a feature. If you set it to one, the script won't show up in the admin console. Finally, the approver author are free text, so you can spoof whatever identity suits you. Then we add an entry in the BGP task table, which will reference the script that we've created. So we have one part is the template ID. We set it to 15 to say that it's a task to request script execution. Then you have the param column, which holds a task param XML document. So this document will include the ID of the script to execute. So this is the document that will be sent to the clients, and the clients will use that information to fetch the script and execute it.

Then we add an entry in the BGP REST task table. So this is where you assign the task to a client. You can see there is a field for the resource ID for the device, managed device, that you want the task to be executed on. So this insertion triggers a push notification to the client, and it downloads the XML document and the script later. To check the script output, in fact, the task's output is sent back by the client to the management point, which stores it in the script execution status table. So you have the exit status code along the route output. So you have to, after a slight delay, you have to query that table. Regarding the login, the only thing on the client side that is logged is the task param XML document that is left in the CCM notification agent log file.


## [24:40]


But the script content isn't logged, so that's OK. However, beware that the script, which is downloaded on the client side, is stored in a script store folder. But that file isn't removed after the execution, so you need to delete it manually. And now I'm glad to introduce you to sccm-sql-client.py. It's a tool that implements these techniques to automate as much as possible. So it started as a fork of Impact's MSSQL client with queries to explore an SCCM database. This is a tool that I've created for a red team a year ago and that I've kept improving with new ideas from the on-the-field experience, but also the knowledge I've gained from my research throughout the time. So regarding the feature, there are some queries to map the topology, but also to list clients or list store credentials. But also you have some features to run the PowerShell scripts on clients, but also to decrypt the credentials automatically.

And this tool also includes some anti-forensic measures as it automatically prepends a small command to your scripts to clean the script store folder. So you don't leave anything behind you. And it also includes some helpers to clean the artifacts easily from the database. And let me just describe you some of its key commands. First to map the topology, you can use SCCM servers. It will just list the servers within a hierarchy so you can check for server from other sites. Then you have SCCM devices. So you will get a full list of known devices, also devices from other sites. Then an important one is SCCM devices BGP status. So we get only the clients for your current sites with their BGP status. So the BGP status is the thing that will tell you if you can execute scripts on those clients. And to run scripts on a device with the tool, you will have to load its content first, either in line with a set PS1 script or from a local file with load PS1 script.

And then you'll have to run SCCM run script with the resource ID of your target device. And at this step, the tool prepends the commands to clean the script store folder, as I said. Then to get the output, you use last task output. It's a helper to check the execution output. It uses the generated GUIDs that are kept in memory. Finally, use the last task clean, which is a helper to clean the artifacts from the database. It will delete everything. Regarding secret extraction, so to look for secrets in the database, you can use SCCM user accounts. It will query the table that stores NAA accounts and push accounts. And here the password field is encrypted with crypto API on the site system server.


## [27:41]


And only that site system server that issued that blob can decrypt it. And you also have SCCM AAD apps. So this is the command to list the intra-ID credentials that are stored when you have configured to not attach feature. So here we have two columns, secret key, which is a blob that can be decrypted by a single site system, but also secret key for SCP, which uses a key shared between all systems. So this is mostly interesting. It makes it easier to decrypt. And to decrypt the secrets, we need to execute code on the site system servers that created the blobs. And by default, they are enrolled as clients too. So we can run a specific script to decrypt those blobs. So the tool has an SCCM decrypt blob command to automate that process. So you need to give it the resource ID of a site system server and the blob to decrypt. And depending on the blob format, it will use the right PowerShell command snippet to decrypt it.

So these commands use C-sharp assemblies available on the SCCM servers and that expose methods to decrypt those secrets. So you have more details in the slides for later. So it's time for a demo. So I will start as an authenticated attacker and I will exploit the mutual TLS bypass to register clients, then dump the NAA credentials. Within those NAA credentials, I will use one of those accounts to access the admin service API, exploit the SQL injection to create myself and admin access to the database. And finally, with the database level access, I will showcase you the SCCM SQL client tool for some commands. So I'm starting with some recon with the bypass for mutual TLS. So here with the token auth, and with the auth auth, I'll show you that I can get the list of management points over HTTPS. And now I'm using a modified version of SCCM secrets to dump the policies. So I'm not giving it any Windows credentials and I will dump the policies like that over HTTPS too.

So this is the name of the client, the fake client that will be created. And here we go. And we have three accounts that are dumped by the tool and the last one we'll use it later. So now I'm gonna show you that the fake clients that is created from in the SCCM console is approved. So this bypass makes you create an approved client. So now let's use the SCCM user, user SCCM read only to access the admin service API and exploit the CV 2025 and create our self an admin account on the database.


## [30:54]


That's done. Now we can use SCCM SQL client to access the database and confirm that we are administrator. Now we can list the sites and it works. So now let me showcase the SCCM SQL client. First by executing commands on a domain controller. So now with the BGP status, I confirm that the DC has online status at one. So we can execute commands on it. And right here, I'm loading a command to execute on the controller. Then I will launch it. So you need to pass the resource ID to run the command. And now we wait a bit and we're gonna get the outputs. And that's all right. Okay, so with the last task clean, you can clean behind yourself the insertion that were done in the database. So now let me show you how we can extract the credential and decrypt them. So this is for the user accounts. We have the push account, the NA accounts here. So now I'm gonna look for the site system server, which is on this deployment is the management point.

CMC. And I'm gonna get the resource ID of this server. Then use that resource ID with SCCM decrypt blog and pass the blog to. So right here, it's gonna run the scripts on the targets and wait a bit for the outputs. Then it will print out the passwords for that account. And that's it. We have the password right there. And now let me show you the same for those credentials for the application on Entry ID. So this is pretty much the same. We get our blog and we pass it in the comments for decryption. And that's our app. That's all for this demo. All right, so let me conclude this talk with two persistence methods. First, it consists in backdooring the CCM messaging protocol with a rogue service handler. So you only have to do is create a DLA that implements the ECCM endpoint interface and the method executes. It's a com method. So I will, in this example, the method will receive PowerShell commands and returns the commands in the outputs.

Then you have to register the cellist ID on the management point.


## [33:55]


Then create a new CCM service endpoint configuration WMI object that use that rogue cellist ID. And the POC is available on our GitHub. So you can grab it if you want to test it. And right here, this is the signature of the execute method that you need to implement for this backdoor to process incoming messages. And to do so, the first agreement is the CCM messaging service. It has to be used to send the reply back with the send message method. And to process the incoming message, it's as the second argument of the execute method. So you use that argument and call the get body wide string method on it to get the input XML. Let me show you a demo of this backdoor in action quickly. So this is a session on a management point. I opened the WMI Explorer to explore the config related to SSM components. And mostly the one for the service handler that are registered on the management point. So I'm looking for it here.

So I'm going to look for the one that starts with MP and the one with the visibility set to all. And this is the one that I'm going to copy and use as a template for the backdoor service handler. And now I'm going to compile the ROC service handler code which is written in C sharp here. Then I will use reg ASM to register the ROC class on the system. And finally, I'm going to use a partial script to register the ROC service handler in the WMI database of the CCM as a CCM service endpoint configuration instance. Here it is named MP backdoor and the visibility is set to all so we can exploit it unauthenticated. And we can see in the WMI explorer that we have our ROC service installed and it will persist between reboots too. And now I'm going to use a Python client to interact with that service. So I'm going to talk with the management point over HTTPS. And now I'm giving it the name of the service backdoor.

Then the command to execute WMI, for example. And we got the output here, the inter-authority system. And let's do some directory listing too. And we got the output too from the backdoor. So that's all for this demo.


## [36:55]


All right. Last persistent technique without a demo but it's an idea. And the idea here is to backdoor the stored procedure that are used by endpoints accessible without authentication on the management point. So there are some legitimate procedures that are altered so that when a specific pattern is detected in the input, the procedure executes our supplied SQL statement. And beyond functioning as a backdoor, this technique can be leveraged when a SQL injection exists but direct access to the database isn't possible, for example. And in such cases, the management point acts as a SQL client allowing execution of arbitrary commands through the backdoor procedure. And here below you have some example of unauthenticated endpoints exposed by the management point and the stored procedure they call. So you have the first two ones for downloading policies and script documents that call MP get policy body or MP get SDM document.

But also the last one is more interesting because it is through the CCM messaging and through post requests so you can send bigger payloads. So you use the site information request, for example, on the MP location service handler which calls the MP get sit info. So this is a technique that I'm going to blog about a bit later with more details, so stay tuned about it. So thank you. Let me wrap this talk quickly. So all the code examples that I presented are accessible through the links provided here. And follow our blog for upcoming posts on this topic and also disclosure of pending issues. And thank you very much for your attention. I'm grabbing some questions from...
