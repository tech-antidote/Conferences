---
title: "BitUnlocker - Leveraging Windows Recovery to Extract BitLocker Secrets"
speakers: ["Alon Leviev", "Netanel Ben Simon"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - BitUnlocker - Leveraging Windows Recovery to Extract BitLocker Secrets - Alon Leviev, Netanel Ben Simon.eng.txt"
sha256: "3b0b32c508cbabe46f05bdcd3a3a43f48d41b577423f0887f6ea1c46dfd38f70"
duration_seconds: 2303
words: 6135
text_chars: 36239
redacted_secrets: 0
converted_at: "2026-08-12T06:24:13Z"
---

# BitUnlocker - Leveraging Windows Recovery to Extract BitLocker Secrets

**Speakers:** Alon Leviev, Netanel Ben Simon  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (38 min, 6,135 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Okay, so hello everyone. Thank you for joining us today. Welcome to our talk, Bit Unlocker Leveraging Windows Recovery to Extract BitLocker Secrets. Can you hear me? Let's go. Today we'll take you inside our journey of attacking and securing BitLocker. We show you how we found and exploited and fixed new vulnerabilities in the Windows recovery environment that allowed extracting all the BitLocker protected secrets. But just before we dive in, a quick introduction. So my name is Alon and here with me on stage is Nathaniel. We're both security researchers working with a security testing and offensive research team at Microsoft, also known as STORM. Our expertise is in vulnerability research focused on everything that happens before the operating system fully loads, covering the various boot stages and its security features. So a secure boot, BitLocker and others. But enough about us and let's dive into our agenda.

So we'll start with the research background and give an overview of BitLocker and WinRE. Then we'll dive into the vulnerabilities that we discovered, how we exploited them. And finally we'll wrap up with the research results, the fixes and actionable BitLocker countermeasures that you can apply today. So starting with the research background. Today we'll explore a security feature that is designed to protect your sensitive data at rest. It aims to defend against theft scenarios where an attacker steals your laptop, aiming to extract your sensitive information, compromise your machine and even backdoor it. You're here at DEF CON surrounded by some of the brightest minds in cyber security and let's be honest, some of the juiciest targets for hackers. Now picture this, you step away for a quick coffee break and when you come back your laptop is gone. Stolen by a thief that is after your research, internal tools, credentials and personal data.

Without data at rest protection, the thief can directly access the contents of the hard disk, extract sensitive data and even modify system files to install a backdoor, then even return the computer without detection. Data at rest protection ensures that even if your laptop is stolen, your data remains encrypted and inaccessible and your system remains untampered. Now I know what you're thinking, the odds of your device getting stolen are so low that you're probably wondering if you should even bother worrying. So consider this. Studies show that the laptop is stolen every 53 seconds. The odds of a laptop being stolen are 1 in 10 and the average cost of a stolen laptop around $49,000. This figure reflects replacement costs, productivity loss, legal expenses but most importantly, the loss of sensitive data and intellectual property. These numbers emphasize the criticality of data at rest protection features and with that, let's dive into our journey of attacking and securing BitLocker which is Windows' data at rest protection feature.

So BitLocker is a full volume encryption technology that is designed to protect individual disk volumes. Once enabled, it encrypts the target disk volume protecting all data stored in it.


## [03:01]


Users have the flexibility to choose which volumes to encrypt based on their specific needs and by default, BitLocker targets the OS volume ensuring that all data stored within the OS environment is protected and encrypted. So with BitLocker encryption in place, even if your laptop is stolen, the protected data remains unreadable because it's encrypted. Now the BitLocker threat model assumes an attacker similar to a common thief which means someone with full physical access to the device but without advanced credentials such as user names or passwords. Not only the BitLocker attack surfaces are shaped by the interfaces accessible to an attacker fitting within this threat model. And as we examined the different attack surfaces and interfaces, a surface, one surface stood out. A surface with significant vulnerability potential that surprisingly has received very little attention in period research.

This surface is the Windows recovery environment also known as WinRE. Any BitLocker attacker can directly boot into WinRE by holding the shift key while selecting restart from logon UI rendering it a potential candidate for deeper inspection. Given WinRE's fit within the BitLocker threat model , the lack of period research and its high vulnerability potential, we decided to conduct a security review that is dedicated towards finding new vulnerabilities, exploiting them, fixing them and then hardening WinRE while reducing its exposed attack surfaces. The end goal of our review was to make both WinRE and BitLocker more secure and resilient. So let's take a closer look at WinRE, what it is, how it functions and which areas present the most promising opportunities for vulnerability research. So WinRE is Windows' recovery platform and it's designed to address critical system issues such as startup failures, persistent crashes, BitLocker errors and more.

For instance, if your machine crashes, WinRE is the component responsible for analyzing the crash, identifying corruptions and resolving it by using its recovery tools. Now if you use Windows, the chances are that you have encountered WinRE at least a few times in your lifetime. Architecturally, WinRE operates with its own standalone operating system known as the recovery OS. This recovery OS is a lean version of Windows with recovery specific customizations. These customizations include a unique set of recovery tools that are all integrated to the known blue screen recovery UI. Among the exposed tools are familiar options such as startup repair, system reset, system restore and others. For storage, this entire recovery OS including all of the DLLs, executables, drivers, they're all compressed into a single WIM file named WinRE.WIM and this WIM file is then stored on disk. Then when WinRE boots up, the entire WIM is decompressed from disk into RAM creating a temporary RAM disk that hosts the entire recovery OS run time.

Any changes made within this environment within the RAM disk are not saved back to the original WinRE WIM file making the recovery OS inherently volatile.


## [06:11]


So modifications to the RAM disk are discarded upon reboot. Now when BitLocker was first introduced, WinRE had to evolve to support recovery from BitLocker related failures. And this led to several architectural and design level changes to enable that recovery functionality. We wondered what impact these changes had so let's take a closer look at them. The first design change focused on the location of WinRE WIM. It was moved from residing in the OS volume which is now encrypted by BitLocker to a dedicated recovery volume which is not encrypted. This change was necessary because WinRE must be able to recover from BitLocker related failures. And if the OS volume is encrypted by BitLocker and becomes inaccessible for example because of a decryption failure, storing WinRE in that location would prevent future recovery. Since WinRE is a critical component that must always be available, it was moved to a separate recovery volume and this ensures that it can function even if the OS volume is not accessible.

The second design change focused on the integrity of the WinRE WIM file itself. So to support integrity validation of WinRE, a feature called trusted WIM boot was introduced. It verifies the integrity of WinRE WIM by comparing its hash to a known trusted hash. So a simple hash comparison here. If the hashes match, the OS volume is automatically unlocked and if the hashes don't match, the OS remains locked. Now these two states, auto-unlocked and locked, define the level of access that WinRE has to the main OS. So in the auto-unlocked state, WinRE has full access to the OS. It can perform recovery operations without any user intervention. While in the locked state, WinRE has no access at all to the encrypted OS volume. Now this change was necessary because WinRE now resides on an unprotected recovery volume and it can no longer be blindly trusted, right? Because physical users can mount this recovery volume, change WinRE WIM and boot.

And we want to prevent it of course. So with trusted WIM boot, any unauthorized modification to the WIM file breaks its trust effectively blocking any tampering. The third design change and the last one focused on limiting recovery tools that are inherently risky to BitLocker. So for example, among the exposed recovery tools is the command prompt. To prevent a scenario where an attacker abuses a command prompt to access the BitLocker protected data, a volume re-lock functionality was added into WinRE. This functionality is triggered every time that a risky recovery tool is selected and it basically re-locks the OS volume. Then to regain access to the OS, the user must manually insert the BitLocker recovery key. Otherwise all contents remain completely inaccessible. So this is how it looks like in practice. If an attacker launches the command prompt, WinRE WIM is trusted, auto-unlock occurred.

But if it launches the command prompt without inserting the recovery key, access to the BitLocker encrypted volume will be denied resulting in the following lock error. So to summarize the impact of the design changes, as long as WinRE WIM is trusted and no risky recovery tools are triggered, the OS volume is unlocked allowing WinRE to recover it automatically without user intervention.


## [09:20]


In contrast, if WinRE WIM is modified without authorization by a physical attacker for example, or if a risky recovery tool is triggered, the OS volume is re-locked preventing WinRE from accessing it. So now the critical question arises, any attack surface is exposed because of these design adjustments? So as it turned out, the auto-unlock state, during the auto-unlock state, WinRE parses files from unprotected volumes, specifically the EFI volume and the recovery volume. This parsing presents a very interesting attack surface which wasn't valuable to explore before BitLocker's introduction. Because before BitLocker there was no security boundary here. So even with full code execution within WinRE and if a physical attacker didn't grab any new capabilities, but this is no longer the case. With BitLocker, any code execution within WinRE in the auto-unlock state can be utilized to bypass BitLocker and extract all the protected secrets.

So we decided to take a look at this parsing of WinRE during auto-unlock and to see if we can attack the parsers, maybe find memory corruptions or logical vulnerabilities and potentially bypass BitLocker. So we decided to dig deeper into these files and their parsing and specifically the files that we will be focusing on today. Or the RA agent XML and boot SDI files residing in the recovery volume and the boot configuration data store, the BCD store, residing in the EFI volume. So without further ado, I'm going to pass it on to Nathaniel who will walk us through the research process of the parsed external files. Thank you. So let's begin with the boot SDI file. The SDI stands for system deployment image format and it is optionally used for booting from virtual disk into RAM disk. This format consists of multiple binary blobs with certain blobs being essential for the RAM disk boot process.

Below you can see the layout of RAM disk in memory which contains the OS disk image and more. When an SDI file is specified, it gets prepended to the virtual disk within the allocated RAM disk memory region. In our scenario, we are trying to boot Windows recovery from a WIM file. The boot SDI will contain a WIM blob which contain an offset to the appended trusted WIM. Additionally, the boot SDI must contain an empty NTFS volume. This empty volume enables the in-memory WIM to be presented as NTFS volume, ensuring compatibility with other Windows components. Now let's briefly examine a pseudocode that demonstrates this process. The code retrieves the sizes of both the SDI and the WIM files, then allocates buffer large enough for both. Next, the SDI data is copied to the beginning of the RAM disk image buffer. Then the WIM data is loaded and positioned immediately after the SDI data. During this process, the loading API computes the WIM hash for validation.


## [12:23]


If the hash validation fails, the main OS volume gets re-locked. Finally, the WIM address is determined by adding the WIM offset stored in the SDI to the RAM disk buffer address. Notably, there is no verification linking between the WIM being used to the one that was hashed earlier, allowing the offset to be set arbitrarily. With that in mind, let's dive into our first vulnerability. We know that WinRE WIM must pass hash validation when loaded immediately after the SDI. However, the boot SDI itself undergoes no validation whatsoever. And since we can manipulate the WIM offset to point to an arbitrary location, we could append an untrusted WIM to the SDI and adjust the offset to point to it. The hash validation for the trusted WIM will succeed, but the untrusted WIM will be executed in its place. This allows us to load and execute any WIM we choose while keeping the main OS volume still unlocked.

Now let's see a demo for the first vulnerability. Here is a locked machine which we don't have the password for it. We press on shift restart to reboot into WinRE. Once we are in WinRE, we will execute the exploit we prepared beforehand. Now during boot, the main OS volume gets automatically unlocked by BitLocker. We reach WinRE UI actions, we select troubleshoot, then we select advanced options. Then we select command prompt. Now the main OS volume got relocked. We don't have the key, so we skip this drive. Now if we try to access C drive, we can't access it. It's locked by BitLocker. If we check the BitLocker status, you can see it is locked. So we change path to the D drive which is the recovery volume, the unprotected volume. We execute the exploit we prepared beforehand. It adds the custom WIM to the boot SDI and then adjusts the offset to it. And once the exploit completed, reboot the machine back to WinRE.

Now instead of the WinRE UI actions you've seen before, we expect our custom WIM to be loaded and we set it to launch CMD. So here is the CMD. We can change path to C drive. If we check now the BitLocker status, you can see it says unlocked. And if we list all the files under the C drive, here are the secrets. Thanks. After we completed our review for the boot SDI, we shifted our attention to the ARIA agent XML. The ARIA agent XML file contains the current recovery state and other configuration settings for WinRE. Upon WinRE startup, it immediately consumes and parses this XML file. When WinRE state is configured to execute a scheduled operation, it will run the designated recovery operation such as startup repair or other tasks.


## [15:31]


All of this occurs prior to reaching the UI actions you've seen before. Today, we will talk about two scheduled recovery operations that can get arbitrarily executed by modifying the state in ARIA agent XML. First offline scanning and then WinRE applications. Starting with offline scanning operation. These operations allow AV run, allow running AV scan from within WinRE against the main OS. It is mainly used against malware that does not run inside WinRE. We found we can control which app performs the scanning, though there are a couple of limitations. First, the offline scanning is performed only by executing applications from within the main OS volume. Second, this application must be digitally signed by Microsoft or WHQL. Third, the signatures must be embedded within the binary itself. We search for all different applications that satisfy these requirements and we discover approximately 30.

No CMD is not among them. And we could not execute all the applications on that list mainly due to compatibility reasons. We reviewed them one by one until we got to an application named TT Tracer. This application is a time travel debugger utility that allows tracing and arbitrary executable. Okay, so if we can trace any binary we want, let's trace CMD then, right? Now let's go over the complete flow. Before starting the machine, we set the recovery state in our agent XML to schedule offline scanning operation. The XML is then parsed by WinRE. Then according to the state we just set, WinRE performs the scheduled operation, which is offline scanning. It will execute TT Tracer, which will trace any binary we set. We specified CMD, so in our case it will launch and trace CMD. And at this point, while still in auto unlock state, we have a full shell access with the main OS volume unencrypted.

Now let's see a demo for the second vulnerability. Again, here's a locked machine which we don't have the password. We shift restart again to reboot to WinRE. We will execute an exploit we prepared beforehand. Now once we reach WinRE, we select troubleshoot, then advanced options, then command prompt. It prompts to ask for the BitLocker key. We don't have it, so we skip this drive. Again, if we check the, if we try to access C drive, you can see it says locked by BitLocker. If we check the BitLocker status again, it will say it's locked. Now we change path to the D drive, which is the recovery volume, unprotected volume.


## [18:33]


We execute the exploit we prepared beforehand. It sets TT Tracer to the scheduled offline operation. And once the exploit completed, we just reboot the machine back to WinRE. Remember, this is the recovery volume unprotected volume. We can do anything we want in it. Now instead of UI actions, we expect an application to spawn. So here is TT Tracer. And it asks us to accept the ULA. We accept it. And CMD is launching, as you can see. Now we can change path to C drive. And if we check the BitLocker status, you can see it says protection on and unlocked. If we list the files under the C drive, we can see again the secrets. There are. Okay, so we completely go and offer the offline scanning. Let's proceed with WinRE applications. Another scheduled operation we can configure is WinRE apps, which allows us to run applications within WinRE. If these applications are trusted, they get executed while the system remains in auto unlock state.

Otherwise, they run after the main OS volume has been re-locked. We went looking for the hash validation process, and we found that trusted applications are registered within the WinRE registry using their executable name and the file hash. The registry resides within the WIM file, and a physical attacker cannot modify it. Any alterations to the WIM would change its hash, causing the main OS volume to be re-locked during the boot hash validation. Now when WinRE is scheduled to run a trusted application, it first calculates the hash of the target application, then searches for a match in the registry. If a match is found, the application is marked as trusted. If no match exists, the application is marked as untrusted. The main OS volume gets re-locked, and then later the application still gets executed, but with the main OS locked. So overall, that sound as WinRE trusted apps validation is solid and works properly, right?

Well, we wondered if an already registered application can expose an attack surface, so we went hunting for registered applications. We found a legitimate usage of this feature by a program named setup platform. This program is, this application is registered as trusted app and used during Windows upgrade. Only once the upgrade completes, the trusted app entry remains in the registry, and it is not removed. So we can execute setup platform, but what else can we do with it? We found that it registered shift plus F10 hotkey to launch CMD. Convenient, right? Well, we tried that, but it fails to execute due to missing configuration file on the main OS volume.


## [21:35]


Since this configuration file resides on the bit locker protected main OS volume, it cannot be created or edited. This configuration requirement caused setup platform to terminate early due to this file being absent. Now, the time window between the hotkey registration and the process exit is very short. It's quite impossible to trigger this hotkey manually. So we wanted to find another way to trigger CMD. We went back to analyze setup platform. That is when we discovered something really interesting. Immediately after registering the CMD hotkey, the application searches for a file named setup platform INI on the recovery volume, which is the unprotected volume. And we can fully control it. Now, by configuring this file properly, setup platform will trigger a message box, which prevents applications from continuing the execution. Essentially, blocking the application from early termination.

That will open an infinite time window to spawn CMD and type any thing we want in it. Now, let's see the complete flow. We modify the XML and set the state for scheduled and trusted app. More specifically, we set setup platform to get executed. We validate this app can be trusted. The validation succeeds and the main OS volume remains unlocked. Next, setup platform gets executed. It registers the hotkey for CMD, which is shift plus F10. It then looks for setup platform INI file and loads it, which will trigger a message box. This way, the process got blocked and won't continue until we press okay. Now, we can simply press on shift plus F10 to trigger the CMD. And we got a shell up and running while the main OS volume is still unlocked. Now, let's see a demo for deferred vulnerability. So, again, here's a locked machine. We don't have the password. We shift restart to reboot into WinRE. In the action, select troubleshoot, advanced options, command prompt.

We don't have the key, so we skip this drive. Now, we will try to access again the C drive. You can see it says locked. We'll check BitLocker status again. It says locked. Protection is on. Now, we change back to the D drive, the unprotected volume. We execute the exploit we prepared beforehand. We set a platform to get executed. We write down the setup platform INI file. And next, we reboot the machine. Now, instead of DUI actions this time, we expect our setup platform to get executed. And we set setup platform INI. So, we expect a message box.


## [24:39]


And here's the message box. Now, we can simply press on shift F10, we spawn the CMD. If we change path to C, we can change it. If we check the BitLocker status, it says protection on. And the BitLocker status is still unlocked. Now, we can just list the files under the G drive, get the secrets. Here it is. Now, we'll continue to talk about the BCD. So, let's move on to attacking the BCD parsing. So, first things first, for those of you who are unfamiliar with BCD and what it stands for, BCD stands for Boot Configuration Data. And it's basically the file that defines how Windows boots. It stores the boot entries, controls the boot parameters, recovery settings, and many more boot related configurations. Now, in the context of WinRE, the usage of BCD is considerably minimal. WinRE uses the BCD mainly to determine the location of the target OS volume on the disk so that WinRE knows which volume to end the recovery operations to, right?

Because we have multiple volumes on the disk, WinRE must know which volume is the OS volume to recover. So, when WinRE starts, it reads the BCD store, queries the target OS volume location from that store, and then ends the recovery operations to this volume. So, the first question that came into our minds when we investigated WinRE BCD usage is what can we potentially gain from targeting it? As it turned out, WinRE places full trust in the target OS, assuming it's out of attacker's reach. Now, generally speaking, this assumption is solid from a design perspective because with BitLocker enabled, the target OS is encrypted and the attacker has no access whatsoever. So, it's safe to store the configurations, query these configurations, and not further validate them because again, a physical attacker can't change anything in the main OS. It's encrypted. Having said that, since the target OS location is defined in the BCD, which we could modify, we began to question this trust assumption.

Could BCD manipulations allow us to undermine it? So, what if we could trick WinRE into thinking that an attacker controlled volume is actually the trusted BitLocker encrypted one? Our focus has shifted towards identifying a primitive that allows impersonating the target OS location. The goal is to manipulate the target OS volume to point to an attacker volume, such as the recovery volume, instead of pointing to the trusted BitLocker encrypted one. So, any time that WinRE then, if we gain this primitive, if WinRE queries configuration assuming it's out of attacker's reach, the attacker can modify it because it will be in the recovery volume and not in the encrypted volume. So, this primitive if gained would break the trust assumption. So, we already know that the target OS location is controlled in the BCD, as we just mentioned, which we can directly modify.


## [27:42]


So, what happens if we change the location directly in the BCD? Well, theoretically it's possible, but it isn't valuable. Let me explain. The target OS location isn't used only by WinRE, but also by the boot manager which uses it to know which volume it should auto unlock for WinRE so that WinRE is able to access the OS. So, any change to the BCD store would affect both the boot phase and the OS phase. In this specific scenario, if we modify the target OS to point to the recovery volume, it would prevent the boot manager from auto unlocking the BitLocker encrypted OS volume. And this encrypted volume contains the secrets that we are after. So, in this state, even it breaks the trust assumption, but even with full code execution within WinRE, we are not able to access the secrets. And that's why it's not valuable. Our primitive must not interfere with the boot procedure and the auto unlock functionality.

We must go deeper. So, we decided to dig deeper into the way that WinRE searches for the BCD store. Any potential flaws at this stage would not interfere with the auto unlock functionality because it executes afterwards. So, to locate the BCD store, WinRE iterates over these volumes and searches each one for the store. The first store that is found is the one used to further calculate the target OS location. So, a simple volume iteration logic. Now, this iteration logic is actually an interesting point to further inspect because if you think about it, only the EFI volume contained the BCD store. So, any attempt to locate the store on other volumes, especially if they are not protected, could potentially be abused. Internally, the functions that are used to iterate over the volumes are find first volume and find next volume. These are two standard Windows APIs and notably, the remark section highlights a very interesting point.

It says that one should not assume any correlation between the order of the volumes that are returned by these functions and the order of the volumes that are on the computer. So, under the hood, these two functions perform further filtering and sorting which causes an inconsistency in the returned volume order. So, for example, on the monitor, you can see the typical volume order returned by the disk part list command. The order is first the OS, then EFI and only then the recovery volume. However, the volume iteration functions would return a different order. It would return first the OS volume, then the recovery and only then the EFI volume. Can you spot the inconsistency? With this iteration logic, the recovery volume is iterated over and checked for a BCD store before the EFI volume. By placing a custom attacker control store in the recovery volume, we can trick WinRAR into using it instead of using the EFI BCD store.

So, with this setup, WinRAR doesn't even reach the point of querying the EFI volume which contains the real store used to boot the machine. So, any modification or corruption we make to that custom store will only impact WinRAR and will not disrupt the boot procedure at all. This flow allows us to achieve the desired primitive. During the boot phase, the boot manager locates and uses the legitimate store from the EFI volume to unlock the encrypted OS.


## [30:44]


And this OS contains the secrets. So, during the boot, it's unlocked. However, during the OS phase, WinRAR instead locates and uses the attacker control BCD store due to the iteration logic that we just explored. And it uses this attacker store, leading it to consider an attacker volume as the BitLocker encrypted one. Right? Because once we control the store, instead of pointing the volume location to the main OS volume, the encrypted one, we just point it to a different volume that we control. So, with this primitive, any configuration that WinRAR retrieves from the target OS, previously assumed to be beyond an attacker's reach, is now entirely under the attacker's control. So, any sensitive configuration it queries, which we weren't be able to modify, now we're able to modify. To further exploit this powerful primitive for bypassing BitLocker, we need to find a WinRAR flow with three characteristics.

First, a flow that can be triggered either from the WinRAR UI or from RE-agent XML schedule operations , so that we're able to trigger this flow freely. Second, a flow that doesn't trigger the wheel of functionality so that we won't lose access to the secrets. Because we don't want a flow that when we trigger it, relocks the volumes and we lose access to the secrets. Third characteristic is a flow that queries configuration from the target OS to perform sensitive operations. And this is actually the interesting part here. The idea is to chain this flow with the gained impersonation primitive to trigger sensitive operations in the auto-unlock state. Such operations that we wouldn't be able to trigger beforehand. So, after exploring different WinRAR behaviors, we found a potential candidate. This is the push button reset feature, also known as PBR. PBR is Windows assistant reset tool and it provides various reset functionalities.

It supports multiple trigger modes with one of its modes known as online PBR meeting the characteristics that we defined for the exploit. So, first, only PBR can be triggered freely as a scheduled operation in RE-agent XML. Second, this flow doesn't trigger the wheel of functionality thereby benefiting auto-unlock. We won't lose access to the secrets. And third, it queries configuration from the target OS to perform sensitive operations, thereby can be controlled with the impersonation primitive. Specifically, the sensitive configuration that we identified was an XML file named reset session dot XML which defines all the operations executed by PBR. Notably, it included the decrypt volume directive which instructs PBR to decrypt any arbitrary BitLocker encrypted volume. So we now have all the pieces that are required for the full BitLocker bypass exploit chain. The exploit required modifying a few files on the recovery volume.

So first, the XML file with online PBR scheduled operation. Second, the BCD store with the OS device element pointing to the recovery volume instead of the encrypted OS. And third, the PBR configuration with the decrypt volume directive specifying the target OS volume to decrypt as the OS volume, the encrypted one. With this setup, what will happen is that in the next WinRAR reboot, WinRAR will go through the online PBR flow.


## [33:48]


During the BCD store look up, PBR locates the attacker BCD store and mistakenly identifies its target volume as a recovery volume. Then PBR loads its configuration from that recovery volume which instructs it to decrypt the BitLocker encrypted OS volume. Once this entire process completes, BitLocker secrets become freely accessible as BitLocker was essentially disabled by PBR. So we went from a considerably scoped OS impersonation primitive to an arbitrary BitLocker decryption primitive . With that, let's see the demo, the last demo of the day of the full exploitation chain . So we have here a locked machine which we don't have the password to this machine. So we select shift and restart into WinRE in order to execute the exploit. Inside WinRE, we select troubleshoot advanced options and then command prompt. Here the recovery key is requested. We don't have this key. We skip this drive. Now you will see that we don't have access to the C drive that contains all the BitLocker secrets.

So now what we need to do is to change drive to the D drive. That's the mounted recovery volume which we control. We placed our exploit in that volume. So we now execute this exploit. And once the exploit finishes, we will reboot into WinRE. Now you will see during the reboot that PBR gets executed. This process takes a few minutes. This is what you will see now on the screen so we fast forward a bit . In the meantime, let me tell you what the exploit did. So it placed this XML file with online PBR and the recovery volume. It then created the BCD store on that recovery volume as well so that the iteration logic considered this store. And the volume, the target volume on that BCD store pointed to the recovery volume instead of the encrypted volume. So now when PBR looks for its configuration, it queries it from the recovery volume instead of from the encrypted volume. This configuration tells PBR to decrypt the OS volume that we are after its secrets.

So what happens now under the hood, what you see on the screen is actually PBR decrypting the OS volume. Essentially disabling BeatLocker on that volume. So once PBR completes, we'll go back into the main OS. You see now the logon screen. And of course we still don't have the password right so we'll restart into WinRE. And once we're in WinRE, we'll check what is the current status of BeatLocker. So select troubleshoot, advanced options, command prompt. At this point you will see that the recovery key is not requested. We change drive to the C drive. List the files and you can see that we have full access to the C drive that contains all of the secrets. Now if you wonder why this is possible, if we check the status of BeatLocker, you will see that the protection is off and the drive is fully decrypted. So we bypass BeatLocker by disabling it, able to extract all of the secrets.


## [36:53]


Thank you. So closing remarks. Starting with the vulnerability fixes. So all the discovered vulnerabilities and exploitation techniques were fixed in July's Pitch Tuesday. The CVs for the vulnerabilities that we presented today are displayed on the screen. To further enhance the security of BeatLocker, we recommend enabling TPM plus PIN for pre-boot authentication. This significantly reduces the attack surfaces and limiting the services only to the TPM. So if TPM plus PIN is already enabled in your machine, you are protected from BeatLocker bypasses that originate from WinRE, the boot components, logon screen and other attack surfaces. Because to get the BeatLocker key, you have to insert a PIN . So to mitigate BeatLocker downgrade attacks, we advise enabling the revised mitigation. This mechanism enforces secure versioning across critical boot components, preventing downgrades that could reintroduce known vulnerabilities in BeatLocker and secure boot.

For the full article on BeatLocker countermeasures, please scan the QR code on the screen. Lastly, we want to emphasize that our journey never ends. We continue to proactively research BeatLocker, WinRE and the related components with a never ending goal of increasing their security and resiliency. It was a pleasure for us to share with you our research project and the security work that we do on WinRE and BeatLocker. If you have any questions, please catch us in the hallway and also feel free to reach out on social media. And with that, we would like to thank you all for joining us today and we wish you a great rest of your day. Thank you.
