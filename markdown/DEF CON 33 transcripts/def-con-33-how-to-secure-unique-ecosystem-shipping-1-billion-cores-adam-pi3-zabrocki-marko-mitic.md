---
title: "How to secure unique ecosystem shipping 1 billion cores"
speakers: ["Adam 'pi3' Zabrocki", "Marko Mitic"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - How to secure unique ecosystem shipping 1 billion cores - Adam 'pi3' Zabrocki, Marko Mitic.eng.txt"
sha256: "5df7daea525f0eb64c60eed67da5ccfd0b245e285d0458cd85ccdb98d28f3327"
duration_seconds: 2219
words: 6354
text_chars: 38039
redacted_secrets: 0
converted_at: "2026-08-12T02:50:00Z"
---

# How to secure unique ecosystem shipping 1 billion cores

**Speakers:** Adam 'pi3' Zabrocki, Marko Mitic  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (36 min, 6,354 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Hello everyone. I'm actually surprised there's so many people on Saturday, relatively in the morning, so thank you everyone for coming to this talk on a Saturday morning. I'm delighted to be for the first time on the DEF CON main stage and have an opportunity to speak about how to secure, and how actually NVIDIA secure, a unique ecosystem shipping more than a billion cores. However, are we even the right people to speak about such an important topic? I don't want to spend too much time here, but here we are. Currently, I'm working in NVIDIA as a Director of Offensive Security. My name is Adam Zabrocki, and I'm leading various offensive security research efforts across the entire company. I'm also involved in the RISC-V International, when I'm serving as a Vice Chair of J-Extension Group, and I'm author of various CPU ISA extensions. And I'm also security architect of the GPU and next generation NVIDIA product.

While Marko Mityk, who is here, co-presenter, he's software security architect and manager at NVIDIA, where he leads NVIDIA core RISC-V team. And he also GPU product security and RISC officer and lead. So the most important question, I think, is why we even have this talk today. So NVIDIA is known from their data platform product, who looks like this. You can see on the screen. And this is not just a specific product. It's a platform. It's an entire platform who handles very complicated workloads, including AI workloads. This is the most known. And it's a huge machine. But that's not the only product which NVIDIA has. We also have this type of the product, which actually can buy and plug into your already existing data platform and handle also AI workloads and other complicated workloads. But we also have traditional GPUs who looks like this. The one which is on the left, it's actually consumer GPU.

But on the right, it's actually also server type GPU, which is different computer architecture. Then we also have other products like that. We have system on the chips like Tegra, which is used in various products. We also have upcoming NVIDIA Digits and Spark. But what is important, even all of those products has completely different features. They look even completely different. They have completely different functionality. They have different security expectations. They have different threat model. On their core, on their heart, they're still running only one specific NVIDIA processor. So even they have completely different security warranties, they still are based on the ecosystem, which is NVIDIA processor. So there's nothing hidden under the sun, and we wanted to give you a little bit insight how we redesigned the system to be able to fulfill so many various use cases. So first thing first, each NVIDIA chipset, which you saw on the previous slide, may include between 10 up to 50 microcontrollers in one die.

There's various level microcontrollers like functional level controllers, like memory controllers, chip-to-chip interface. There is chip-system level controllers like RM, like PMU, like security and more. There is also data processing. And what is important that in the past, all of those microcontrollers were actually designed and run on the internal proprietary skies across Falcon.


## [03:03]


However, our demand of grow and our demand of adaptability was much, much faster than Falcon could actually keep up. The legacy architecture couldn't keep up. So we have different trajectory of the grow, and it was very difficult to scale on the Falcon architecture. So it was sufficient at the time when we used it, but requirements and expectation changed. The security layer needed to be updated to fulfill all of the modern and also future expectation, which we don't even actually know how it's going to look like, yes? And NVIDIA processor and chip must meet the demand which we have, especially of the grow and adaptability, and they must be flexible enough to actually fulfill these needs. AI work was still booming, actually, not only AI work, but most of the complicated work was booming, and NVIDIA processor is actually a crucial piece of all of those processing. And we had actually an opportunity to redesign this ecosystem from the scratch to be able to fulfill all of those use cases, and actually wanted to do it in a secure manner that will be scalable in the future, which we don't even know how this future is going to look like.

And we decided to do this on the RISC-V. Why did we choose RISC-V? Mark will speak to you more about. Thank you, Adam. Hello, everyone. I'm really happy to be here. So let me continue with answering why did we choose RISC-V. So first, we wanted to retire our proprietary Falcon architecture and move towards an open and more future-proof foundation. The RISC-V offered better baseline performance without legacy overhead, and its flexibility enabled rapid hardware-software co-design through custom extensions. But the layered security isolation primitives were the key factor here. So essentially, we wanted to build a strong isolation model as strong as physically separate controllers. We did not want to keep adding new standalone controllers whenever we wanted stronger isolation for something. So we needed a common configurable platform for all microcontrollers across all products. And such a platform would need to be scalable, both up and out, so that we only pay for what's needed whenever we needed it.

And RISC-V met all our requirements. This shows the rate of adoption of RISC-V at NVIDIA. So naturally, with more applications we run on controllers, we have more cores in each of the products. We first introduced RISC-V in Turing architecture, and then focus was on migrating from Falcon, making investments along the way through hardware-software co-design on security hardening, performance, and scaling. I do want to mention that we have RISC-V in all NVIDIA products, not just GPUs, but I only listed GPU architectures just for simplicity. So there are many aspects to forming a secure execution, and this list here is by no means an exhaustive one. What I highlighted are just items that our talk will be touching on today. And now let's go briefly over some RISC-V basics as it's important to follow what's coming.


## [06:03]


So most commonly RISC-V has three privilege modes, machine mode, supervisor mode, and user mode. And there is also this HS mode, or HS mode when hypervisor extension is present. Each privilege mode has its own set of CSR registers, and there are multiple access control primitives that are configurable per privilege level. PMP and IOPMP are the most fundamental isolation primitives, isolation units. So PMP, physical memory protection, this is what restricts code running at different privilege levels from accessing certain memory regions. And IOPMP would be pretty much the same thing, just for peripheral devices. So essentially it restricts devices like DMA or crypto to access some memory ranges. And from processor's perspective, IOPMP is used to restrict access to devices' control registers. NVMPU is NVIDIA's memory protection unit, that's NVIDIA extension, this is what enables tasks or processes if operating system is present on a controller.

And apart from memory protection, it also does virtual to physical address translation. So NVRISC-V would be NVIDIA's implementation of the RISC-V ISA. It is highly configurable, and it has several NVIDIA custom extensions. I only listed security extensions here, and I believe we'll cover most of them throughout our talk today. And Peregrine is our hardware subsystem that includes NVRISC-V processor and peripheral devices. Within Peregrine there may be multiple NVRISC-V cores, and the RISC-V extensions may or may not be present. We also have certain configurability within the peripheral devices as well. And depending on what microcontroller does, some cores may be in DCLS mode for safety and security. So internally we describe this Peregrine ecosystem as one core strategy. Essentially having one foundational hardware software architecture for all microcontrollers across all products. This way, investments we make in these foundational elements, everything benefits from these investments.

So on a given microcontroller, execution starts in immutable bootrom, or boot plugin if bootrom is not present. And then we have separation kernel software which creates and manages these isolated execution environments we call partitions. And partitions are where all the application software runs, where all the use cases are implemented. And this partition architecture is our foundation for running mixed criticality applications on a single NVRISC-V controller. And at the same time, that's also a foundation for our GPU software security. So because majority of applications, majority of software runs within partitions, it's very important to secure that execution environment. And this is what Adam is going to talk about next, and I'll come back to dive deeper into core software elements.


## [09:07]


Thanks, Marko. So yes, as Marko mentioned, the Peregrine ecosystem is actually much more than RISC-V, yes? We have entire ecosystem built on the top of this basic block, but still the foundation of everything what we've built, it's actually RISC-V. And it's architecture flexibility which RISC-V gave us is awesome, yes? It's great for the innovation, but if you want to build entire ecosystem on the RISC-V, you must take into account some caveats which comes with it. So open source and flexibility, despite their undeniable advantages, they are actually some caveats that you need to take into account. Like, for example, fragmentation. What actually is implemented in the hardware? What software can expect? Specific optional extension exist or not? Is this specific functionality exist or not? How do I run securely on that specific hardware? Does this even include the hardware functionality which software expects?

We don't know, because it's optional or mandatory. And there is some remediation for this problem, because RISC-V International provides something which they call profiles, that mandate that specific profile must actually provide specific functionality. Some extensions are mandatory or optional. Also, when we decided to design this ecosystem on the RISC-V, their software ecosystem were not as mature compared to other architectures, like x86 ISA or ARM ISA had much better software support at that time. And the remediation is that RISC-V partners are trying to close that gap and fulfill that gap. And there is also, whenever we work on the new extension functionality for the RISC-V, they also provide software support for those. There is also this kind of spirit that RISC-V becoming more and more popular, so open source community also starting to bring in more support for those. However, speaking about the profiles, they are great at addressing the fragmentation, but there are still some caveats there.

They may not always be mutually compatible. For example, RVA is not compatible with profile RVB. Also, even RVA profile does the best job to be backward compatible, still there are some caveats. For example, profile RVA22 has vector extension as optional. However, RVA23 mandates vector extension, so there are still some caveats. And also, custom extension must be actually costly, and so we always say, please contribute to the RISC-V. Because maybe you have a problem that nobody else had at that time, and you design your own custom hardware solution for those, yes? However, in the future, maybe someone else made the same problem, and they developed also solution in the hardware for that problem, but together with the RISC-V. Then you will find out yourself that your custom extension might be incompatible with the official ratified one, yes? So please contribute to the RISC-V to avoid this problem.

And also, new extension may actually solve your problem in the better or maybe not way, so contribute to RISC-V. And also, if you have custom hardware, it means you also must have custom software support. Nobody else is going to do it for you. So it's better just to contribute to RISC-V and make this ecosystem grow together. So also speaking about the Peregrine, as Marco mentioned, Peregrine is much, much more than just RISC-V.


## [12:11]


There is other processors, and there is other interesting components also from the security perspective, but still in the core of itself, the execution environment is still based on the RISC-V. In our implementation, which is NVIDIA RISC-V. So how do we secure this new execution environment? At first, we wanted to learn from the past, yes? We don't want to repeat some mistakes. So we decided we do not intentionally, we do not want to implement ASLR because we believe it has limited value in our specific execution environment. But it's still not free. You still need to pay cost of having ASLR. So we decided to use these resources instead of implementing something which has limited value in our ecosystem to be something more, like, it's better from the security perspective. It's more efficient from the security perspective. Instead of buying these resources for something which, you know, would have a limited value.

Also, Peregrine, it's not the only one ecosystem there. There is other Peregrines in the die. As I mentioned, there could be between 10 to 50 Peregrines on the one core. Some Peregrines have different security levels, some have different threat models, some have different warranties. We need to take this into account when we design the ecosystem. Also, how do we communicate between this ecosystem or between these different privilege level Peregrines? We must have a complicated software and firmware. So we found out that still the software is the biggest attack surface for us. So how to actually effectively find the software vulnerabilities? We have a video of anti-security research and among other things, we also do obviously manual vulnerability research because this is something which we must have, that everybody must have. But actually, that's not sufficient because it's neither scalable solution, yes?

We cannot just rely on those. It's something which we must have, but it's not enough. So we obviously need to have also automatic vulnerability detection because it's a crucial piece to automate some of the processes. So fuzzing can help here. And how to increase the effectiveness of the fuzzing? Obviously, we need to have some other sanitizer and code coverage driver instrumentation. That can help, yes? The problem is that RISC-V did not support that at that time. So what we've done, we've actually proactively modified the compilers to be able to bring other sanitizer to RISC-V ecosystem. Over the time, also software community pick it up and now everybody can use other sanitizer on the RISC-V. However, when we're starting to use it, we realize it's still not fast enough for our ecosystem because it's very complicated. So we needed to have hardware support for the better fuzzing and automation.

So that's how I created pointer masking extension for the RISC-V. And the main goal for this is aim to add hardware support and memory tagging later to RISC-V ecosystem, especially to our ones. But our ecosystem is unique. So among standard use case like S and U mode, we also have unusual use case. We need to support M mode. We need to support bar metal code for fuzzing microcode, yes? We also needed to support with and without OS layer. So because of those, I created on custom developed extension and brought it to RISC-V international to the TE group, which is now rebranded to Runtime Integrity.


## [15:12]


However, when I've done that, I realized and I met other folks working on the similar problems. It turns out that independently people from the Google were also working on the somehow similar pointer masking solution. So instead of fighting between ourselves, we decided to unite and our use case and promote one single standard for everyone because this will be beneficial entire ecosystem, yes? So that's how pointer masking extension was created. And at first, the main principle of pointer masking is to serve as a framework for any feature who requires masking the pointers. And there's various more use case. They could be hardware agent and member tagging like we wanted. They could be pointer authentication code. They could be hardware memory sandbox. Like you can see the visualization on the right side, top right slide, because it allows you to create in-process memory sandbox. But during gratification process, we simplified use case only to hardware ASAN and to be able to have the best integration because there is so many corner cases and to be able to serve as a framework, you must solve all of those problems.

And we actually achieved that. And we also created a mode that you can have up to seven bits masked from the top or up to 16 bits masked from the top. And also in the meantime, we were working with the risk of international to be able to include pointer masking as a part of the profiles. So some of the profiles actually mandate pointer masking to be implemented in the hardware and other profiles make them optional. And in total, we spent more than four years of work to be able to introduce pointer masking. And it's actually not one extension. It's umbrella for five different extension, which is split per privilege level. And also there is additionally two virtual kind of extension that only describe execution environment, but they have no bearing in the hardware implementation. Just mandate that you must have support of pointer masking in the U mode or S mode. And there is some method to be able to enable that.

We also obviously create software ecosystem support for hardware ASAN which is listed here. And internally, when we actually finish all of those work, we added the software support for our customer ecosystem. So as of today, we actually run the GSP firmware with the hardware ASAN. And we're also running as of today resource manager microcode also with the hardware ASAN. And we're also working to enable hardware ASAN on the bar metal files in microcode as well as the firmware under RTOS. So there is more people involved in and that's the list and kudos to everybody who are involved in this great research. Another extension which we believe makes sense, it's very effective from the security perspective, is actually hardware control flow integrity. Not software CFI, but hardware CFI. I'm not going to describe how the ROP and return-oriented programming works because it's DEF CON, so I assume everybody knows how it works.

But this is some visualization on the right slide. However, CFI protects against code reuse attacks, protects against return to rip-seal, protects against the ROP, drop, and this type of attacks. And CFI is actually a sub... it's a two extension. There's two sub-extensions inside of the CFI. One of them is a shadow stack, which enforces backward edge control flow integrity, and it creates a new region called shadow stack, which keeps the copy of return address.


## [18:20]


It's visualized on the right side of the slide. And it also preserves original stack ABI, and we introduce new register and also new instruction for shadow stack management. And before we return, we just compare if the return address from the shadow stack, it's equivalent on the sign which is in the normal stack. And the shadow stack region is protected by the hardware, so attacker doesn't have access to it. So then you cannot actually overwrite the copy of the return address. And if the return address doesn't match, the hardware will realize the exception, and we can kill the process or however we want to. And we also believe that this extension is so powerful that we can actually remove the stack cookies and replace them with the shadow stack. So because of doing that, we do not blow up the code, because the size of the instrumentation of the function code will replace the one which is used by the stack cookies, so the code will not grow in size.

And it's much more effective than stack cookies, so why keeping both? And again, the second extension under CFI calls the landing pad, which enforces forward edge control flow integrity as opposed to backward edge on the shadow stack, that it's actually mandated any indirect branch must always be landing in the landing pad instruction. It's a new instruction. However, this new instruction additionally encodes 20-bit label. And whenever we have enforcements of the landing pad, then any indirect branch must always land in the landing pad. Additionally, this landing pad must always have correct hash. It can't be just random landing pads. It must be specific hash. And because we have 20 bits of the space to use for the hash, we actually can reduce the collision problem with other ISA architecture have, because our label is much longer than other architecture. So as today, we believe the RISC-V hardware CFI is the strongest CFI ISA implementation.

And again, we also are working with the RISC-V profiles and RISC-V international to include CFI profile. And actually, hardware CFI is mandated to some profile and optional to another one. We also have software ecosystem support for those. And inside NVIDIA, we are adding software and hardware support for this, and we committed to bring it to the production in the Ruby timeframe. And again, there's more people involved in the CFI than just us and other folks. The list of everyone who are involved of those, and especially kudos for the George and who are chair and co-chair of this group. And also, what's next? What we are planning to? Obviously, memory tagging, because this was the main idea also behind the point I'm asking, yes? It addresses the performance issue, which you can still have with the hardware ASIN. And additionally, memory tagging is targeting to be enabled in the production binaries.

Also, we are obviously actively contributing to the RISC-V memory tagging extension, and we want to bring support for this in the software and the hardware and ratify this extension. So, basic concept, whenever you allocate the memory, then pointer will get the special tag or color, however you call it, assigned to that pointer. Additionally, the memory itself will also have another separate tag or separate color, which will have actually the same value as the one assigned to the pointer.


## [21:25]


So whenever you reference the memory, the hardware will compare if the tag matches the one in the pointer with the one in the memory. If the tag matches, it means you're inbound of access. Otherwise, the hardware will detect that you're touching different tag, different color, and tries the exception. So it's actually kind of very effective and different approach to detecting the problem, because it can detect any type of out-of-bound access, memory corruption, use after free, all of those, it's detectable by the hardware. But what is important, it's detected before corruption happens. Most of the mitigation detects post-corruption. This is before corruption. So there's no chance for attacker to do anything yet, because hardware will kill it in the root cause before the corruption happens in the first place. Another extension which we're working on now, we're trying to extend the SafeEye M mode, ShadowStack to the M mode, actually, because landing pad is defined in every mode, but ShadowStack is only defined, not defined in the M mode.

So we want to extend ShadowStack to the M mode as well, using PMP to be able to protect also critical M mode software. And another extension which we're looking at is actually Hardware Fault Isolation, HFI, which addresses in-process memory sandbox, which was removed from the original pointer masking, but this is very interesting use case. We are interested on that feature as well. There's no TG yet, but we are evaluating the benefits of bringing such sandbox to our software ecosystem. So how it works? In principle, HFI introduced something like user mode concept of memory region. Doesn't need to be on the user mode, but it can be enabled also in the user mode. It's similar to concept of x86 segmentation. So any access outside of this region, predefined region, will generate a hardware trap. So before what you do, you generate the list of the base length and attributes of the sub-regions, and whenever you want to execute untrusted code, you just enter to this sub-region, and then hardware will enforce that you cannot touch anything else, excluding the predefined hardware region of the memory.

And it's enforced by the hardware. And there's another interest of interest for us, which is post-quantum cryptography, PQC. We also evaluate side channel protection hardening. We're also looking in the cherry, and we're also looking on the enhanced hardware fault injection protection. So what I was speaking about, it's a hardware foundation security. What about the software? So this is what Marco will speak about next. Thank you, Adam. Now let's go over software layers that form this foundation. So our partition design draws inspiration from multiple independent levels of security and safety, or MILS architecture that's common in higher assurance systems, like in defense and avionics industries. So again, partitions are isolated execution environments defined by manifesting partition policies, which are signed encrypted static configuration sets that have fine-grained configurations for hardware access control primitives we have.

So essentially, bootrom first parses manifest and then configures the hardware such that it limits capabilities and the maximum privilege for the rest of the software that comes on that core.


## [24:34]


And partition policies are parsed by separation kernel, which then further configures hardware and thereby defines a partition where application software runs. Important thing to note here is that manifest and policies are signed separately from partition images, so nothing done within partitions would affect policy configuration. So at any given time, only one partition is active per core, and all the information flow in and out partitions is access controlled via policies. So the separation kernel is that software component that creates and manages partitions. It is not a hypervisor, so it only controls what hardware is exposed to partitions, it does not obstruct any hardware, nor provides any high-level functionalities for partition software. During a partition switch, separation kernel would clear out the previous partition state, then parse the target partition policy, reconfigure the hardware, and then transfer execution to that target partition.

From separation kernel's perspective, scheduling is simple, it's cooperative, but different scheduling policies can be built on top. And there are mechanisms to detect when a partition is stuck, preempt it, and then transfer execution to a predefined fallback partition. This kernel is small in size, it is written in ATA Spark programming language , and it is formally verified to be free of runtime errors. But before separation kernel, we have the bootstrap, which is immutable in hardware, embedded in hardware, and also written in Spark language. Because of its nature and importance, we added specific extensions to further secure its execution. For example, the ROM protection here means that the ROM is split into sections, such that after each phase of bootstrap execution, hardware will disable the previous state, previous section. So even if you somehow manage to get the core to jump back, it won't work.

And we have execute-only memory, so the code cannot be dumped out. All functions are leaf functions, so there is no return address, bill, and stack. And during the core software execution, the core is in lockdown state, so all external access is disabled. We have multiple glitch attack mitigations in hardware, but what I added here is a reference to Adam's talk, where they explain details of a flaw they found in RISC-V ISA itself that makes glitch attacks more successful. And as I said before, depending on what microcontroller does, some cores may be in DCLS mode. Essentially this means that there is another core that's in lock step with the main core just delayed by a few cycles. So if there is a mismatch between the execution, it means something off is going on and it will be detected. This is added for safety, but it also makes glitch attacks less successful. Harder. Okay, now let's talk about language-based security a bit.

As I mentioned Spark multiple times now.


## [27:35]


So traditionally we rely on tests to show program's correctness, or to point that there is a security vulnerability somewhere in the code. And this is good, but there are no guarantees that you won't have a single overflow in a large C code base, for example. But with Spark we can prove that whole classes of vulnerabilities will not ever happen. And we do that through contract-based programming, and due to the fact that the Spark is very strict subset of Ada language with formally defined specification. So here I have one simple procedure in pre- and post-condition contracts. And this is the implementation. So with Spark we can prove that this implementation is correct given the contracts. And this is done through this high-level pipeline that involves multiple steps and some fancy tools like SMT solvers. But the beauty with Spark nowadays, and the tools we have available with it, is that we can abstract all that complexity with pruning away, and use Spark and its tools to get to a formally verified implementation without knowing the intrinsics of the SMT solvers or the underlying mathematics of it.

So the pipeline would actually look more like this. So all that complexity with pruning is let's say hidden under this tool called Gnat prove, which you would use through this iterative process and if some property cannot be proven, Gnat prove would give you counter examples of why something doesn't hold. And then you would go back, change specification, implementation, or even the contracts, and do it again. But most likely you won't be able to implement 100% everything you want with just Spark. So you would need to make calls into A to C or assembly. In this case, you would test for what you cannot prove. Essentially, if you make a call into C, your preconditions should be tested because non-Spark code is invisible to Spark and its tools. And you should make sure that there are no side effects to that non-Spark call or at least assert and test for expectations that the Spark environment has for those.

Now you may be wondering why you can't do all this with C, Ada, or Rust, for example. And what follows is my modified section of a talk. I saw ones that I really liked that explains the fundamental difference between the languages and why we can use Spark to do all this. So let's say this is a finite machine state set of a given machine, so all possible combinations of memory, registers, everything. And this would be a subset of that machine state set that is attainable through a programming language. And the green box here would be machine states that fall under correct execution of your program, so the thing that you want to build. And C is the most powerful language. It tends to extend its state set that it can attain, while Ada, Rust, and other languages are more strict, so they have a reduced state set.


## [30:42]


But Spark is a very strict subset of Ada programming language with no ambiguities, with no undefined behavior, with formally defined specification. And this is the main reason why you can rely on Spark, build on top, use the tools I mentioned, and get to a formally verified implementation for something. Again, most likely you would need to use these escapes, as I call them, to a non-Spark world and make calls into C, Ada, or Assembly, in which case you'll fall back to testing and test for what you cannot prove. So the main point here is to minimize usage of those and to keep them very small so that you can rely on testing and manual verification. But if you have most of your code in Spark, or let's say some sub-components 100% written in Spark, having this guarantee that there will be no runtime exceptions at any given time for any given input is extremely valuable for both security and reliability perspectives.

So coming back to the original architecture diagram, this multi-partition architecture is our foundation for running mixed criticality applications on a single MVRIS5 microcontroller. This isolation between partitions is done through hardware access control primitives that are only configurable by core software that is formally verified to be free of runtime errors. The AORT, or absence of runtime errors, is a common term used for this. Now there are certain attack vectors that you can't really defend against via language only, and side channels would kind of be the obvious ones. So we designed our hardware and configured it such that we mitigate against those. Although, theoretically, it is possible that you model all the bits of the hardware, so all the branch predictors and caches, and model that in software and then use that model to prove that your software mitigates against these. But this is extremely complex and a much more practical approach would be to combine software techniques with proper hardware configuration to mitigate against these kind of attacks.

I was thinking about top five takeaways for all this journey of ours, and I would say if you have a chance to design something from ground up or redesign something, first you should think holistically and start a design from both top down and bottom up. So things like confidential compute and virtualization introduced requirements we had to consider from the very beginning when we started building this foundation. I do understand that not everyone is in a position to do hardware-software co-design, but when building a large ecosystem, a hardware-software co-design is a must. Being just different does not imply innovation, so standards are good, and it does pay off when standards are followed, so innovate when you must. And we saw how memory safety is a harder problem as well.


## [33:43]


But ultimately there are no silver bullets in security, and defense in depth remains the most reliable guiding principle. And now over to Adam, and he's going to close with some lessons learned. So yes, what are the lessons learned? So actually, hardware extensions are not enough, yes? The biggest attack surface is still actually a software, as we discussed at the beginning. So we must co-design hardware and software to be able to create a secure execution environment, and some hardware extensions that we discussed actually help doing that. But that's not enough, because even in the bound of the hardware limitations, software can still misbehave, and there's some room for the playground. So that's why it's so important to co-design and software and the hardware. There's no silver bullet in just one layer. Also, formally verified language like Spark or memory-safe language like Rust, they are great.

They're awesome. And they have significant security return of the investments, but costs are actually substantial. It's not cheap, yes? So it's very likely we are going to live in the hybrid world for a while, because either we cannot afford to rewrite the software in the more secure language, or maybe we must rely on the third-party library, which we do not control, and they're just not going to be rewritten at all. We just don't know. So it's very likely it's going to be a hybrid world for a while. And also, it's worth to keep in mind there is also non-memory safety issues. Any type of the language, yes? And we actually make a talk about the bugs on the formally verified Spark language on DEF CON 30, so we recommend to watch on this. And I think this is the most important bullet from the entire talk. So actually, how to create innovative ecosystem? You actually need to have, it's a demand, you must have a forward-thinking mindset.

At first, you don't know how you're going to evolve, so you must be flexible. You must be flexible enough to be able to have an opportunity to evolve your, to have an evolution of your hardware and the software in the way that you might not even know from the beginning. But you must be flexible enough to be able to have such kind of primitive to be flexible, yes? And also, how to do it? You must kind of predict the industry trends, some kind of informed prediction of the industry trends. How to do that? You must be part of the various initiatives and organizations, because this, actually, see where the entire industry is moving, and you can have informal prediction of where it's going to move anyway. And sometimes, you know, it's not obvious, but you can read them from the air on this type of discussion in the organization. And also, scalability, flexibility, reliability, performance, and security must be taken collectively, not separately.

It's very tempted to be able to optimize, for example, reliability on the cost of the security, but then you will lose the flexibility of your ecosystem, which means you may or may not adopt the future threats, which can arise and you don't know. And also, we believe that hybrid attacks, not just pure software attack, not just pure hardware attack, are going to be on the rise, and they're still rising now. We see the attacks like RawHammer, side-channel attacks, or even the bugs in the ISA, which Marco referenced, those type of attacks, neither hardware, neither software, it's between.


## [36:51]


They are hybrid attacks, and we believe they are going to be on the rise. So that's it what we have today. If we have any questions, we are happy to answer. Thank you very much.
