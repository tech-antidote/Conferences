---
title: "Infecting the Boot to Own the Kernel - Bootkits and Rootkits Development"
speakers: ["Alejandro Vazquez", "Maria San Jose"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Infecting the Boot to Own the Kernel - Bootkits and Rootkits Development - Alejandro Vazquez, Maria San Jose.eng.txt"
sha256: "2364dcd758c6a06e3b8501e3c537cda2b44f0fda1e4619a8e98bae5776aacf44"
duration_seconds: 2364
words: 5332
text_chars: 29906
redacted_secrets: 0
converted_at: "2026-08-12T06:24:14Z"
---

# Infecting the Boot to Own the Kernel - Bootkits and Rootkits Development

**Speakers:** Alejandro Vazquez, Maria San Jose  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (39 min, 5,332 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Hello, everyone. It's a pleasure for us to be here at DEF CON 33. And honestly, it's exciting just to see so many people here interested in building this malware. So thank you for coming. I'm not going to ask questions. I suppose you want to develop malware, but whatever. Before diving in, we want to make something very clear right from the start so everyone is on the same page. This is not a talk about theoretical bootkit and rootkit development. What we are going to share with you today is a fully functional working piece of malware that we have developed. And in a moment we will explain why we have chosen to make it public. So during this talk, we will share with you a complete documented bootkit ready for you to use, build and study. Alongside that, a rootkit as well that implements some cool techniques to modify the Windows kernel. We will also point you to our repository with over 400 great resources that you can use to carry out your own research into bootkits and rootkits.

And because setting up the environment can be half the battle, you will have access to a set of scripts we use to automate the entire setup. And by the way, this talk ends with a demo, a real vulnerable computer we have right here. We have right here this Lenovo. Showing the bootkitting action with secure boot enabled, taking control of the Windows kernel and running the entire malware exactly as designed. We hope you enjoy. My name is Alejandro, I'm a geek, which of course stands for a gentleman, as you can see. A gentleman exploring UFI and OS kernels, or at least that's what I like to say. And she's Maria. Hi everyone, I'm Maria, also a geek. A girl who explores exploits and kernel threats. You can find our full biographies on the official DEF CON page. But to summarize, I work in a law enforcement cyber crime department where I've spent the last few years analyzing malware. Don't worry, I'm not working right now, this is a safe place.

Meanwhile, Alejandro is a red team operator focused on Windows internals. So we are two geeks with two different perspectives, two octopus lovers ready to publish a boot kit for the entire world to use. And the question is, why? Why are we publishing this? Well, we want every offensive team out there, every red teamer to walk away from here today with a real boot kit and a root kit they can actually use in their operations. And the knowledge to develop their own. Instead of spending a year crawling the internet, piecing together scattered bits of information and trying to figure it all out, you'll have it today, ready to customize, improve and build your own in just a few months.


## [03:12]


This entire subject fascinates us. And we believe it shouldn't feel like some kind of dark magic. Building a root kit and a boot kit isn't about having the secret knowledge look away. It's about having the right guidance, the right resources and the time to learn. So over the next 40, 45 minutes, we're going to explain everything in a very straightforward way, plain language. We want absolutely everyone to walk away with a clear understanding of what this is and how it works. That's what this talk is about. So let's get this started, guys. A boot kit is a program designed to implant itself as early as possible in the system's boot process. It gains control over every stage of the system's startup and modifies the entire boot flow, which we will break down shortly. How does it do that? By hooking and patching key functions in files involved in the boot sequence. Those hooks and patches are what allow the boot kit to keep executing while the boot sequence continues.

And to install components that remain alive even after the operating system takes control. A boot kit is just a root kit that runs before the operating system is loaded. So what is a root kit? A root kit is another program designed to run inside the kernel, the core of the operating system. Because it operates at the kernel level and the kernel manages memory, processes and interrupts, the malware can do almost anything. It can hide files, network connections, disable security protections and modify the internal structures of the operating system. Now, someone who wants to get started in this field doesn't really understand a damn thing about these definitions. So let's demystify it. On any system you will find kernel mod drivers, legitimate components that run at the highest privilege level inside the kernel. Now, if you take that same concept and make it malicious, you get what we call a root kit.

A malicious kernel mod driver designed to tamper with the kernel. And if the computer uses UFI BIOS to boot, then the starting point of the boot process is a UFI application. So a malicious UFI application is what fits the definition of a boot kit. At its core there are two main components we need to develop. Later we will see there are more. As the foundation, a malicious UFI application for the early boot stage and a malicious kernel mod driver to modify the operating system once it's loaded. And now that we have demystified them, it's worth mentioning, you will find official documentation from Microsoft and from UFI forum available to help you build both of them.


## [06:19]


This is not dark magic, it's just offensive software development. But of course it's not just about reading the official documentation. There are some amazing resources out there, almost like sacred texts that you simply cannot miss. If you really want to understand what's going on under the hood, start here. And of course, huge thanks to all the amazing folks that explored this subject before us and shared their knowledge, malware and tools with the entire community. Now it's our turn to give something back. By now we can summarize it like this. A boot kit is essentially a malicious UFI application and a root kit is essentially a malicious kernel mod driver. We already know the two main components we need to develop. But what actually prevents us from just installing them? Well, we are two protection mechanisms we need to talk about. First, we have security boot. Security boot is all about the chain of trust.

Enforced through digital signatures and cryptographic keys. When it's enabled, every component in the boot process must be signed by a trusted vendor and verified against a list of allowed keys. That's the point. Malicious UFI applications, boot kits, simply won't run unless they are signed by a key that the firmware trusts. But like any system, and we've listed a few CVs there, if you exploit a weakness , or in this case, you find a way to include your own key into the chain and sign your binary with it, the whole thing collapses. And once security boot is bypassed, every protection that comes after, including what the operating system tries to enforce, basically doesn't matter anymore. Because if you control the boot, you control everything. So what happens when two geeks find a machine vulnerable to one of these flaws? Well, that's exactly what you'll see in our final demo. The second protection we need to talk about is driver signature enforcement, or DSE.

If you already compromised the boot process, this protection doesn't really matter anymore. But let's assume you didn't. Maybe you couldn't find a vulnerability to break the boot signature chain. In that case, you can't install the full boot kit. But you can still find a way to install just the root kit part. The problem is, Windows won't let you just load a malicious kernel mode driver unless it's properly signed either by Microsoft or by a trusted vendor. As you can see on the screen, if you try to drop your sys file into the system and load it, you'll get an error saying the signature is invalid. That's DSE doing its job. But there are ways around it. And one of them is the technique called bring your own vulnerable driver.


## [09:23]


The idea is instead of loading your malicious driver directly, you first load a legitimate signed driver with a known vulnerability. And once that vulnerable driver is running, you exploit it to execute arbitrary code in kernel space and load your root kit from there. The vulnerability you use can be too well known. Because Microsoft maintains a block list of drivers which are already known to be abused for this technique. And it won't allow them to load. But at the end of the day, it's a constant kind of mouse game. As new vulnerable drivers are discovered, attackers use them, Microsoft eventually blocks them and the cycle keeps going. Now let's quickly recap. A UFI boot kit starts with a malicious UFI application. A root kit starts with a malicious kernel mode driver. To install either of these components, there are protection mechanisms you'll need to bypass. There are public vulnerabilities, public exploits and proven techniques you can adapt to get each part, each component of the boot kit we're sharing up and running.

So to understand how our boot kit or root kit behaves, you first need to understand the boot process. And here is the general idea. When you press the power button, the system performs an operation called power on self test or post. A process that checks the hardware to make sure the essential components are working properly. After that, UFI framework is executed and it looks for the files and partitions required to boot. One of the first files it loads is the Windows boot manager, a legitimate UFI application found in a special disk partition called the ESP, EFI system partition. The Windows boot manager loads the Windows OS loader, which loads the operating system into memory. And from there the operating system takes control and initializes its main subsystems. So we have three key files involved in this process. The Windows boot manager, the Windows OS loader, and the Windows NT kernel.

Three files you will want to load into your disassembler, reverse engineer to understand their inner workings, and figure out exactly which functions to hook and patch, just as we mentioned earlier, in order to take control of the process. This is the simplified explanation of how the malware works. Remember, each component in the boot chain loads the next one into memory. So at specific moments in the process, the next component, the next file is already in memory, but it has not yet taken control. That's the key. Those moments are when we can hook functions inside that next component, keeping our code in execution as the process continues.


## [12:24]


We begin by replacing the Windows boot manager with our own malicious version. This malicious version, or UFI application, hooks a function inside the firmware responsible for loading the original boot manager. But instead of immediately passing control to it, our code ensures that the original boot manager is fully loaded into memory first. Once it's loaded, and this right here is one of the most important lines among the thousands that make up the boot kit, once it's loaded in memory, we scan the memory using a known pattern to locate a specific function in that file that is called when the next component is already in memory. Then we hook it by patching its entry point with a small assembly trampling, redirecting the execution to our own custom handler, and then back to the original function, keeping our code without breaking the boot process. We repeat the same approach at each stage.

So inside the Windows boot manager, we hook a function that is called when the OS loader is already in memory. Then inside the OS loader, we hook a function that is called when the anti-kernel is already in memory. Hook by hook, stage by stage, we maintain control through the entire boot process. But these hooks don't just keep the boot kit alive. Along the way, we also use specific functions in these files to drop the root kit, the malicious kernel mod driver, in memory at the right moment. So the root kit is ready and running by the time the operating system starts. And now let's see a quick demo. The first one. So here we have an unsigned kernel mod driver. We're going to try and load it in a common way and you're going to see an error because the DSE is enabled. So what we're going to do now is perform an advanced restart and we're going to change the way we boot. We're going to boot into the ESP, the EFI system partition, because in a specific folder in that partition we have our malicious EFI application.

We're going to go to that folder, we're going to execute that EFI application and the EFI application is going to modify the entire boot flow. And we have configured the application to drop the root kit in memory. But we provide you with a tool to create the EFI, sorry, to create the configuration the EFI application uses because the EFI application can drop something in memory, modify partitions, can disable security protections. But in this demo we have only configured the EFI application to drop the root kit in memory. So as you can, as you are going to see now in a moment, there is something in kernel space that is printing, hey, hello, hello, hello.


## [15:31]


There is something in kernel space that can modify the entire operating system, how the operating system works. And we're going to explain that in a moment but you can also use that, a user mode application like that, hello.exe to communicate with the kernel mode driver. And something important to note is that the EFI application can disable DSE, of course you can configure the application to do it. But it's not necessary, it's not mandatory to drop the root kit in memory. In fact as you are going to see in a moment, DSE is still enabled because we saw, we saw that the root kit is already in memory. Now let's take a moment to talk about the power of the UFI specification itself. When we think about a boot kit, it's easy to just focus on hooking boot functions but UFI applications are not limited to redirection of patching. A UFI application can interact with a wide range of low level services because the UFI specification spans thousands of pages covering protocols for managing memory, file systems and even networking.

That means the first element of the malware we build before the operating system even loads can fetch files from a remote server on the internet, modify partitions, create loads and leave markers for later payloads. In other words, you can do almost anything you want using a UFI application. And a word of advice, if you ever decide to go through all the thousands of pages of this specification, don't bother with coffee. It's useless. You need at least a couple of beers. Continuing with UFI, UFI defines two major service categories. Boot services which are only available during the early boot process is the blue zone. And then run time services which remain available even after the operating system has taken control. That's the orange zone. This distinction is crucial because every UFI application relies on boot services. So all the memory and resources it sets up are discarded once a call is made to terminate boot services and the operating system takes control.

We've already loaded the root kit into memory by this stage. So losing boot services doesn't really hurt us. But if we design our code with this in mind, we can take advantage of run time services to establish something during boot that persists even after our application itself is gone.


## [18:33]


One way to achieve this is by developing a DXC run time driver. A special kind of UFI driver that stays in memory after the boot and allows you to hook run time services. In other words, it plans hooks that remain active while the system is running. This gives you an almost invisible back door which allows you to execute arbitrary payloads and read or write anywhere in memory. So at this point, what is our boot kit in practice? It consists in two main components. First, a UFI application that cooks key functions to stay alive through the boot process, drops the root kit into memory at the right moment, disables security protections if needed, and takes full advantage of the UFI specification. And second, a malicious DXC run time driver acting as a back door that can read and write in memory while the system is running. So let's see the second device. So as you can see here, we have an HTTP server up.

We're going to do the same we did in the first demo. We're going to boot into the ASP and in a specific folder now we have, we're going to see two files, the UFI application and the DXC run time driver. We're going to execute the UFI application and the UFI application is going to download a UFI utility in the disk. We don't use that UFI utility in this demo, but if you see the source code that is completely available, you're going to realize what new capabilities this UFI utility gives me. The UFI application is going to load the DXC run time driver in memory too. And it downloaded that file. And now the DXC run time driver is in memory because the UFI application loaded. We have developed a tool to communicate with the DXC run time driver. Something important to know is we, give me a moment, I have to pause here, you have to execute that UFI, sorry, you have to execute the program we developed with admin privileges.

That's very important to communicate with the DXC run time driver. So we're going to use that specific tool first to check if the DXC run time driver is in memory. He's going to say, oh yeah, I'm alive. So now we can drop another malicious component in memory, we can execute payloads, we can execute shell codes, we can read and write anywhere in memory.


## [21:41]


And we have configured specific functionality to communicate with the DXC run time driver and say, hey, I want you to load this rootkit. I want you to load this malicious kernel mod driver. And it loaded. So now you have two ways to load something in memory to load the rootkit. You can use the UFI application or you can use the DXC run time driver to drop something in memory that as we are going to see now in a moment can modify how the operating system works. The core of the malware and what truly gives the bootkit its power is the rootkit. The rootkit is the component that lives in kernel space, operates with the highest privileges and enables deep manipulation of the system. From intercepting callbacks to hiding files and network activity, the rootkit is what delivers full control. In fact, everything we have discussed so far, UFI, hookings, early boot hijacking, run time drivers, has just been the setup.

It's all part of the infection chain that ensures the rootkit runs reliably. As you can see in the right circle, a rootkit, it could be, but most of the time a rootkit is not just a malicious kernel driver. There are more components that make up this concept, all working in a coordinated way. So the question now is what makes this rootkit we have built, this driver , a good one? What key aspects did we focus on to give it a solid foundation and meaningful capabilities? Well, there are more techniques implemented, as you will see in the source code, but we think these six form the essential foundation to start with. First, communication. A good rootkit must be able to communicate either with a user-learned application, with a command and control server, or both, while still being capable of acting autonomously when necessary. This is what allows you to send instructions, receive responses, and adapt the driver's behavior on demand.

Second, direct kernel object manipulation. This is what allows the rootkit to, for example, do something as fundamental as hiding a process. And while that may sound simple, now in 2025 it has to be done the right way, otherwise you risk a protection and crashing the system with a blue screen. Third, keystroke logging. It's also possible to capture keystrokes at the kernel level, intercepting some kind of packets.


## [24:45]


Fourth, network filtering. By leveraging the Windows filtering platform technology, the rootkit can alter, block, you know, capture network traffic in real time. Fifth, kernel level command and control manipulation. As you can see on the screen, Winsock kernel allows the rootkit to make HTTP connections entirely within the driver. So enabling a full command and control in the internal space. So this is very important. You don't need a user land process to communicate with the server on the Internet, download files or even handle network connections or communications. Because the driver can handle all of it on its own. And finally, file system filtering. There is a special class of drivers that many security solutions use called mini-filters. And the rootkit can also make use of the underlying technology behind those drivers to hide and deny the access to specific files and folders, effectively preventing the operating system and other tools from analyzing them.

So we're going to see a rootkit with these six capabilities. We're going to boot into the ESP. In the ESP we have a malicious UFI application. We're going to execute the UFI application. The UFI application is going to modify the entire boot flow and it's going to drop all rootkit in memory. We have developed a user mode application to communicate with the rootkit. You don't need admin privileges to open this, to execute this application. We're going to use this user mode application to communicate with the kernel mode driver and say, hey, the first thing we're going to try is hiding a process. We're going to try and hide the one drive.exe process with that specific bit. It's going to fail because we have to execute some commands to see if the kernel mode driver is in memory. So we execute them and now you're going to see how that specific line is going to be printed in red and the process disappears from the eyes of the user.

Well, from the eyes of the task manager and in this case the process hacker program. We can list the hidden processes and we can hide in an easy way only with the identifier and the process appears again.


## [27:52]


We can do the same with files and folders. So we're going to copy a specific folder here and we're going to communicate with the kernel mode driver and say, hey, I don't want the user to see that specific file. We reload it and it disappeared. We communicate again with the kernel mode driver and it appeared again. It's like there is something in the kernel space that modifies how the operating system works. We can block connections too. Maybe we don't want the computer or the user to be able to communicate with some kind of IP addresses, some servers on the internet, some range. We block those IP addresses and the user, the machine cannot communicate with them. There is something in the kernel space that denies those communications. And we can hide network connections too. We execute netstat, as you can see there are some TCP connections to the remote port 443. We communicate with the driver and say, hey, I don't want the user to see those connections, maybe there is a shell code or whatever.

We execute netstat again and the connections disappear from the eyes of the user. The thing is, you don't need a user mode application to communicate with the driver. You can develop some kind of alternative ways of communication. You can, for example, enable a keylogger. The driver can capture, as you are going to see in a moment, can capture every keystroke you type. So you can use the keylogger to communicate with the driver so you don't need the application. We are going to show you that we have categorized everything in different techniques. We have seen something related with the kernel object manipulation, keyboard filtering, WFP, mini filter and now WSK. As I said before, this is very important. You can enable a fully common and controlled channel in the driver. So you enable the command and control and the driver is going to call your server on the internet and your server responds, hey, I want you to execute this thing and the driver is going to execute it.


## [30:58]


In this version we don't provide you a full command and control in the source code. So you can find everything you need to develop your own command and control in the kernel space. But that is the idea. You can wait a few months and we are going to release that as well. So we cannot explain these six techniques in detail, but you have all the source code and not just the root source code itself but also a stand-alone proof of concepts for each example. There is a book just for communication, another just for how to hide a process, another just for the key logger and so on. That being said, understanding how a user mode application communicates with a kernel mode driver is crucial. Learning the fundamentals of how IOCTLs and how IRPs work is going to give you a solid foundation and will naturally prepare you to move on to exploiting vulnerabilities in kernel mode drivers as you progress in this area.

That's going to be something you are going to introduce yourself as you progress. I hate doing this but I don't have so much time. So communication, you have some work to do at home. So communication, kernel structure manipulation, keyboard filtering, network filtering, kernel level command and control, file system filtering, and of course the entire boot process infection chain we have been talking about along with the security bypasses we mentioned at the beginning, all of that brings us here. We are going to see the last demo. This is vulnerable by the way. So we are going to bring up a physical machine. The boot kit works in virtual machines and physical machines. So we are going to install the boot kit, UFI boot kit. You need admin privileges to install the boot kit. We are going to execute, sorry, we are going to pause it here. We execute a script. Everything is available so don't worry.

We execute a script to check if secure boot is enabled. And yes, secure boot is enabled as you see on the screen. But there is some kind of flaw in the UFI firmware, so who cares if secure boot is enabled or not. The machine is vulnerable. We install the UFI boot kit. We add a new boot entry by default. I'm going to, you know, the internet connection was slow and we have to download something. So the root kit, sorry, the UFI boot kit is installed.


## [34:04]


So now we shut down the machine. We power on again and the UFI boot kit is there. UFI boot kit, you know, you can configure the boot kit to not show the messages. The UFI boot kit, the UFI boot kit is dropped the root kit or root kit in memory. By default we have configured the UFI application to drop some kind of basic kernel mode driver, some kind of basic root kit in memory. So you can use that specific kernel mode driver. You can communicate with it and say, hey, I want you to load another kernel mode driver. We do it by default this way because maybe you want to load or root kit or maybe you want to load the proof of concept we give you to blind IDRs or you want to load your own root kit. So by default the UFI application dropped a basic kernel mode driver in memory and you can communicate, as you can see in the screen, you can communicate with that specific kernel mode driver and say, hey, I want you to load another one.

So this machine is fully compromised. There are no more levels. Now that you have seen the final demo, it's time to take out your phones and make a picture of the screen. On the screen you will see six QR codes. This gives you access not just to everything we showed here today but much more. Starting from the top row, left to right, the first QR takes you to the repository or UFI boot kit project. It includes the UFI application, basic kernel mode drivers that UFI application can drop in memory, the driver, the tool to create the encrypted configuration UFI application uses, and a massive amount of documentation on how to get started in developing boot kits. So everything you need to develop your first UFI boot kit from scratch is there in the first QR. The second takes you to the Bantic repository or full featured kernel mode boot kit.


## [37:08]


You will find the kernel mode driver, the application to communicate with the kernel mode driver, and a dozen proof of concepts for each technique implemented and documentation as well. The third QR takes you to a repository with hundreds of resources you can use to dive even deeper into the subject. On the bottom row, the first QR, I think it gives you access to a set of scripts you use to automate the entire development environment set up. You need to configure something to develop boot kits and root kits in a machine. So you can execute over scripts and it's completely automatically. The second QR, that's a good one, the last one, there is some notes there to install the root kit on a physical real machine. And the last one includes some notes to help you analyze your own UFI firmware and check for non‑vulnerabilities. We believe this is the first time so much documentation and code has been shared with the entire community to help you build both a boot kit and a root kit.

But that was the idea, to provide anyone looking to deliver adversary simulation services with the knowledge to emulate the kind of malware only APTs deploy. And the last thing we want to say, as you can see on the screen, our lovely dog, Shira, passed away recently and without her we wouldn't be here. So you all owe her a big thank you for making this talk. And that's all from us. If you want to see, if someone wants to see a live demo, we have the vulnerable computer right here. We are very approachable people and we have some cool stickers too. So that's all from us. Enjoy the malware. You are not going to find anything like this on the Internet. This is very, very cool. So enjoy it. See you soon.
