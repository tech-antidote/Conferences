---
title: "Safe Harbor or Hostile Waters - Hidden Perils of the TorchScript Engine in PyTorch"
speakers: ["Ji'an Zhou", "Lishuo Song"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Safe Harbor or Hostile Waters - Hidden Perils of the TorchScript Engine in PyTorch - Ji'an Zhou, Lishuo Song.eng.txt"
sha256: "a0f04d6333af1ff06092a9888afccd7f81567ad70b2fc9b5b05c27e4c2e7c921"
duration_seconds: 2080
words: 4286
text_chars: 24815
redacted_secrets: 0
converted_at: "2026-08-12T02:50:00Z"
---

# Safe Harbor or Hostile Waters - Hidden Perils of the TorchScript Engine in PyTorch

**Speakers:** Ji'an Zhou, Lishuo Song  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (34 min, 4,286 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


So we have a pre-recorded video, pre-recording from the speakers, and we're going to get started right now. Thanks. I completed this work together with my colleague Lishuang Song, and we were really looking forward to presenting in person. But unfortunately, due to visa application issue, we are unable to attend the event on site. And we sincerely apologize for this. And as a result, we have to share our presentations through a pre-recorded video instead. And if you have any questions, please feel free to contact us. So here, this is the agenda. So first is the introduction section. And after that, we will share where the story begins, and then I will analyze how it only works, and introduce the TorchScript engine. And finally, I will share the impact of this vulnerability, and share some defense recommendations and summary. So first, introduction. And PyTorch is a machine learning framework based on Torch Machine Learning Library, and it was developed by Facebook, and now it is an open source project.

And it is very easy to use and has some really great features. And PyTorch is widely used in the field of deep learning research, natural language processing, or Corning LP, and computer vision. And to be honest, in the field of machine learning, there are many, many popular frameworks, and well-known projects like TensorFlow and Keras. But however, according to the paper with Kodo website, and you can see that PyTorch has undoubtedly been the most popular machine learning framework. If someone can find some bugs in such a famous AI framework, and that will be a really cool thing. And next comes the second part, where it all began. And in the early days, PyTorch used Pico to load and save models for convenience. However, Pico is one of the most well-known security issues in Python, and in fact, the official Python document was that using Pico can be unsafe, especially when loading untrusted data.

So in 2021, someone raised an exception on GitHub and called for a solution to address the Pico issue in PyTorch. And finally, in 2022, PyTorch released a notable update introducing the waste-loading parameter. And let's briefly analyze the implementation of waste-loading. As you can see that by default, waste-loading is set to false, and it will use Pico to load the model. And if we set waste-loading equal true, you can see that it will use something called waste-only and Pico, and you can consider it as a restricted Pico. So now, let's try it out.


## [03:01]


So first, since by default, the waste-loading is false, and we will use Pico to load the model. So here we can easily create an eval model and use this reduce magic method. And you can see that after we save the model, and then we load this model, and you can see that we successfully execute the system command and we get the output route. And however, if we set waste-loading equal true, and you can see that it just threw an exception, and it seems that the vulnerability is effectively mitigated. And also on the official PyTorch website, and you can see that the consider-toys-to-load with waste-loading equal true is also secure. And it also warns that loading and trusting checkpoint with waste-loading equals false must never be done. And additionally, very similar security issues are discovered in other PyTorch-based components, and they often fix it by using waste-loading equal true. And here is an example.

So initially, VLM used torch.load with all waste-loading equal true, and which introduced a security risk. And they fix it by simply add the waste-loading equal true parameter. And so not only VLM, and in general, the number of software is found to be using torch.load without waste-loading equal true, and the fix is also the same way, just adding the parameter. But here comes the question, can we blend in trust and security or waste-loading? So next part, we will dive deeper into it, how waste-loading actually works. And earlier, we point out that waste-loading is something like a restricted Pico. So before we analyze how waste-loading is implemented, and we need to understand how Pico works. And Pico is something like an interpreter, and it reads and interprets each opcode in sequence, and it's executing the corresponding operation. So for example, here, we first encounter the global opcode, and it will use the load global function.

And you can see that it will call found class, and it will import this model. And so here, after we execute this command, and you can see that it will get the system function and push it onto the stack. And since now, we get the function, and we need to get the arguments. And so here, we can use the unicode opcode to read the string. And so here, we will get the Huamai. And since arguments must be in turbo format, and we need to wrap this string into a turbo. So we use this turbo1 opcode, and you can see that it will replace it with the turbo. And so here, after we execute the load turbo, and you can see that we wrap this string into a turbo. Now, we get the function and its arguments. So now, we can use the reduce opcode to execute this function. So here, we use the reduce, and it will execute the load reduce function. And you can see that it will get the arguments, get the function, and call it.


## [06:01]


So finally, we can get a command execution. So how does Visconi address this issue? And through the above example, and we can roughly say that the most dangerous part of the pcode deserialization is load global and load reduce. So Visconi and pcode just add some restriction to this dangerous opcode. Looking at the code, you can see that the load global function has some blacklist and whitelist. And also, load reduce is restricted in a similar way, and it has a whitelist. And so in general, Visconi is implemented using a whitelist and a blacklist. So how can we bypass? And a common approach is to analyze the whitelist and check whether there are any potential dangerous functions that could be exploited. And I did the same, but I didn't find anything useful in the end. And we are also limited by fixed ways of thinking. So for example, here in this case, most of us may only see the obvious whitelist and blacklist mechanism and stop here.

But however, in reality, the loading process of Torch.load with Visconi equal true is quite complex. So is it possible that there might be some issues as well in the loading process? So I tried to conduct a complete analysis of the Visconi workflow to ensure nothing is missed. So I started analyzing the entire Torch.load process and stepping into the code and debugging the code line by line. And during one of my analysis, I noticed something very strange in the Torch.g.load. So what is that? And I had no idea with this. So I just searched it on Google and the document just said that Torch.g.load is something to use to load models in the TorchScript format. So now you can understand that why we named this topic. Yes, we will analyze and exploit the TorchScript engine. Hello, everyone. This part will cover the basics of TorchScript and its code implementation. TorchScript is used for deploying models in environments without a Python interpreter, such as CPP and mobile.

It can be compiled from Python code through scripting or tracing, and saved as a PDF file which can be loaded and run in CPP and mobile. The save and load process involves serialization. I will explain how Python code is compiled to TorchScript, the serialization and deserialization process, the structure of PDF files, and finally, how it executes. First, we can use TorchJIT Creator to script one Python function to TorchScript. The steps to compile are, first, press Python source code into a Python AST. Second, convert the Python AST to a JIT AST. Third, transform the JIT AST into an AR graph.


## [09:06]


The AR graph initially contains many unnecessary nodes, which are eliminated through optimization to make it more concise. Let's take a deep look about the compile process. Before we start, I need to show what is the function and model in this context. You can see that one is a function, the other is a class. The scripting process for these two are somewhat different, so we will introduce them separately. Let's look at the function first. The compile process is handled within the script mpl function. There are two main functions, getJITDev and JITScriptCompile. The getJITDev function performs two main tasks. First, it uses the AST library to convert Python code into a Python AST. And second, it calls buildDev to transform that Python AST into a self-implemented JIT AST. JITScriptCompile is a function bound from CPP to Python. With its implementation in the CPP layer, it further converts the JIT AST to AR within the CPP code.

It eventually invokes the toAR function, which finishes the transformation from JIT AST to AR. Additionally, toAR also calls some optimization functions to simplify the AR graph. Now the function compile process is done. A high-level understanding of the process is enough for this sharing. If you are interested in the compile source code details, you can use the code stack I have provided to navigate through the source code. Then let's look at the model. The overall process is similar to that of functions. Expect that there is an additional step at the beginning. Retrieving all functions from the model. It needs to retrieve functions and compile functions one by one. The stub fn function is responsible for handling this. Stub's fn is analyzed for infer methods to compile in this connect. Let's see what the infer method to compile function does. In the infer methods to compile function, it first uses dir to get all attributes and methods of the model.

After filtering, it saves the methods that need to be exported, thereby collecting all functions in the model that requires compilation.


## [12:08]


The make stub fn method converts the collected functions into JLTST using the getJLTDEF function, which is the same one used during function compilation. Back to create a separate model MPL, the method stubs variable stores the JLTST we just converted, and the create methods and properties from stubs fn takes two method stubs as input and is responsible for converting it to AR. We can see that this call stack is similar to the call stack for function compilation. Both eventually finish the conversion into AR function. Now the model compile process is done. Overall, some key steps are the same as for function. Both use getJLTDEF to convert Python to JLTST, then convert it to AR through toAR. The serialization filter can save models and deploy them to other environments. Use touchsave to save model as pdfile. Use touchload to load pdfile as model. The Python code here is converted from AR. You will notice it's slightly different than the original code.

Let's see how separate function and separate model is serialized. Separate function has an additional step compared to separate model. It needs to be converted to separate model first. After that, both call serialize for serialization. During the serialization process, some information is stored in the data.pkl and constants.pkl in big format. Additionally, it saves the Python code converted from AR and debug info to the code folder. To summarize the save process, for touch function, first convert it to touch model. The remaining process is the same. First, the app value corresponding to the model is serialized in PQL format as data.pkl. Second, obtain code and debug info via Python print and write to code directory. Third, save tensor constants to constants.pkl. Now the serialization is done. Let's look at the detail of the PTFEW. The PTFEW is compressed achieve using ZIP compression.


## [15:11]


The field tree after decompression is shown above. And we can use PicoTools to disassemble it. Above is the disassembled output of data.pkl. The meaning of this PicoTools is create an object named touch placeholder model and set its training attribute to true. And the touch placeholder model object is a secret model. So when interpreting data.pkl, secret model object is created. The constants.pkl contains Pico code, which can create a value object with tensor. The touch.py field contains Python code strings. It's the same as the content of the code attribute in secret model because they are obtained in the same way. OK, now we understand the three most important parts saved in the serialized field. Data.pkl, constants.pkl, and the source code in the code directory. Let's see how these parts are restored into a secret model. We can see that in the deserialized function, the three main parts of the serialized field are converted into a secret model.

Let's look at the function implementation. Deserialize will call read achieve to read constants.pkl and data.pkl. Then we'll get one model in CPP, which is a secret model in Python. During the deserialization process, first I'm picking constants.pkl to obtain tensor constants, then save them in constants.table. Second, I'm picking data.pkl during the process of I'm picking data.pkl. Relevant source code is read from the code directory and pressed as needed, and the related constants are also read from the constants.table. The code processing process includes converting Python code to custom tokens, and then transforming tokens into JIT-ST, and finally into AR. This process differs significantly from the earlier Python-to-Tetrascript compilation. To summarize the load process, first, reach main logic via deserialization.


## [18:17]


Second, call read achieve to read constants.pkl, convert constants to n-values by I'm picking and save them to constants.table. Third, call read achieve to read data.pkl, restore corresponding n-values by I'm picking data. Fourth, during data.pkl I'm picking, source importer reads code fields and constants.table to restore AR through pressTab to find namedTab to import namedTab. Now the deserialization is done, let's take a look at how Tetrascript is running. The AR graph contains many nodes, and every node contains three parts, yin, output, and opcode. Opcode is the key to execution. The process of executing Tetrascript is to iterate through the AR and convert it into instructions. Then interpret the instructions. Every instruction has one opcode. There is a switch table here that finds the corresponding process logic through the op and the execute set. Now we have a basic understanding of the Tetrascript workflow.

While reading the code, I discovered something interesting. There is one opcode named op, and it can do a call on operator table. An operator table contains many functions corresponding to operators. I'm curious about what operators they are. Some built-in functions register themselves as operators, requiring op instructions to call corresponding functions. The registerOperator class manages these operators, and registers them by registerOperator function. I page the PyTorch code and add one function to output all operators. You can see there are over 2000 operators here. Are all these operators safe? During our code audit of these operators, we discovered two interesting operators, save and fulfill. Here we can see the corresponding implementations of these two functions.


## [21:17]


The save function will save specified value as a model to specified field. This can lead to arbitrary field writes. But the saved field contains a lot of dirty data, which model information. The fulfill function will read the field which I specified, and convert the field contained to tensor. Then return the tensor, we can get the field contained from the tensor. Now we know these two operators have vulnerabilities. But there is still a problem. How to call them from touch script? To get the answer, we need to first understand the operator registering process. The getBuildingTable function registers some object member functions to building of ops. And during this process, it also adds prefixes to the member functions for rename. Finally, the address of these functions and their modified names will be placed into built-in table. During the touch script compilation process, two shrugged values will be triggered, where it checks whether the Python object exists in the built-in table.

If it exists, it returns a built-in function. Finally, this built-in function will be inserted into the AR graph through emitter built-in call. Back to the built-in registering process, we now understand that add and save may come from one of these objects in models containing built-ins. By testing each object individually for the presence of a save function, we will find that the save function exists in the touch object. Adding from field can also be obtained in the same way. So, we just need to call touchSave() or touchFromField() to get arbitrary field write ability in touch script. Now, we can try writing a field to get RCE, but since there are some dirty characters, only specified special fields will work. Though we want to call and type a field because of a permission mismatch, our script uses touchSave(), which writes a field with 644 permissions, but the current demo requires 6000 permissions.


## [24:27]


Here is one poke video. Okay, you can see we get RCE. The vulnerabilities we have discussed so far might be more similar to logic vulnerabilities. On the other hand, some hackers may prefer memory-related vulnerabilities. We have also discovered a few overflow issues. Here you can see that when we load this modelpt field, it triggers a heap overflow. As a result, we can observe that the heap address and the loaded address of the library are leaked. This is one poke video. Okay, you can see we get RCE by memory bug. But due to time limited, we won't go into them in great detail here. We hope to share them in the future. This is the full details of the CVE. The fix is also relatively simple. It prevents the use of touchscript when voice-only is enabled. So what is the impact of this vulnerability? The entire AI ecosystem, like this house and petology, is one of the fundamental components. If there is an issue with the petology, just like we have removed one of the bottom-level components, the entire AI ecosystem is then compromised.

We did a simple search on GitHub for projects using voice-only code. We can see that in fact, many projects use it in this way. Here we select two well-known projects as an example to illustrate the impact of this vulnerability. One is VOM and the other is Transformers. VOM is a high-performance library for efficient large-language model inference and optimized for speed and memory usage.


## [27:30]


So why did I choose it? Because it has a CVE number and you can see that it uses the torch.load function and the voice-only parameter is the default value of force, so it has an RCE vulnerability. The fix is very simple, just add the voice-only parameter. This seems to solve the problem, but because of the petology issue, mitigation ends up being ineffective. And the sea harbor you once saw was actually also waters. And at the same time, I found a very interesting phenomenon and you can see that when I check this requirements.cpu.txt and you can see that it just hard-coded the torch version. And also in the GPU version, you can see that it also hard-coded the torch version. So what does this mean? It means that even if PyTorch released a new patched version and users of VRM cannot update it timely because VRM has hard-coded the version. So as a result, the users will remain exposed to this vulnerability for a long period.

And so now let's set up the environment. Since the latest VRM has already patched its issue, so we need to install the old version, exactly 0.7.3. And you can see that when you install VRM, then it will install the exact version of torch. And so here we will use this function as an example to illustrate the existence of the vulnerability. And at first, I didn't think too much about that. I just want to take the poke from the previous vulnerability and just hoping for one should succeed. And you can see that finally, we will definitely enter the logic we want and it will load our email model. So we are ready to proceed. But it failed and you can see that we don't create this fail, so why? And let's take a look at the previous poke and you can see that after we use torch.load to load this model and we need to call the model to invoke the model in order to trigger this forward logic. But here you can see after we load this evil model and it didn't invoke the model.

It just fetched the items and then deleted the model. So this model was not invoked. And is it really an end? And at that point, I noticed the error message and it says that the model don't have the attribute items. And this error message gave me some inspiration. And items is essentially a keyword in Python and it is used to iterate the dictionary type object. So you can see that in fact, items is a function. So here can we spoof the function name? And so here I just try for the first time and you can see that I change the function name from forward to item and just run the code. And you can see that why it is still saying that our model don't have the items attribute. But if we write in this way and you can see that we just call the items function in our forward logic and we run a code and you can see that we successfully create a field and this means we succeed.


## [30:43]


And this is very strange and the reason is that when retrieving model functions, it determines whether these functions need to be exported. For functions not decorated as exports and a lazy export strategy is adopted, meaning that they are only exported if called within the exported function. So here you can see that in the left picture, we just decorated our items function with exported. So here it will export the item function and we can get this item attribute. And in the right picture and you can see that since we call it in our exported function forward and our object, we also have this item function. And so finally, we report our ability to the official team and who highly acknowledge our work. And you can see that I said it's very interesting since PyTorch document claims it is safe. And the patch is very simple, just change this version. And next, Transformers. Transformers is a popular open source library by Hugging Face and provided pre-trained models and tools for natural language processing tasks using the transformer-based architecture.

And Transformers also implemented some security hardening measures. For example, here you can see that it just used which only equals true to load the model. And for now, we set our environment and you can see that since the latest version of Transformers also fixed this issue and we need to install our old version 4.51.3. And here, this is the official document example from Transformers and you can see that we need to load the model from Hugging Face and create a pipeline and then we can create our conversation. So, here, for example, we just ask a question. And you can see that just give me some answer. And here, this is my demo project and you can see that we only have three fields, readme, config.json, and a test PyTorch field. And it doesn't contain any evil code. And if we run the code and you can see that it will use Torch.load to load our remote PyTorch model. And let me introduce this code and when we load a remote Hugging Face repo, then it will load this model.

And by default, fields in C tensors or PyTorch is allowed. And just like the previous VRM vulnerability and it will call the keys function. So, we can just change the function from items to keys and we save the project in our local repo. And, of course, if we run this code and you can see that we can exploit and we can create this field. And after that, we call this vulnerability to Hugging Face and you can see that it just patched it.


## [33:46]


And the patch is also very simple. You can see that it can check Torch.load is safe. It checks the Torch version greater or equal than 2.6. And finally, it defends a summary. So, now, if you are still using your own version of PyTorch, please update. And here is also some recommendations. So, for example, from the model format perspective, we should use more secure format like safe tensors. And also from the model community perspective, we should scan and flag malicious models and Hugging Face has already done the relevant work. And also from the user perspective, we should not load untrusted models and it is better to load them in a sandbox. So, any questions? And thanks again for listening. And if you have any questions, please feel free to contact us at any time.
