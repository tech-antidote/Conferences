---
title: "Emulating Embedded Linux Devices at Scale with Light-Touch Firmware Rehosting"
speakers: ["Sigusr Polke"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Emulating Embedded Linux Devices at Scale with Light-Touch Firmware Rehosting - Sigusr Polke.eng.txt"
sha256: "6fdceb4173dd0aba22cbb8881b6695888c31afba66d615c420c0917b1a08359c"
duration_seconds: 2300
words: 6338
text_chars: 33365
redacted_secrets: 0
converted_at: "2026-08-12T06:24:13Z"
---

# Emulating Embedded Linux Devices at Scale with Light-Touch Firmware Rehosting

**Speakers:** Sigusr Polke  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (38 min, 6,338 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Okay, hello. Welcome to this talk, Emulating Embedded Linux Devices at Scale with Light Touch Firmware Rehosting. Today I'm going to give this a kind of higher level approach, like overview of what we've been doing in the advanced research team at CrowdStrike, which is kind of giving us this quite low investment, high return ratio when we've been emulating embedded Linux systems. So by the end, you should be able to kind of take the principles that I'm talking about here and apply them in other emulation stuff. It's a little bit of a patchwork approach and a bit proof of concept, but hopefully a little bit helpful at least. At the moment, most embedded Linux emulation works in user space, so a lot of the emulation approaches take a stock ARM or MIPS KME machine, boot into like a normal distro, unpack the firmware into that guest, CH root into it and then run binaries with like a LD preload to paper over incompatibilities in it.

This approach works pretty well if you're looking at a specific binary that you want to look at on a specific device. There are some cases where it gets pretty tricky, but in a lot of cases, it's totally fine to do. But when you're doing that kind of approach with the LD preload approach, what you're basically trying to do is compensate for missing peripherals and a different kind of system set up that the binaries are not expecting. So NV RAM is the classic example. The user space way to deal with NV RAM is to LD preload something like libNVRAM or some descendant of that with extra functionality. But there's certain cases where we can't really address these kind of incompatibilities just by LD preloading stuff. When the systems are designed to work more holistically with a lot more moving parts. So the traditional kind of way of doing things in user space doesn't really work for those kind of approaches.

So our question was, can we try and do it in a different way? We want to do vulnerability research on these systems. We just want to do things like run honey pots. So we want something like a full system emulation. We want the whole thing to be running as it usually would on the device. But we can't really emulate just like a specific device in full without a lot of time and effort and the payoff might be kind of low for how much time we'd have to put in. So we want to see if there's another approach that we could try and try a few different things. And we came to this.


## [03:02]


This kind of re-hosting concept. The main idea of this is we don't touch user space at all. We don't want to do anything in user space. We don't want to LD preload anything. We don't want to do any hacks during the boot process. We just want to offer up a firmware image to the re-hosting environment. Ideally the exact same firmware image that's bundled in a firmware update image. And for it just to run. User space can only kind of interact with what the kernel gives it. And we, the re-hosting approach means we can make loads of changes in the kernel and do as much as we want in the kernel. But we just kind of create an environment. Create this re-hosting environment for it. So the idea is we would build a kernel for a particular KME machine with a compatible architecture. And then modify the kernel to create an environment which looks as close to a real device as possible from the perspective of user space.

And then we can just tweak the kernel, tweak the KME machine, like add peripherals, add hardware whenever we want. Or whenever we need to. The question is then like where do we start with something like this? All embedded systems are running the Linux kernel. But they're running a Linux kernel built for a particular system on chip or sock. A sock is just a bundle of CPU memory, other peripherals, things like that. Networking, storage. It depends on the sock. Some peripherals might be integrated directly into the sock. Others might have like a physical interface that the hardware devs can wire another chip to, like flash memory or something like that. But each sock is designed by a hardware to cater to a particular market. So a specific sock will get used in lots of different embedded systems, sold by lots of different vendors. So Netgear, Asus, TP Link, they don't buy, they don't build the sock themselves.

They're not chip companies. They buy a sock from a manufacturer and then use them in their devices. And so the same sock is used across multiple devices by multiple different vendors. So the supply chain looks like this. The sock manufacturer makes the socks, so like Broadcom, Qualcomm, Media Tech, all these kinds of companies. Vendors buy the socks from the manufacturer and the manufacturers give the vendors some kind of SDK they can build their own firmware with to run on that sock. The vendor will use that SDK to build the kernel and file system binaries. So it kind of means that any code running close to the hardware is usually manufacturer provided. Like vendors won't really have much to do with that. And the vendors can do whatever they want in user space. They just use their like APIs and libraries or whatever provided by the manufacturer as part of the SDK as well. Well, what does that mean for us?

Well, we're only trying to make changes to the kernel.


## [06:03]


So instead of emulating a specific device by like a specific vendor, we think about like we're trying to emulate the kernel or like create a new kernel that looks a lot like one for a specific sock. We're not trying to emulate the hardware of the sock itself and try and run a prebuilt kernel on it. We did try that. It's a bit tricky. So we want to just build a kernel and sort of tweak a KMU machine if we need to. So that from the perspective of user space, user space thinks it's running on that particular sock kernel combo. The benefit of this in theory is if we can do this, then we should be able to take firmware from any device based on that sock and run it. So we could build one rehosting environment and get a few dozen possible emulated devices out of it. We've got a lot of things we can kind of lean on to try and do this. We don't obviously have data sheets from the manufacturers for these socks.

We don't have kind of proprietary information. We do have KMU machines which are probably good enough for getting started. We have the Linux kernel. We have firmware images for devices that we want to emulate. We have reverse engineering tools. We have GPL code in a lot of cases. We have compiled kernels. We have kernel modules that come with the firmware images. We have things like open WRT, DDWRT, kind of open source distros which might have releases to run, which run on the sock we're trying to target. They kind of do the opposite of what we're trying to do. They're trying to run on the actual hardware and we're trying to not run on the actual hardware. But they're kind of similar. We have kind of similar but opposite intentions. We don't have data sheets. We don't have really that much source code for binary blobs. We don't have documentation. We just don't have unlimited time and energy or unlimited money.

We're just trying to get to a point where the thing runs well enough. So this is the vague plan. We start with running a mainline kernel, similar to a version, a similar version to the one that actually runs on a normal device. And then we compile that to run on the most compatible KMU machine we can find. And then on that machine we load the file system and just try and run stuff. And we see what breaks and we try and figure it out. And then we patch the kernel again and maybe make modifications to the KMU machine. And we just keep doing this in this kind of twisted development life cycle. We can, during all this, we can infer behavior from the user space to kind of figure out what it's actually expecting and what it wants to see and what it's doing. And then we can make changes reflectively in the kernel to cover that. So this was the device we chose to do this. It's an older Asus router.

It's based, it's the AC87U.


## [09:05]


It was released in 2014. It went end of life probably 2021. It's based around the BCM4709, which was your Broadcom sock. And the 4709 is used in quite a few routers. It's a router sock with a Cortex A9 core. It's in the broader BCM4708, 4707, 4708, 4709 family. It has an integrated ethernet stack. It was built for the router market so it gets used in routers and extenders and things like that. The list up here is kind of non-exhaustive. It's not every router that runs with this, that has this sock, but it's a list, it gives a kind of indication of what different vendors, which different vendors were using it and what kind of device it's being used on. So the AC87U was kind of chosen at random. The idea was we were kind of looking at Asus stuff and we wanted to just kind of see how easy it would be to do emulation for them. But the idea is that whatever we build for the 87U would maybe work for other Asus devices running on the same sock and there's a lot of them.

I should also say we didn't actually have the physical device for this at all. We worked only with the firmware image. So there's also a bit of an artificial restriction. But I kind of just wanted to see if it could be done. There's a lot of common wisdom when it comes to embedded systems emulation that you need the physical device to do it, to like dump the NV RAM and get all these things out of it. But our intention longer term is to emulate devices at scale. So we don't necessarily want to buy every single device that we want to emulate. So it's a bit of an experiment in this as well. The first step for this we can just get the firmware. And we get whatever is publicly available. We can get the most recent firmware from the Asus website from 2021. Get the GPL code which has some like Broadcom commented code in there as well. The firmware image is in this TRX format which is like an older Broadcom firmware file format.

We can unpack these like really easily with Unblob. And using Unblob also means we can carve out the specific sections of the firmware. And we don't have to do any post processing stuff like we'd have to do with Binwalk. So you can carve everything out very precisely with Unblob. Once it's unpacked, so we get this Lisma compressed kernel and we get the SquashFS file system. We can, with this unpacked firmware we can kind of get a sense of what the device is actually running. What it's running on. And so what we need to emulate. So we get the kernel version out, a string out of the kernel which is 2.6.36.4 BRCM arm. It's this Linux 2.6 with some Broadcom modifications I guess. It's compiled with GCC 4.3, 4.5.3 from build root 2012.2.


## [12:08]


So we know what it's compiled with as well. We can use the same compiler and just kind of keep things as similar as possible when we're building our own kernel. We can also get the boot args out of the kernel blob. So this can give us an idea of how it's going to boot. They might not be the actual boot args that the real device is using but it's a decent way to start. So the root boot arg points to MTD block 2. So it's booting from this particular part in flash. The init boot arg points to S bin pre init. If we look at the unblob file system we can see that pre init is a link to a binary called RC. There's no init scripts or anything like this. It's just this RC binary being run as pre init. A lot of other binaries linked to RC. It's a pretty big sort of system functionality binary. And it checks how it's invoked. So it will run in this pre init mode when it's run as pre init. Or in another mode if it's run with a different name.

The layout of the unpack file system tells us a lot how the system is expected to run. Kind of directions we'd usually expect to see useful stuff in populated or actually just broken sim links to temp. So temp is probably populated at boot time, probably by the init process, probably by pre init in particular. And so in order to get, when we're doing our emulation we want to get the system into a state using the normal init process they're expecting to perform. So that we can run all the other binaries and things on it and the system will be up kind of in a normal state. So we don't want to go through and kind of paper over these things and do this stuff in user space. We just want to make sure that the init process is capable of kind of completing. So this is, we can apply this same kind of life cycle that we had before to this setup. We know the architecture, kernel version, compiler, the way the file system has been loaded from memory, from flash sorry.

So the first kind of naive plan which is definitely going to fail in the first instance is like build a KMU machine with a cortex A9 core, sorry find one. Build a 2.6.36.4 kernel for it. Attach the squash FS image which we carved out earlier. On some kind of emulated flash device and then hopefully the kernel will load the file system and then we can try and run stuff and see what breaks basically. And then try and fix it and make it better. For this one in particular, so KMU has this V express A9 board which has a cortex A9 core and flash memory. It's no flash not NAND which is not a huge problem but in some cases it can be a little bit of a problem. But just for the start, just for starting this is not a huge blocker. There's a def config for the V express machine in Linux 2.6.36.4 and there's kind of machine code for the V express series like Mac V express code in the kernel as well.


## [15:14]


So we can build the 2.6.36.4 kernel using the V express def config, using GCC 4.5.3 from build root 2012. Getting that old version of GCC compiled with old build root can be a bit of a hassle. So I'm not going to go into it here but it just involves using like old Ubuntu machines and kind of commenting out bits of code that don't work every now and again. But it does get built and it does work. Once the kernel is compiled and running, it kind of works pretty well. The kernel loads into the V express machine. You do need to edit the kernel config a little bit. You have to make sure that you enable things like squash FS and MTD and the kind of stuff that you want to have in your rehosting kernel. You also need to kind of think about things that you might want to compile into the kernel. Because we're not going to be able to load anything later. Like user space is not going to be able to load any kernel modules.

So we just want to, in this kernel we can kind of just build everything that we might potentially need into it. As long as it's not incompatible with it with each other. And it kind of makes life a little bit easier later on. The first thing we have to do is figure out how to present the file system image to the kernel. So we need to forge some kind of flash. The flash needs to be laid out in a super predictable way. But because we control the boot and the kernel, we can pass the MTD parts boot ARG to the kernel. Which will tell it what our flash layout is and where to look for certain things. So we can still use MTD block 2 as the place we're going to keep our file system. And just pass it in the MTD parts boot ARG to tell the kernel that that's where it should boot from. So the flash layout can look something like this. We have a couple of imaginary empty parts, then the squash FS file system.

One of the quirks of VXpress boarding KME is it needs the file that's passed as flash to be exactly 64 megabytes. But yeah, we can just put what we want in the place we want it and just pad the rest of the file out. We compile the kernel, construct a KME command that looks a lot like the one up there and the file system doesn't unpack at all. But we do get a relatively decent error. The one is, it tells us it's failing because it's something to do with the compression of the squash FS file system. So it's figured out it's squash FS but it can't do anything about it because it's figured out it uses some kind of unknown compression. And the main problem is that the squash FS image that ships for the AC87U is compressed with EXED. There's no native EXED compression support for squash FS in the main line kernel until 2.6.38 and we're on 2.6.36.4.


## [18:19]


So Broadcom obviously does something a bit different. And we can figure out what they're doing by looking at some of the GPL code. The GPL code we got from the ASA site for this device has some squash FS related code that includes some EXED support. So it's a slightly modified version, kind of back ported EXED support. And similarly we can just back port EXED support from 2.6.38 into our own build or just use the code from this GPL release. And it works. The file system unpacks. But it's a bit of an indication of the kinds of tweaks we have to make to the kernel as we're going along trying to do this kind of emulation effort. The first thing we do is just pass BNSH as the init command. And we can just drop into a shell and poke around a bit . This is a bit like the situation we'd be in if we were doing the CH root kind of user space approach. But of course nothing actually interesting runs because the system hasn't been initialized.

The file system is mounted but nothing has actually run. We know that preinit is responsible for the initialization. So let's have a little quick look at that. So you can see here a lot of what the preinit code does is create directories, set up symlinks, copy files around into expected places, mounts pseudo file systems, normal init process stuff. So yeah, in theory if we can get preinit running and completing, it will be a big step into being able to emulate the whole system. There's another notable thing about preinit just from some static analysis, which is that it contains routines which only run when it's running with PID 1. So we actually need it to be the init process in order for it to do stuff. Like it's a kind of orchestrating process in the system, in this kind of holistic architecture of the system. So we can't just invoke it from a shell and expect it to work. We can just start the thing again with the init boot up pointing to preinit and see what fails basically.

I mean the big one is we got a whole load of NVRAM errors. This is definitely expected. NVRAM is the most common blocker in embedded Linux emulation. So we need something to work to take it's place. We kind of need to make a plan here. We're not touching user space, we just need to change things in the kernel to create a device, a dev NVRAM basically, which would behave just like the dev NVRAM device would on the AC87U. We can get a decent idea of what, how dev NVRAMs are actually interacted with by looking at the user space binaries in the firmware image itself. So Brawl compromises these APIs to interact with NVRAM.


## [21:23]


In particular this lib NVRAM SO, which is kind of the official Broadcom lib NVRAM. On the 87U, user space binaries will import the lib NVRAM, the Broadcom lib NVRAM and call functions like NVRAM get, NVRAM set, NVRAM commit. We can have a look at this, at this library and see what it's doing under the hood when it's doing these kinds of things and hopefully, yeah, this can inform what we're going to do. So yeah, looking at lib NVRAM, it opens, it has a function called NVRAM in it, which opens dev NVRAM and stores the handle to it at this global memory location. And then NVRAM get will call read on the open file handle with a pointer to a string of the key that wants to be read. And another buffer where the value should be written. NVRAM set calls write on the open file handle with a pointer to the key equals value string. So it's using really basic sys calls that we can handle when we make our own kernel device that does something like this.

So yeah, we build a character device in the kernel. In lib NVRAM, the whole NVRAM buffer is expected to be a contiguous block of key equals value string separated by null bytes. So we can kind of just mirror what that is. We do need somewhere to persist it. So we can back it with just another partition in the flash memory. We make sure that dev NVRAM, the file is created. So user space can call open on it. And then we just handle all the read, write, IOPTL operations as well. And when we're doing that, we just look at what the Broadcom lib NVRAM is expecting to receive and we just return what it wants. We're just kind of servants to the user space in that way. And this is kind of a rough idea of the kind of code we have in the kernel. I'm sorry you probably can't see it here . But lib NVRAM is just doing super basic reads and writes. We also handle calls to like NVRAM commit which is an IOPTL with a specific command number and we just write the buffer back to the flash when we get that.

And then there's calls like NVRAM get all which is just like NVRAM get but passes null to the second read arc. And if we get that, we return all the NVRAM key value pairs. So this is relatively, I have to say, quick to write. We can write it, compile it, try it, squash bugs, compile it. It's just kind of development life cycle stuff. And the approach just kind of works. And we can test it with the command line NVRAM tool that's in the ACES firmware and get all the data out of the sort of fake NVRAM partition. But with this approach we just get a system wide NVRAM device that has persistent backing and works in the way that the user space is expecting. And so it will probably work on other similar era devices that use the same mechanism as well.


## [24:27]


Alongside doing this, we're also trying to emulate the thing. So we have to solve the problem of what goes into the NVRAM. And as I said earlier, the common wisdom is that we often need to be able to dump NVRAM from an actual device in order to do anything with it. This isn't actually the case on the AC87U or any of the other ACES devices. There's a lot of logic which populates NVRAM with default values. And there's certain like NVRAM keys that you can set that will trigger a kind of full NVRAM reset. So there's no need to have an actual physical device for these devices. And we can kind of freestyle based off the defaults that pre-init creates. And yeah, once we have NVRAM, pre-init kind of works a bit better and gets a bit further. But we start hitting other issues that we weren't necessarily expecting. So pre-init needs to be there to be more than one network interface. It's looking for F1 but it can't find it.

We can't just add another interface without tweaking the V-Express machine and the kernel, Mac V-Express a little bit. But we can quite easily do it. We can just map a new ethernet interface into memory in the KMU code and in the Linux code we just add another like IRQ for the, in the Mac V-Express board files which lines up with the IRQ value in the KMU code. It's a relatively light, light touch strategy but, yeah. It's pretty, it's a little bit fiddly but it's not a huge amount to change but it, and it just kind of works. So in our, in our, in our examples here I just kind of copied the V-Express machine, just created a new machine called BCM4709 with modifications. And yeah. So it just means we can add, like pass another net flag to KMU and we just get another, like a second network interface showing up in the guest. We can do this as many times as we want but like two or three is probably the limit for what you need.

So pre-init's happy. It's got two NICs. We get different errors. So we start to see more errors related to the JFFS2 directory. This doesn't seem to exist. There's no JFFS2 partition on the firmware image. We can base it off. So of the, of the firmware image downloaded from the ASUS site. So assumedly there's something that's created or mounted at boot. To figure out what it is, we just have to go back to pre-init and see what's going on. Pre-init tries to mount certain partitions from the flash to the JFFS and ASUS JFFS directories. It checks the proc MTD file to find partitions with names BRCM NAND and ASUS and then it just mounts those partitions as JFFS to those particular directories mentioned earlier. And we can fix this just by modifying our flash again. And just changing the boot ARGs again. We modify the flash layout.


## [27:28]


We make sure we tell the kernel which parts of the flash are called BRCM NAND and ASUS. And that means when we boot the MTD file will contain those names that pre-init's expecting and pre-init will try to load those particular MTD parts as JFFS file system. And this just works. So the JFFS directories are created. The files are getting written there. Things like log files, sys log, certificates. But it makes booting a lot smoother and a lot less stuff fails. We're pretty far into the, into the init process but we get a lot of errors out on the terminal. They're not like functionality blockers but they kind of pollute the terminal a lot . In particular we get a lot of like dev GPIO related errors. So to quieten down the machine we also create like a dummy GPIO interface. A bit like the NVRAM one. A lot like the NVRAM approach we look at user space binaries for GPIO interactions and see what's going on.

The binaries always just call, make an octal call on the open dev GPIO file with just specific operation codes and like a bit map of the pin that they're trying to read or write to. That's being polled or poked basically. With GPIO we need to know what the active low or the active high state is for each. So luckily there's some device true files we can look at to reference that. And then we can use that to determine which pins are expecting what. And we just return a kind of neutral state back on them. So it's a little bit trial and error but it's not a huge overhead. And yeah, once we, once we get rid of the GPIO errors we get a few more kind of network interface issues. We gave the system a couple of NICs but the driver for the NIC that KMU emulates doesn't actually behave how user space is emulating. So the KMU machine is expecting the SMSC911X and the firmware is expecting some Broadcom device of some kind.

So the specific point in pre-init that's actually failing is a function called get file status which is checking the physical state of the interface by trying to read a register somewhere. It does this again by making an IOPTL call to a socket handle but it just fails because the driver code for the SMSC911X doesn't know what to do with that particular code. This isn't really a huge blocker because we don't need the solution to be kind of perfect or complete. We're not trying to implement networking here but we just do a little bit of fraud in the kernel. We modify the SMSC911X driver to intercept that particular operation code and then we just return whatever user space is expecting again. Just to say, yeah it's physically up. We just spam all the bits back like hi for this one in particular. And yeah, at that point we're doing pretty well. So we've got a system with a specific flash layout, fake MV RAM device, fake GPIO, slightly modified network driver and it mainly works.


## [30:29]


The main thing, the final thing we kind of need to do to get it working, to be able to connect into it is to have a kind of LAN interface attachment. And this is a solution we found for this. So on the A7 you pre-init creates this like bridge device which takes the kind of LAN IP address which is 192.168.1.1. F1 is one of the LAN interfaces for that. So we can try to connect to whatever is on BR0 through F1. And the cleanest way that we found to do this was to kind of create a tap on the host with a static IP in the same slash 24 range as the router and just make sure KMU knows how to use it . So once you have that you can connect to the LAN interface of the device just like we would on the device itself. And yeah, demo time. Let me just try and get these demos, these demos . Hold on. Yeah. So this is running the ASUS router here in the emulated one just on the right top right. It takes a little minute to warm up.

So the video is a little bit long. So apologies for that. But you should see there I'm dropped into a shell on the kind of emulated, on the emulated AC87U there. And I've just SSHed into it in the bottom tunnel there. You can see all the processors are running in that. A lot of them. So assumedly the in it process kind of worked. It got the system into a decent enough state to run stuff. We've got a few network interfaces there as well. And we've got a whole bunch of services running and a whole bunch of sockets open. The interesting one there I guess is the web management interface. A lot of the time this is the kind of high value one. So I hope this is the right video and shows us the web management interface loading up. It sometimes takes a little bit to get started. But yeah, here we go. So we've got the web management interface started. We can log into it. This one in particular had already been set up.

The set up process is a bit boring so I didn't kind of, I didn't keep that as a video. But it's already been set up. And the web management interface works almost entirely in this particular one. And yeah, that's it. So yeah, all the interesting services kind of work including the web management stuff. You can configure SSH to run on boot.


## [33:29]


You can change the NVRAM and you can do these things on the flash file itself or you can do it from inside the device itself, inside the emulator device itself. In this particular one there's, in the AC87U there's a strange kind of parallel quantenna sock on there where it's expected to be on there which we didn't emulate. But yeah, now we get a pretty decent like 95 percent functioning like emulation for this device with not a huge amount of layout on it. And we can do it for other devices as well. So other ASUS devices running the BCM4709 work basically immediately. Netgear devices are quite quick to get running. They're expecting another specific device to exist but as long as it exists in most cases it just kind of works. Tender devices work as well but they use a different kind of NVRAM. They handle all their own read writes and their NVRAM is called like CFM and it's just a partition in flash as well which just has like a CRC32 at the beginning.

But other than that it's just, it's essentially just key store, key value store. Some devices are a bit more tricky, I guess partially because of the way the images are distributed. So we might not have everything tied up with a nice bow. Others expect like a really precise flash layout so it's more precision than just building a fake flash blob. And there's always like a little ramp up period in like going from like blank NVRAM to a running system. So there's a little bit of like the process of what breaks and why going on. But in general the baseline principles are pretty much the same when you're doing all of them. For a lot of these, getting devices from one particular vendor up and running might take a day. And once you've got like one vendor kind of covered, it can maybe take an hour or so to get another device in that family up and running. And I'll show you a few more if they work.

Yeah, this is a D-Link device, same configuration, same setup, also just the exact same emulation environment, just a different firmware, just a web management stuff is running in theory so we can have a look at that. And yeah, there we go, there's the D-Link interface there. You can poke around in that and have a lot of fun with that. Another one now, this is the Netgear. Again, with the Netgear one we can just, I think I sped up this one so we didn't have to wait around for the whole thing to warm up. But once it's configured running, there's a few limitations like for the Netgear one, it's got this ping that it's trying to do before it will load the web interface.


## [36:36]


But other than that, most of it works here as well, which is quite gratifying. And then finally there's just one more example, there's a Chinese vendor called Ficom that I have never really heard of before. But we've got one of those devices emulating as well, just because it's based on the same SOC. So we have all of this potential based on this one emulation environment. So yeah, conclusions, it's still early days proof of concept but it works. It's relatively light touch. Compiling support with these dummy peripherals is pretty smooth. It's not totally fool proof and not everything is going to be working the whole time. But just to get things running, it does seem to work. We can tweak KMU machines when we need to, we can make changes when we need to. We can take these same principles, we can move them to other SOCs as well. We've started porting the same principles to other SOCs with some success.

Different SOCs have different profiles, the results may vary. It can take a bit of effort in the first instance to create these environments but the return on investment can be quite high because you've got maybe tens or a hundred or so different devices you can emulate based off them. Thanks for listening. I'll stick around for a chat down here afterwards if anyone wants to have a chat. Feel free to get in touch on my massively neglected Blue Sky account if you've got any questions. Thanks.
