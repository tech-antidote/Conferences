---
title: "Dead Made Alive Again"
speakers: ["Bypassing Intent Destination Checks and Reintroducing LaunchAnyWhere Privilege Escalation-Qidan He"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Dead Made Alive Again - Bypassing Intent Destination Checks and Reintroducing LaunchAnyWhere Privilege Escalation-Qidan He.eng.txt"
sha256: "39e7662a918676394fd4202809b472f9e9cebc1001287caceafc7080b27c3cbc"
duration_seconds: 2786
words: 6740
text_chars: 38960
redacted_secrets: 0
converted_at: "2026-08-12T06:24:13Z"
---

# Dead Made Alive Again

**Speakers:** Bypassing Intent Destination Checks and Reintroducing LaunchAnyWhere Privilege Escalation-Qidan He  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (46 min, 6,740 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Okay, hello everyone. And I think this is the time for my talk. And it's now half past one, and welcome to my talk on the Android security, which I believe maybe is the only topic at this year's DEF CON on Android security. So the topic of my talk is Dem Made Alive Again, Bypassing Intended Destination Checks and Introducing Launch Anywhere Privilege Escalations. So a brief introduction about myself. I'm currently the Center Director and Chief Security Researcher at GD.com. I'm leading the Dawn Security Lab, which our lab is mainly doing anti-fraud, client security, security research, et cetera. And I'm previously a winner of the Pantone and Mobile Pantone competitions, and the 2022 Pony Awards Best Privilege Escalations. And I'm also a speaker at Black Hat, DEF CON, Consulate West, Recon, POC, and Hacking the Box. So today's agenda is as follows. My talk will be categorized into five parts.

The first part is the Android security backgrounds for those who are not currently Android experts. And the second part will be the recap of the launch anywhere stories and mitigations and the vulnerabilities in history and can help you have a better understanding of the battery resolve. And the third part is the core part of this talk. It's the battery resolve vulnerability series of CVs we have found in the newest Android system. And the fourth part is about using the power of ARM and MCP. The final part will be summary and QA. So for a quick introduction of backgrounds, I will talk about some knowledge about the intents in Android. So what is intent? Intents are actually the core of the IPC mechanism in Android. It's actually abstraction for messages sent within and between the applications containing the embedded data. And in Android, applications communicate with each other using largely, you can see a figure that activity A, if it wants to call activity B, it needs to use the start activity API call and passing intent as arguments.

And this API call will be processed by the Android system. And in particular, it's the activity manager service. And the activity manager service handles this call and actually invokes the callback, the uncreated callback of the target activity and passes intent for the activity B to process. So this is a basic flow of the activities communicating with each other using intents. So there are like 5 major parts, 5 parts of complements in Android applications. We usually call it like 5 King Kongs.


## [03:02]


And complements can be studied by intents and receive from- receive input from it. And complements can declare intent filters in the Android manifest about what kind of intents they are expecting. And the exception is the dynamic broadcast receiver and this is out of scope for today's talk. And the expected here means that it's not always enforced. It's only the intent filters criteria is only needed to be met when in the implicit intent resolving. So the lower part is the screenshots of the search trampoline activity in the Android manifest. I'm not sure if you can see it very clearly. You can see that this activity declares an intent filter that's saying that it wants an action that named the search result trampoline and the category with the default category. So intents itself uses bundle to store the key value pairs and the key value pairs can contain like primitive data types like arrays for example like intents put extra defcon 2025 and it can also contain objects implementing the serializable and the possible.

And objects, the serializable and the possible, they can define their custom serialization scheme. And intent itself also implements possible which means that intents can contain intents that contain intents and you can loop whatever long you want and so as bundle. So for the intent resolution, as we have mentioned earlier, intents can be explicit or implicit. The explicit intent target is deduced from the complement set within it. This means that you can call set complement on the intent and ask it to be explicitly directed to a certain complement. However, for implicit intent, it must go through the resolution process if there's no complement specified in this intent. The resolution process is currently defined in the resolve internal function of the resolve intent helper class of the android system. And this resolution process considers intent filters of the complements, the permissions of the complements, the privilege level of the complements and also the preferred options of this activity.

There are lots of details here and we will go back to it later on. The details are very important in the exploration of the bad resolve. And also, of course, security restrictions that a normal application cannot start the protected activity or unexplored activity of other applications. This is the security restrictions of the intent resolution. And in the right part of the slide, we can see an example of the implicit intent resolution. An application calls start activity with an implicit intent with the action android.internaction.callprivileged, which means that it wants to make a phone call.


## [06:07]


And this implicit action, this implicit intent is finally resolved to the privilege call activity of comp.android.phone. And we can see that this activity declares the matching intent filters, the intent actions, and the intent categories. And then the onCreate function of this activity is called. But there is one important point here that in order to call this activity, you need to hold the specific permission of the call from privileged. So in the following slides, we will see how we use the intent privilege escalations to bypass this restriction. So this is the background of the intent security and the second part will be the recap of the historical launch anywhere vulnerabilities. So uh what's the non-launch anywhere? Uh we recall that we have mentioned in previous uh slides that activity can perform sensitive actions like install packages, perform calls, or even execute commands. And these sensitive activities should be protected with permission or is always un-exported.

So it cannot be accessed by a normal application uh uh application hold by attacker. However, I recall that intent can contain nasty intents which will lead to the intent reduction and injection vulnerability. For example, in the lower part of the slide, we can see that uh a victim application complements. It contains uh a secret action or privileged action and it actually commands from the intent income. However, this complement is usually protected or un-exported. So an attacking application cannot directly access this complement. However, if there is a vulner- if there is a vulnerability in a breach application, which we can see that it takes an intent from a possible extra and start act- and cause start activity on this possible. And then the attacker application can use this breach uh breach application to attack the victim application. So this is an example of the intent redirection.

So if this breach application is a privileged uh privileged application like the system UID applications like settings, and uh this is a juicy target for the attacker and uh this kind of vulnerabilities will be called launch hand-aware because the system UID application, the settings applications, they hold the privilege to call arbit- to call arbitrary activities. So uh in 2014, we have uh very typical uh example of the bar of the launch hand-aware in the android.com.android.settings. This is a very old bar, but a very typical example. Very classical. And uh in this uh bar, we can see that uh it's in the account management functionality of android. Uh the account management uh functionality android allows application to create an account.


## [09:10]


And uh when it creates an account, it need to ask the settings application. It sends an intent to the settings application and the settings application calls the account managers at account. And here uh this uh account manager at account will call into a system server and system server will in turn invokes a callback in the at account callback of the calling application. And here we mean the attacker application. The attacker application passes by- passes back a bundle and this bundle is uh you- it is processed in the system server and finally passed back to the settings application. And the settings application just blindly take a possible intent from it and calls that- that activity on it. So this is a very classical example of a non-training weird. Of course, this vulnerability gets fixed very quickly and uh this is the first round of fix. And we will go back to it uh later on. This is a very important fix and with many details.

Uh this fix can be actually categorized or summarized as that uh there is a check added in the account manager service. And the account manager service uh checks that if the bundle contains an intent and if there is an intent in the bundle, it will check if the intent is allowed to start. And if the intent check passed, it will then pass it- pass the intent to the settings application- pass the bundle to the settings application. So uh currently all the checks are in the checking intent function of the uh account manager of the account manager service. And we can see that the check is mainly as follows. It's uh perform a resolve activity on this incoming intent and it checks if the target activity have this- have the same signature as the calling application. Uh which means the attacker application. So this check restricts that the attacker application can only use this functionality to start an activity of itself.

And uh it is- it is disallowed to start other activities, other privileged activities of arbitrary application. So this is round one, right? Round one is not finished. We still have round two. And uh what if the account manager service, the system server, sees a different intent as settings? So maybe they see the different intents and the check will of course fail. Recall that a bundle can contain the possible objects and it's the developer's responsibility to write the park and unpark code. So when developers create their own code, the things will really go wrong here. Uh here is an example, the CAE certificate info possible of the Samsung class. We can see that the developers implements a read from possible which is the deserialization function. And a write to possible which is a serialization function. And there's a tiny difference here that's uh on the possession of the third length, the integer.

We can see that in the read from possible, it checks that if the third length is larger than zero.


## [12:12]


However, in the write to possible, it checks if the third length is- is equal to zero but not if the third length is larger than zero. This will cause a mismatch occurs when the possible is written and read. So if you have a third length like minus uh like minus one, the mismatch will occur when the possible is written as a read. So this will lead to the bundle contain different objects after the deserialization, the serialization, and then deserialize again. Which will bypass the check intent check because actually now the system server that can measure uh service sees different intent as a settings. And uh this uh the detail is quite complex here and uh because this is not the core part of today's talk, we will not elaborate here. And uh for anyone who is interested in the next slide, give an overview. And uh this kind of possible mismatch box is largely exploited in the wild. So this uh figure is taken from the Google Android security team uh Hawker's Black Hat EU uh 2022 slide.

And uh we can see that uh uh the bundle is uh deserialized and serialized in the system server and uh because a mismatch occurs, the system server and the settings see different intent. Which will lead to the launch anywhere vulnerability again. So this type of vulnerability uh is largely exploited in like billions of devices in previous years. And there are like more than 100 possible mismatch vulnerabilities in Android. And uh literally Google wrote out many mitigations and uh currently we think the possible mismatch vulnerability is not as effective as past. So now, we'll go to the third part of the talk. We'll go to the bad result vulnerability which is the uh CVEs we discovered. So uh recall that in the possible mismatch scenarios that uh we want to make the account manager service and the settings sees different intent to bypass the check. However, what if the account manager service, the system server, and the settings see same intent but the intent is actually resolved to different target activities.

So we see the check from a different angle and uh we here we try to make the launch anywhere great again. So looking back in the fix, uh as I have mentioned earlier that this fix uh primarily checks the target of the intent resolution of the implicit intent and check the signature of the application. So uh we can see that it performs check here and literally the intent is used in the settings. So there are some ideas that flow on my mind that can we do a talk tool here? The core idea is that uh uh the system server checks the target user result activity and the same to the settings if the check parsed and then the settings will start its intent.


## [15:19]


And the start activity itself actually implicitly calls the resolve activity internally. So what if the draw solution result change between these resolves? Like is it possible for a risk condition? And we need to inspect the source to figure out that if there is something we can manipulate, we can risk with. So the lower part of this slide is uh illustration of the check and start uh check and start code of the between the activity manager service and the settings. We can see that uh uh if it possible so that we can make the when the check intent call the result activity, it will resolve to a benign activity which belongs to the attacker's application. However, in the second start activity, it will resolve to a privileged activity or protected protected activity that attacker wants to start. So can we like achieve this result? So if you want to like uh uh exploit or find or talk to a vulnerability, you need to find your primitives.

What what things you can change or what can you control? The primitive here is that application is allowed to use the package manager set complement enable settings to enable or disable its own complement like activity, et cetera. It's in run time. And the disabled complement is considered to be vanished as if it had never existed. So can we utilize this primitive to manipulate the resolution results? For example, can we disable the result complement in the check run so that we can force the latter result point to a different result? So we can see the other time window here between the activity manager service and the settings. Like can we like disable the activity of our application so that in the first resolution, the result is our activity. However, when the second resolution, our activity is disabled so it cannot appear in the result list. And now the privileged activity or protected activity appears in the result list.

So this is the basic idea of our TOCTO. However, we need to check if our TOCTO exploitation idea, is it feasible? As we have mentioned earlier, that intent can result to a single target or multiple targets because activities can declare similar intent filters and an implicit intent may result to a list of activities. So the first case is that when intent resolved to a single target is seen, whether it's an explicit intent or it's an implicit intent, when the intent resolved to a single target, if the target is disabled, the process fails and the exception is thrown.


## [18:22]


And of course, the explicit intent always resolved to a single target. Explicit means that a complement or the class field of this intent is set. However, the implicit intent may resolve to a single target if there is only one candidate in the intent resolution. So the lower figure illustrates that if the intent resolved to a single target and the target disappears, the target may be disabled, then exception will be thrown and the start activity will fail in the settings application. So after we dive deep into the resolution process and we draw out a figure of the functions called and we can see that the procedure is as follows. First, when the system server, the activity manager service start resolving on the intent, it will first check if the candidates is a single result. If the candidate is a single result, it will just return the single result. If there are multiple candidates, here comes a problem.

If there are multiple candidates allowing the resolution of this implicit intent, it needs to find a single target or let the user choice. So it will first exclude the disabled ones. And then after this exclusion, if there is still multiple targets, it will decide that if the targets have different priorities. We know that developers can specify priorities in the intent filters and the resolution process will check is there an activity intent filter that contains the largest priority. There is a cork here that normal applications can only declare priority zero or lower than zero. It's a privilege of the platform application and system application to declare priority larger than zero. So if there is a candidate that holds a largest priority, this candidate will be returned. So if there are all candidates do not declare priority of the intent filters, it will try to find if there is a preferred target.

What is a preferred attribute? The preferred attribute is we can see in the lower left, lower part of this slide, there is a pop-up here. For example, when an implicit intent resolves to different targets, different candidates, a pop-up of the chooser application will be given to the user and let the user decide which activity he or she wants to start. So if a user starts a candidate here and clicks on always, this activity will be marked as preferred. And this will be memorized and will survive the installation or will survive the update of this application and will be used in the next resolution process.


## [21:34]


So if there is a preferred one, this preferred one will be returned. And if there is still no preferred one and we still have multiple candidates available, then the chooser activity, the pop-up will appear. So we have introduced the flow of this resolution. So the following steps is summarized. Now we can see that first a disabled target won't be taken into consideration. And then the largest priority is chosen and returned. And if there is no largest priority, a preferred one will be chosen and returned. If neither is satisfied, a chooser dialogue is a pop-up for the user's confirmation. And however, from an attacker's perspective, which steps here we can affect or we can manipulate? The answer is that we cannot manipulate with the priorities because I have said earlier that normal applications cannot declare priorities larger than zero. And we cannot make this resolution point to a chooser application because the chooser application is a system platform application and it has different signatures than our attacker's application.

So if you like the check intent finds that our intent is finally resolved to a chooser activity, it's not happy with it. Then we will like bail out and the check fails. So the only choice left here is that we need to make our activity a preferred one. So the whole idea is that it's like follows in the beginning of this resolution of this exploitation flow in the when the account manager service calls check intent and here we make our we supply an implicit intent and here the implicit intent will return multiple targets. However, we have already marked our activity as a preferred one. So here the check intent will happily find that this result is an activity that belongs to our application. So this check is passed. And then we disable our compliment during the time window of the account manager service and the settings application. So when the settings application accepts this bundle and retrieves intent from it, when it calls the start activity, it will find that there is only one candidate left because our candidate activity has already been disabled.

It has vanished. So only one result is left and it will happily start the protected or privileged activity, the target activity the attacker wants to exploit. So here is the basic idea of our TOCTO attack flow. However, we know that there is another important factor in TOCTO exploiting TOCTO vulnerabilities. You need to check the time frame.


## [24:36]


Do you have enough time to manipulate the state of the compliment? Here we use a profetto. Profetto is a perf application that comes within the AOSP. It's a very useful application that it can that has inspect that the functionalities in the system server in the settings, how long do they take? For example, how long does the resolve intent take? How long does the start activity take? And this is crucial for our to deciding the time window of our TOCTO exploitation. And here using profetto, we see that there is actually two check intent calls in the system server. And after these two checks being passed and the start activity is called in the setting application and the start activity, we can see that it in itself calls the resolve activity internally. So it seems that we can just risk within this time window and we are good to go now. So actually, things are not as easy as we have thought here.

And using profetto, we found that our time window is only one millisecond, which means that you can suddenly do anything in a one millisecond time window. And except if you are Superman or Flashman, you can like manipulate this compliment in one millisecond. However, so and we can see that the resolve intent here only takes like 0.5 milliseconds. So given this time frame, we cannot do anything meaningful. We need to find a way to like extend this time frame to extend to like at least tens or hundreds of milliseconds. So here that we have several ideas that we need to get a larger time window, but which direction should we take? Like, can we slow down the resolve intent and disable the compliment in this time frame? However, there is a question here that if we change the compliment states and the change of the compliment state is instantly reflected in the resolution result, we will still fail here.

And can we like slow down the IPC between the system server and the settings application and disable the compliment in this time frame? So we have two ways here and finally we choose the formal way. To make the formal way work, we need to dive deeper into the resolve internals in the activity manager service. So actually, the compliment information query is done or fulfilled by the compliment resolver in the package manager service. It maintains a mapping of the four king cons, the activities service, broadcast reservers, content providers, their mappings from these compliments to the internal filters they declared. And this information is stored in the fields, in the activity internal resolver fields, the provider internal resolver fields, the receiver internal resolver fields, and the service internal resolver fields.


## [27:44]


And the actual resolution process is done in the computer engine. The computer engine is the M compliment resolver, the field of the computer engine is a snapshot of the compliment resolver. And this snapshot is fetched and updated when the package manager internal-based resolve intent is called. And then the queries are performed on this snapshot of this compliment resolver. The snapshot actually creates a copy of these big four mappings. So here in the right part of the slide, we can see this is a summary of this whole flow. The resolve internal helper, resolve internal internal calls the computer engine, query the component internals, and then call the component resolver query activity, which performs matching on a snapshot of these compliment mappings. And then it will call the choose best activity as we have explained the steps of choosing a candidate from a candidate list. So what is a snapshot in package manager service?

A snapshot creates a read-only current state copy of the package infos. And the queries are done in the frozen compliment mappings and states. So once the snapshot is taken, any changes during the time frame of this resolve intent will not be taken into consideration. So we can see in the right part of the slide, the implementation of the resolve intent, the snapshot is created in the first argument of this resolve internal function call. So thank you snapshot here that we know the snapshot, once it's created, it will not be modified and we can manipulate everything we want in the time frame of this function call. And the result will not be affected by our operations because all queries are done in a frozen snapshot. So here, we have our idea of extending the time window of this resolve intent call, and we need to find some way to extend the time spent on searching the activities of this snapshot.

Our solution here is to play with the Android manifest. So can we like declare lots of compliments, lots of attributes, lots of intent filters, which are lots of elements which will fill up the snapshot and increase like obstacles when it's performing a linear search on these compliments. And also, there's a very helpful flag here. The flag debug log resolution, and when it can be parsed, it is a flag of the intent, and if the flag of the intent is enabled, we see that a logging functionality is turned on when the intent is used in the activity manager service, package manager service,


## [30:48]


and it will do like a linear ON printing of all the categories of all the intent filters. So we can see, we can know that if we turn this flag on in the incoming intent, it will make this resolution process slow down much more altitude because it will print all the intent filters, all the elements, and this definitely will take up much time. So here we try to declare the intent filters with the large number of categories, and how many categories can we declare? What's the largest number we can declare? The upper bound here is limited by the timeout restriction of the package verification functionality in the package manager, and also it's limited by the format, the XML format of the Android manifest. So after some trial and error, and I found that also based on the code of these parsers, we found that the 10,000 or 40,000 categories in the intent filter is small enough to make both happy, to pass all the checks, and it can also slow down the resolve intent to like 50 or more than 400 milliseconds, and this is ideal for risk condition vulnerability.

So in the lower part, the left part of the slide, we can see here's an example of the malformed manifest, the malformed intent filter, we declared with like 30 categories. We declared 30,000 different categories here in a single intent filter, and now the profiler shows us that the resolution process has been slowed down to like 400 milliseconds. This is a very ideal situation for us. So the final attack flow, the final outcome here in our profiler view is as follows. I can see attacker application triggers the add account functionality of the account manager service, and initialize this launch anywhere attack, and this now system server receives the intent, and it performs the first check on it. And then if the first check passed, it goes in the second check. However, there is a background thread started in the attacker application that in the second check, the time window of the second check, the thread disables the resolution target of the complement declared in the attacker application.

So the second check also passed because the snapshot is taken in the beginning of this function. At the beginning of this function, we haven't disabled this complement, but we disabled this complement in the middle of this function.


## [33:49]


So after the first check and the second check is passed, this bundle and the intent is finally passed again to the settings application, which is the user of this bundle and intent. And here, the settings application will happily start the intent and call the launch anywhere vulnerability again. There is a small quirk here that system UI sometimes will crash on clicking the resolve activities dialogue due to the too large body of the intent filters. Remember that if we want to make our activity a preferred one, we need to trick the users to click on the always button, always label of the preferred activity of the chooser dialogue. However, if you declare a too large body of the intent filter, the system UI will crash here. And reinstalling the application can bypass this restriction because installing an update to an application will not clear the preferred activity's preferences. So the idea here is that we can install a normal version of the application and trigger the resolve and make the attacker's activity a preferred activity.

And then we install an update to the exploit application with a larger number of categories and then trigger this vulnerability. This will give us an even larger time frame. However, this is not mandatory because we can declare like 10,000 of categories in a single intent filter and this will not crash the chooser dialogue. And this can still give us like 100 milliseconds of racing time. So this is just an exploitation technique here and you can take it or you may not need it. And this vulnerability here, the bad resolve vulnerability, has some limitations here because it is an implicit intent resolution. So it requires a target complement to have an intent filter with data element and it requires a target complement. The target complement we want to start the privileged or protected activity we want to start to have an intent filter with the category default declared . So we need to have a target to extend this ability, to expand the ability of this vulnerability , of this bad resolve series of vulnerabilities.

And the idea is that here we first use this vulnerability to call the search trampoline activity with the controlled intent parameter. And in the onCreate function of the search trampoline activity, we can see that it has a very useful gadget here. It retrieves a string from this incoming intent and call the path UI on it, which creates an intent again from the incoming string. So here using this gadget, we can extend the functionality, extend the ability of the bad resolve vulnerability and the cause arbitrary activity at will.


## [36:56]


So there is another interesting detail here that we can see. Normally you cannot directly call the search trampoline activity because there is a permission check here. It will check the colors package. However, using bad resolve, the color package will be setting so you can easily bypass the check and use this gadget inside the search trampoline activity. The fix for this vulnerability in the activator service is quite simple. It just makes the implicit intent an explicit one using the set complement or set package. So this kills the tactile window, the whole tactile scenario of our attack. So digging this further and why I say this is a series of vulnerability because any code snippet that calls the start activity on intent after checking the resolution result can and will be vulnerable. And this previously discussed issue is already fixed and I have found like four other code snippets in the Android open source project that are found to have similar vulnerabilities and reported to Google.

And there are two CVs assigned already. So for the aspiration technique, the key parameter here is a timeout before the disabling complement. So how much time will be spent in the resolve intent when we supply a malformed manifest, a malformed intent filter? It varies on high-end and low-end devices. It is understandable because on high-end devices, it will have higher CPU frequency and speed so it will take less time. It will give us less time for racing. And for example, on Pixel 7 Pro or Pixel 8 Pro, it is like 200 to 300 milliseconds. However, on an old phone like Galaxy S21, this can be extended to like one second. Anyway, we can continuously retry until we succeed. So here is the first demo that we use this vulnerability to call the log password, modify the protected activity or the settings. So it's quite straightforward. You just use the vulnerability to start the arbitrary protected activity to modify ping in the settings application.

So here is another demo in the Android 16 Beta 3 and this is the newest version at the time of writing of this presentation. And I wrote a script to ease the exploitation of this vulnerability. And this is another similar vulnerability in the restriction fragment. And this exploitation calls the performance of phone call without any privilege. And here there is an interesting story here that previously we used our exploit to call 911. And after several submissions, we were told by the Android security team that please stop calling 911.


## [39:58]


Just call other phone numbers. But do not call 911 in your exploit. So we can go even further that there is still a problem with the search trampoline activity because the path UI does not support possible nested possible inside. And some windows, for example, like the owner devices, they removed this gadget in their application. So we found another gadget, the chooser activity, come to rescue. And the chooser activity draws a possible from intent and starts it. The chooser activity takes an actual intent from the input and launches using the start activity as caller. And the Android version of the chooser activity has a priority of 500, which means that it's not applicable for our bad result targets. However, Windows, they tend to implement their own version of choosers, like Xiaomi choosers, or like owner choosers, these kind of things. And this custom choosers mimics official chooser activity, but they do not have priorities specified.

So they can be the target of the bad result. So does this custom choosers verifies targets? The answer is yes, but not enough. The right part of the slide is a summary of the different gadgets in different windows. We can see that in Android, we have the trampoline gadget, but we do not have the custom chooser gadget. However, on like a Xiaomi devices, we both have the trampoline gadget and the custom chooser gadget. So for example, on the Xiaomi devices, we can change the MIUI chooser activity here with the bad result vulnerability. And here we use bad result to start the MIUI chooser activity. The MIUI chooser activity verifies the caller has a permission to start the target intent it passed in, the target complement that the target intent resolved to. However, here that when we use bad result, the caller is a setting. So this check is passed and the MIUI chooser activity will happily start the actual intent passed in and starts the victim activity without any limitations.

However, there are still like different chooser implementations. Some Windows chooser implementations verify the caller and also verify the target needs to be exported. So we need an additional hope here. For example, on Honor and the Huawei devices, we change another chooser activity. The official chooser of the Android of the AOSP here. And so the attack flow will be like attack application use bad result to start the HW chooser activity and the HW chooser activity verifies the caller has permission to start target and verifies target is exported because here the target is a chooser activity. These conditions are met.


## [43:00]


So it proceeds to a chooser activity and chooser activity verifies again that the caller has permission to start target. However, here the target is settings and chooser activity. So this check is also passed. So then the chooser activity calls that activity as caller and finally the victim privileged or protected activity is started. So the final part, I'm running a little bit out of time, so I'll go quickly here. How we can use LRM to find this kind of bugs? It's an interesting idea that maybe LRM is a better fit for finding bugs that bad result because as I have stated earlier that bad result is not a typical like data flow vulnerabilities that you can use static analysis to find. Maybe for this kind of flexible vulnerability, LRM has a better ability or idea to find them. And we design our system as illustrated in the figure. We have a manager agent which uses MCPs to fetch code from the Android USB codebase or the vendor codebase.

In the vendor closed source codebase, we use the JEP. And I have a JEP here with a GitHub link and if you're interested you can use it. And the manager agent retrieves a possible vulnerable code and passes it to the auditor agent. The auditor agent has a prompt to check if the code is vulnerable to bad result. And the auditor agent, the result, the outcome of the auditor agent will be passed to the reviewer agent. The reviewer agent will double check if there is a false positive here or false negative here and then outcome the result. So here the LRM of course cannot directly consume all the USB codebase, so we need to search for APIs, the possible APIs that may appear in the possible vulnerable code, the result activity series, query intended series, and feed the functions and encoding classes into LRM. And we use different MCPs here. We have a quick and dirty prompt that you are Android expert, you need to input output in JSON, and you have a few short example that what is a bad result vulnerability, it's a total vulnerability.

And to avoid false positive, do not consider it vulnerable if the intent has become a specific set and complement, set target, and the intent has been set an action or the intent is not used to start activity or start activity for result. So a quick observation here is that the LRM has false negatives but have a few false positives as there are hallucinations here on whether the intent is used to start activity and sometimes lack of knowledge of the tricky details of the Android security and sometimes lazy on querying call graph. So there is still additional work needed. So conclusion here is that we introduced the bad result vulnerabilities and how we support it and how we use the LRM to extend and search these kind of vulnerabilities.


## [46:11]


Yeah, so this is the reference credits and you can reach us at myxaccount and also our lab's xaccount. So I'm not sure if we have time for QA and here is all the slides. Thank you.
