---
title: "No VPN Needed- Cryptographic Attacks Against the OPC UA Protocol"
speakers: ["Tom Tervoort"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - No VPN Needed- Cryptographic Attacks Against the OPC UA Protocol - Tom Tervoort.eng.txt"
sha256: "04e7a530facc182f48ccc074f05d2e473557d8ca9e49fa5aec211843dc33fe01"
duration_seconds: 2171
words: 6525
text_chars: 36852
redacted_secrets: 0
converted_at: "2026-08-12T02:50:00Z"
---

# No VPN Needed- Cryptographic Attacks Against the OPC UA Protocol

**Speakers:** Tom Tervoort  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (36 min, 6,525 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Welcome to my talk, No VPN Needed, Cryptographic Attacks Against the OPC UA Protocol. My name is Tom Tervoords, I work as a pen tester, general security consultant for Bureau Veritas Cyber Security, and I'm personally based in the Netherlands. When it comes to research projects, my specialization is kind of lies at the edge between offensive crypto research and pen testing, basically trying to achieve useful, well, pen testing results, pwning things, through practically exploitable crypto bugs. I got a lot of publicity after I happened to find a flaw in an obscure protocol used for Active Directory, which led to the zero logon vulnerability, and now I get to spend a lot of time on research projects such as this. So, during this talk, I'll first give a little bit of background on what OPC UA actually is, how the cryptography in this protocol works, and then I'll outline two vulnerabilities that I found, followed up with some conclusions.

So, OPC UA. It's a protocol used for industrial automation. Think of things like PLCs, SCADA systems, basically computers that monitor or manage or change things in the real world, on real machines. Think of pipelines, solar parks, windmills, gas turbines, and so on. There's of course many protocols in this space. A lot of them are pretty old, based on old serial protocols, or they're proprietary, but OPC UA is a relatively new protocol, about 15 years old, and unlike a lot of others, it is standardized in an open standard, it has a lot of open source reference implementations, and it's interoperable through different vendors. But the aspect about it that I personally find most interesting is that it has security features. And it has features for authentication, it has features for transport encryption, and because of this, a lot of users of OPC UA applications and vendors, they point out that, hey, now that we've got these nice security features, we can actually use this OT protocol across our trust boundaries.

Maybe even put a server on the internet, so we can more easily remotely manage things that are in the field. And because we already have cryptography, we already have security features, we don't need to tunnel it over a complicated VPN anymore. We can rely on these convenient security features that are already present. So that, of course, makes it an interesting research target for me, especially because they're not relying on an existing standard protocol like DLS, but they implemented their own cryptographic protocol, so I decided to take a look at how these security features work,


## [03:07]


and are there maybe any issues with it. So in general, OPC UA, it has kind of two different layers in the protocol. It has a communication layer or a secure channel layer, and that part is responsible for, well, doing the key exchange, to arrive at a symmetric key to encrypt subsequent messages with, and to authenticate the client and server device, to do basically the machine authentication parts. Then once you've established this secure channel, you've got keys, you can start encrypting your messages, then before you really do anything, you need to also do a handshake on the so-called session layer or the application layer, and that is mostly responsible for user authentication. And you can have different, you can start a new session on the same channel, and every channel needs to have a session. It's also optional which authentication features you actually want to use, or which encryption features you want to use.

You can go for no encryption at all, you can go for only integrity protection, or also confidentiality protection, you can go for the machine authentication or user authentication, or both, or neither. And for the machine authentication parts, certificates are being used, and, well, the protocol allows for different types of trust models, but what most implementers do in practice is that a device connects to a server for the first time, their certificate gets rejected, then in a list the certificate will pop up as a rejected certificate, and the administrator can then choose to accept this certificate from then on. So it's kind of a bit of a pretty pragmatic trust-on-first-use model that makes it easy to configure or to set up these connections without too much pre-configuration, although you can also just preload certificates on all devices that are talking with each other. And all these different options, do we want to encrypt or not, do we want to authenticate or not, they're all being negotiated during these initial handshakes.

So the first of these two security handshakes, the secure channel handshake, it consists of two messages, an open secure channel request and an open secure channel response. Both sides will generate a random number called a nonce, that will be different for every session, and then the clients, it will already know the certificates and configurations of the server in advance, because these are pre-configured or they are discovered through a mechanism that's kind of out of the scope of this talk. So the clients, they will use the certificates, take the public key from it and encrypt the message from the server and they will also sign it with their own private key and they will attach their own certificates that the server can then validate. If the server looks at the certificate, thinks it's fine, it will decrypt the message, verify the signature and then send its own response, also encrypted and signed.

And then both sides, they have exchanged this random nonce and they plug it into a key derivation function and they will be able to get a symmetric session key that they will use for all the follow-up communication.


## [06:12]


Now part of this handshake is that the client will pick a so-called security policy that it knows that the server supports. And the security policy is basically, it's like a cipher suite in TLS. It decides what kind of crypto you're using. There's a variety of options. Most that are currently implemented in practice are either we don't want to use any cryptography at all or we use some variation of RSA and AES that are actually really pretty similar and mostly interchangeable. So once you've done this handshake, the second handshake will happen and all the messages in this handshake, they're all encrypted with a negotiated session key. And here once again, both sides generate a random number. The standard also calls this a nonce. I call this a challenge to make it easier to distinguish it. And they will put this challenge in the encrypted message. The client will send a challenge to the server.

The server will sign this challenge with its private key, which is the exact same private key that was used in the previous step. And send the client its own challenge. The challenge will sign that challenge, send it back. This can be combined with user authentication and how the user authentication is used. That depends a little bit on what authentication method you pick, like passwords or tokens or something else. I'm actually not going to go too much into user authentication because that's not where the vulnerabilities were that I found. So yeah, you basically just got a pretty straightforward challenge response protocol. I give you a random number, you sign it to prove that you own a certain private key and to prove that you are who you say you are. Something that's, well, if your experience with cryptographic protocol design that you may notice is that it seems a little bit redundant, all these signatures, because in the previous step, we already were doing signing and encrypting, now we're signing again.

Your, uh, both sides during the protocol are doing three different RSA private key operations, which are pretty expensive. While if you want to have an authenticated key exchange, you can design a protocol that really only does one RSA decryption. Uh, so, it's not really a great design in my opinion. Uh, on the other hand, you could argue, well, better to do three times as many, uh, uh, operations than, uh, none at all. Uh, maybe this will add some defense in depth or something, uh, uh, who knows. So, let's take a look at, uh, how, uh, well this protocol actually stacks up. So, what I, uh, noticed, uh, about these, uh, the signing of these challenges in this, uh, session handshake, uh, was that the type of message that's being signed, it's like the challenge of the other, uh, of the other party concatenated with the certificate of the other party. And regardless of whether it's the signature going from the client to the server or from the server to the client, the stuff that's being signed is formatted in exactly the same format.

There's not like some meta data included that says this is a message intended from the client to the server or the other way around. They really look exactly the same.


## [09:14]


And that is something, when you see something like this, this may invite an opportunity for a so-called signing Oracle attack, where you pick one, uh, one message signed in one particular context, but actually plug it in in a different context. And then I started working out on paper like a theoretical attack, um, that I would call a signing Oracle relay attack, where let's say you have two different OPC servers, and server A and server B, and me as an attacker, I go to server A and I say, hello, I am actually server B, and you present a certificate of server B. Um, I would like to log in. Well, then the server A will say, okay , please prove that you're server B, here's my challenge, please sign it. Uh, then you go to start a session with server B, and you say, hello, I am server A, um, I have a challenge here, I want to log into you, please sign my challenge. So server B will sign the challenge , uh, as part of the first step of the protocol.

So you've got this nice signature now , that is actually a signature over the challenge that's originally server A generated . So then you cut off the connection with server B, don't need it anymore, and then you actually plug in this signed challenge that you got from server B in the exchange with server A. And that should allow you to finish the whole, uh, authentication protocol, and now you've effectively bypassed authentication, you've tricked these two servers in logging into each other, but actually giving you access. Or even better, because it turns out in practice, many OPC implementations are set up in a way that servers by default trust their own certificates, so you can actually just log into server A while claiming to be server A, you're logging into the server as itself. And you just set up a second connection where you make the server sign its own challenge, drop that second connection again, resume the first connection, and then you've got an authentication bypass where you're logged into server A as server A, which usually has, uh, maximal privileges .

Of course, this whole theoretical attack only really works on the second phase that I showed, this, uh, this session handshake. Before you can even make any encrypted messages in this session handshake, you first need to do this, uh, this whole secure channel, uh, handshake that also involves the private keys. So if our attack would make any sense in practice, we first need to somehow bypass this first step. And it turns out there's actually a very easy way to bypass this first step by looking at OPC UA protocol varieties. Uh, in most use cases, uh, OPC UA is used as a binary protocol directly layered over TCP, but there is a variance that's, uh, based on HTTPS. There, um, uh, basically all your OPC messages are wrapped in HTTP, uh, challenges and responses and you set up a standard, uh, server authenticated HTTPS, uh, connection. And because then the TLS layer will already offer a transport crypto, uh, the protocol designer says, okay, now we can skip this whole, uh, secure channel step.

We can directly move into the session step because we already have encryption. We don't need to establish it ourselves anymore.


## [12:17]


Um, and then we go right to the vulnerable session handshake. And what's interesting about this is that when you do server authenticated HTTPS, well, it authenticates the server, but it doesn't authenticate the clients while their own custom handshake implicitly does authenticate the client. So they actually have kind of different properties and we can take advantage of that by, well, just simply skipping this, starting on HTTPS. I don't need to prove my identity to set up an HTTPS connection. And then I, uh, try to carry out this attack or this attack. So I wrote a tool that does exactly that and it turns out it totally works. Uh, you just point it at an HTTPS based OPC UA server that uses this client authentication feature. It's not going to bypass the usual authentication. So if the server, uh, is configured to always require a password, that's not going to work. But in most cases where you have like really machine to machine authentication, so you're really talking about automated device, then it's, yeah, makes sense to not use user authentication, but use this client server authentication.

And if you support HTTPS, well, I just completely bypassed it. Uh, only downside though, from the attacker's perspective, in practice, almost nobody actually really uses this HTTPS variant of the protocol. It's a relatively new feature. It's, uh, supported in a few implementations, but it turns out there's not really much need for it. People don't really care about it. There are a bunch of implementations that open up an HTTPS port by default. So if you don't change the default configuration, it will expose this. And even if you don't use it, you may still be, uh, completely vulnerable, assuming that you don't block off this port in your firewall. So it definitely is a potential risk, especially when you use an implementation that turns this on or when you actually do rely on HTTPS. But it probably does not affect the majority of OPC UA, uh, deployments. So I wanted to look a little further and see, is there a way where I can actually attack the standard TCP version of the protocol?

And then I need to get past this first secure channel step. Now something that's, um, I noticed is in the list of supported, uh, ciphers of supported security policies. There is one that has a, uh, well, a familiar, uh, uh, name for me. Uh, it uses, uh, RSA for encryption using the PKCS1 panning scheme, which is a, uh, a way of using RSA that has pretty much been definitively broken in 1998 by Daniel Blankenbacher in this, uh, really nice paper. Uh, but a lot of protocol designers since didn't appear to have gotten a memo about this and kept putting PKCS, uh, one in their protocols. Just as the case for OPC UA, which I think the first version was from like 2006, but they still supported this basically broken, uh, encryption mechanism. However, they eventually did deprecate it. So there's one, uh, security policy that supports this broken mechanism, but, uh, implementers, they're told, hey, please implement support for this, but turn it off by default and discourage its use.


## [15:22]


Um, of course that doesn't stop a lot of implementations from just turning this on by default. Anyway, this deprecation advice is not always followed in practice. Um, also the reason giving for the deprecation advice is about SHA-1. It's not about PKCS, uh, one. So, uh, people may get the wrong idea. Think, uh, rightly think that, oh, the fact that you use SHA-1 is completely unexploitable in practice. I don't care. I'm going to turn this on. Uh, there can also be a lot of users that have like a preexisting configuration from before the deprecation advice. Then if the vendor implements a patch to change the defaults, they're probably not going to change preexisting configurations because that may break stuff. Also, very interestingly, there are applications that even when, uh, the weak cipher is turned off, uh, you can still send a message that's encrypted using PKCS-1. The server will still try to decrypt it and only realize that, hey, this, uh, I'm not actually supposed to use this after it's done the decryption.

So in a later stage it will block the connection, but then it has already done a decryption and for, uh, the particular attack that I'm going to explain, that's really all you need. And besides that, you only really need one vulnerable server. Uh, once you've got one place where you can do an attack on PKCS-1, you can also attack, uh, other servers that implement a more secure protocol, uh, but that reuse the same public key. So a quick, simplified primer on Bleichenbacher's attack, um, RSA encryption is, uh, basically just, um, uh, modular exponentiation of a large integer and because the message that you're trying to encrypt may not actually be a large integer, it may be like a value like one or something that you don't really want to exponentiate, you need to turn it into an appropriate integer. So how PKCS-1 padding does this, it, uh, adds a zero in front, it adds a two byte in front, some random bytes, a zero byte, and then it adds the message bytes and it treats this, this as a big integer it's going to encrypt.

Um, then after the server decrypts it, well, it gets, it gets this integer and it needs to convert it back in a message, so it checks this padding and then it removes the padding. However, if you do the decryption operation and you get an integer that doesn't match this, uh, padding format, uh, then the server should fail decryption. So it's, uh, will probably just, uh, reject the message. And the attack becomes possible at the moment when you, as an attacker, can distinguish the case where a server says, hey, this padding is wrong, I'm going to reject this message, or when it behaves differently and it does accept the padding, but maybe then fails in a follow-up step. Uh, and when you know that you've given, like, some kind of forged cipher text and you know the end result of decrypting it has valid padding, tells you a really important piece of information. Apparently, when I make the cipher text, it will decrypt to something that starts with a zero with a two.

So it's apparently greater than the, uh, than an integer that starts with zero two, and it's smaller than an integer that starts with zero three. So it leaks a bunch of information about the plain text.


## [18:23]


And RSA also has the property that if you have a, um, encrypted value that you don't know, you still have the capability of multiplying it with the value that you choose. So you can kind of multiply this unknown plain text with chosen, uh, with chosen values, give it to the server, get it to decrypt it, and if you do that in a, uh, in a clever way and you pick the right values, you can kind of, uh, look at all these, uh, padding results and you can kind of narrow down what the original plain text is supposed to be by constantly asking like, hey, does this have valid padding? Does this have valid padding? And then eventually after about a million attempts, which is why the attack is also sometimes called a million message attack, you should eventually be able to have decrypted all the, uh, all the bytes in the message and you've effectively broken RSA. So how would we, uh, use this attack against OPC UA?

Uh, well, in this, uh, handshake, there's really two things that are being done with RSA, uh, private keys, uh, on the side of you as a client and you're trying to impersonate the client. So that's what you want to attack. First, you have to sign a message and then, uh, you get the message back from the server and you have to decrypt that message. Uh, well, decryption is straightforward. You just do the attack, uh, in its, in its standard form and you have decrypted that message. You know the nonce, you can proceed to derive the session keys. Um, and for signature forgery, uh, that's actually, uh, like I've already pointed out, uh, not a big problem as well because RSA signatures are basically just decryptions of hashes. Uh, you can just do the exact same attack, um, trying to decrypt a hash value and, uh, you will end up with a valid, uh, signature value. So this would look something like this.

You start out by doing this whole, a million message attack until you've spoofed the signature. What's nice about this first step that once you've done this once, you can just keep reusing this message for the signature. So you, so even if the second step fails, you don't have to repeat the first step. Uh, so you do the whole exchange with the spoofed signature. You get a message back that's encrypted. You can't pick out the nonce from it yet. So you do this whole attack again, uh, while you keep the session open, which is important because if the moment you close the connection, the nonce will be forgotten. It's no longer useful. So you kind of have to keep like one TCP session open and use a different TCP connection to do this whole black and bucket attack and keep sending some people lives because the attack may take a while. And then once you're done, you know the nonces, you can proceed with the next step.

And, uh, as I've showed with the attack against HPS, we've already broken this next step. And even if you couldn't use that attack, you could also use black and buckers attack, even for the session handshake in just the exact same way. So to make this work in practice, you need a padding oracle. You need a way to have the server tell you, is my padding correct or is my padding not correct? For some implementations this is really easy because you just, they just give different error messages. With wrong padding, it says something like decrypt failed. When the padding is right, it says something like signature validation failed and you're done.


## [21:24]


You can just use the attack in its standard form. Unfortunately, most implementations that I tested have a very generic error message. So in either of the two cases, they show exactly the same error codes and you can't really tell by looking at it what actually happens. So then we need a different approach. And the typical way to do that is by using a timing side channel attack. Maybe if you have correct padding, their server will take a different amount of time than if you have wrong padding. And that's something that at first to me, well, sounded like, ah, this is probably not going to work. This sounds really hard. I've seen plenty of papers where people managed to pull themselves in practice against a padding Oracle attack like this, but you're probably going to have to send a lot of messages. You have to do some complicated statistics, deal with network, jitter. It sounds pretty hard to do a timing attack.

I mean, timing attacks have the reputation of being complicated, but keep in mind, not all timing attacks are hard. Think of like the SQL injection, where you add like a sleep five second statement to your query. Well, seeing if a server blocks for five seconds or not is very, very easy. And it turns out you can do something a little bit like that in OPC UA and actually make your timing attack super easy because the way that they're using RSA is very weird. The messages that they encrypt instead of like first encrypting a symmetric key, encrypting the message with that, they directly encrypt the message itself, but Oh no, the message always contains a signature. It's way too long to fit into a single RSA block. So what we do, we split up the messages in different blocks and we encrypt each block individually. So it's kind of the same principle as like the ECB modes, the notorious ECB modes in block ciphers.

Now for RSA it does have a little bit different weaknesses and different properties when you use like this kind of ECB modes. Actually you could probably hide penguin pictures pretty well when you do this, but it opens up all kinds of potential chosen ciphertext attacks. It's really a bad idea to do it this way. But what I could use it for when trying to get this black and bucket attack to work is to take like my guess for this whole black and bucket attack, this one ciphertext I want to try and I simply repeat it a hundred times. And then I sent this long message of like 100 concatenated ciphertext to the server. If my padding is wrong, it will try to decrypt the first message, see the padding is wrong, throw an exception, stop. If the padding is correct, it will decrypt the first message, move on to the second message. Well, it's the same, so the padding is also going to be fine there. Decrypts the third, fourth, fifth, sixth, and then it does like 100 RSA decryptions.

And then at the end it realizes my message is bogus. But yeah, RSA decryption is a very expensive operation and doing it a hundred times, you get some pretty sizable timing differences even when you use less than a hundred messages. So this is like a screenshot of my tool. And in this example you can see like when you have incorrect padding, it takes the server about 0.005 seconds.


## [24:29]


But when the padding is right, there's actually like a 10th of a second difference. So these are really huge timing differences that are like super easy to measure with just a simple Python script. Of course, maybe you have some weird network jitter or issues. So to weed out these false positives, I just simply made my Python scripts, try it a few times again when it does have a hit. And the nice thing about Bleichenbacher attacks is that the cases where you have correct paddings, so the slow cases in this, those are far more rare. So by far the most of these, like 99% of the messages you're sending will have wrong padding. So they actually go by really fast. And only the few interesting ones that give you more information, these are slow. So carrying out this attack with these big messages isn't actually that much slower than using an error-based padding oracle. So I implemented this whole attack in a tool.

I threw it at the lab environments. I made a nice little spitting progress bar thing with numbers that will slowly go up and they go up a little further. And then after about, in this case, eight and a half minutes, it has done the first stage. It has managed to spoof a signature. Then it goes to repeat the whole attack again while keeping the connection open. And then in 15 minutes total, bam, we've completely bypassed authentication on the OPC server by using a timing attack involving about a million messages that really only took 15 minutes, which suddenly starts to sound pretty practical for a timing attack involving a million messages. Now I'm cheating a little bit here because this is basically the fastest example I had, because this was against a server that had a very fast implementation written in C. On other servers, the attack may take like 30 minutes, one hour. Slowest case I found was two hours.

It's kind of the difference between you as an attacker having to go out for a coffee or having to go out for lunch while waiting for this attack to finish. And what I also noticed in most implementations, because this attack kind of happens in an early stage of the protocol and it's not really something that's really expected to happen. A lot of implementations don't do any logging because this happens. So if you're like an administrator and you look at the logs, you don't see anything suspicious. You really don't see anything at all. You don't see somebody is pounding your server with like millions of weird messages. So I tried out these two attacks on seven different implementations, which I choose because that's where I happen to be able to get a lab environment to work. Didn't need like expensive hardware or software licenses. And out of these seven implementations, five of them were vulnerable to at least one of the attack variants.

And four of those had like one of the two attacks that would work in the default configuration. So you didn't have to like turn on the legacy cipher. You didn't have to tweak anything. You just go for a default. You only turn on like the fact that you want to use this client authentication.


## [27:31]


And one of my attacks would work out of the box. And for some other implementations it would work if you had a particular kind of changed configuration. Now these are just seven implementations I managed to get my hands on. Five out of seven is a pretty high number. You can probably extrapolate from that that's well if we would try this against more implementations there's going to be more that's going to be affected. In the end, these doing these attacks successfully, they do depend on configuration. They do, they do depend on implementation details, but they are still protocol flaws. So it's safe to say that probably a lot more implementations will be affected. And indeed, after I did a responsible disclosure, I saw like advisories coming in from different vendors that had confirmed that they were also affected. Like CodeSys, Siemens, had a whole bunch of like WinCC products where they managed to confirm, okay, this attack is actually very relevant for us.

And there will probably be more implementations like that coming forward. So, well, I found this for seven different products, but there's probably going to be many, many more that are affected. How the hell do you even disclose this? Well, thankfully, I didn't really have to do this myself because OPC UA, practically all the vendors that make an implementation of the protocol, they're all connected to the OPC foundation, which like curates the standards. And they have like their own responsible disclosure policy, their own like security processes. And I could just go to them. I could just contact them. So I sent like a responsible disclosure. And they responded to me in like two hours and invited me to like a meeting in the exact same day to where I could explain the details of the attack, which is very impressive. Actually, my compliments to the OPC foundation. That's not how responsible your disclosure usually goes in my experience.

And they basically took over all the hard work of going through all these vendors, sharing the details with them. Some of them came back with specific questions for me and together the foundation would figure out like ways, okay, how do we solve these tricky issues in a way without breaking stuff for our customers? Which is very, very nice. I'm very happy. I didn't have to go through all these vendors one by one and maybe even vendors where I'm like, well, I didn't actually test your solution, but you may be potentially vulnerable because you're using a flawed protocol. I'm very happy that the foundation would pick this up for me. A bunch of CVEs got issued. What may be a little bit confusing here is that some vendors, they issued their own CVE and some, they reused the same like standard CVEs to describe the whole protocol flaw, but some products, they give different CVSS scores to the same CVE.

So it's a little bit confusing when you're looking at the CVE number. Sometimes you see one CVSS score. Sometimes you see another CVSS score and it kind of depends on the particularities of one implementation.


## [30:32]


The way this got fixed, it kind of depended on the vendor. Some were like, well, nobody uses HTTPS. I'm going to turn HTTPS off, for example. So for some, they tried to implement like mitigating measures to make it like very difficult to exploit like the padding Oracle in practice, made it impossible to do like the timing attack in the way that I described and also weren't like their end users. Hey, please turn off this legacy cipher. If you want to know what your vendor did or more importantly, what you should do when you are using an OPC UA implementation, I recommend to look at their advisories and their documentation. And when you're not entirely sure about that, I will first check if this is relevant for you. And this is relevant for you if you rely on this certificate based authentication, but you don't use user authentication because once again, my attack, it's still not going to get user passwords.

There's one small exception. There's like a feature where users can also use certificates to log in. And there's like a few edge cases where you can bypass that as well. But in generally it's, this only really applies to the certificate based system authentication. And if you're not sure, well, did my vendor patch this or not? Am I affected or not? I'm using something very different than what's listed here and maybe I can't patch it. I published a tool that's available now, that allows you to like scan for like these vulnerabilities, try to carry them out in practice. And well, I tested them against these seven implementations. So I kind of hope that if it works against these seven, it should probably work against other compliant implementations as well. If they don't, please send in a bug report. And hopefully that can help people out to try this out in practice and test their own setups.

So what can we take away from this? Well, first of all, always important to reiterate, protocol design, cryptographic protocol design is hard. You may be using like military grade encryption because you use AES and RSA and SHA-2 and so on. But these are really low level building blocks and putting them together in a way to make a, a valid protocol is very difficult. Ideally you really want to involve cryptographic experts, especially of course this protocol is like 15 years old. We know a lot more now on how you can actually design better protocols, how to use formal methods to test them, how to, uh, just better design practices in general. So for, to an extent, you can't really blame the original designers that they didn't really know, uh, these more modern approaches. Uh, but yeah, uh, if you would, if I would design a protocol like OPC UA nowadays, I would just say, yeah, I'm not going to design the crypto myself.

I'm just going to use TLS. Done. Please just use TLS when you have a client server protocol like this.


## [33:34]


If you don't have very particular properties that you need, just use TLS. Um, also, uh, well more than a quarter century later, once again, it's, uh, it's shown that like a Bucker's attack is still relevant. It was still relevant against TLS for a long time. People try to implement fixes to make PKCS1 work in some other way, constantly kept getting broken. It was broken when they decided it was a good idea to support this in JSON web tokens. It just keeps getting broken. Just never ever use PKCS1, turn it off. Uh, just treat this whole petting scheme as fundamentally, uh, broken. Uh, and yeah, when you look at vulnerabilities like this, of course, I am, uh, I may only be scratching the surface here. You really can't rule out that other people may find other crypto bugs in OPC UA or maybe in one of the other elements. I didn't even go for like the old pops up protocol or discovery protocol or certificate updating protocol.

There is quite some attack surface here. Uh, interesting research area. And we can't expect that, uh, well, once these particular issues are fixed, you'll be fine in the future. Um, so does that mean we all have to go back to using VPNs again when we, uh, post OPC UA over a trust boundary? Well, I would say depends a bit on the circumstances. If you're in a situation where you cannot easily patch and you know, you cannot easily patch in the future, then I would definitely say, okay, uh, like it's common in, uh, OT setups, you need to rely on network segmentation because you probably cannot respond quickly to when new vulnerabilities like this crop up in the future. If you can, uh, uh, patch promptly when like another crypto bug would turn up or, uh, uh, or another type of vulnerability, then it may be acceptable. Although I would recommend to use IP allow listing, uh, because if there's like something like a pre-authentication bypass or, uh, and the crypto bug that's allows a similar type of authentication bypass or a certificate validation bug and especially researchers from clarity have found a lot of those types of issues.

Um, you don't want to expose your thing to anyone on the internet. So at the very least for these authentication bugs, I would recommend to always use IP allow listing, especially if your server controls something important. Uh, and otherwise it really just depends on your threat model. And, uh, this is probably an eventuality you should take into account. And, uh, yeah, this was my talk. I think I finished, uh, in the time pretty well. Uh, if people have questions, uh, you can just, uh, approach me right around the corner outside of the hall here. And, uh, thank you all very much for attending.
