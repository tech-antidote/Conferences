---
title: "CTRAPS - CTAP Impersonation and API Confusion Attacks on FIDO2"
speakers: ["Marco Casagrande", "Daniele Antonioli"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - CTRAPS - CTAP Impersonation and API Confusion Attacks on FIDO2 - Marco Casagrande, Daniele Antonioli.eng.txt"
sha256: "7029d64fcdde576da3b63823b6edf005083e7f079e407c33ed7ff57716658e3e"
duration_seconds: 2240
words: 5355
text_chars: 31392
redacted_secrets: 0
converted_at: "2026-08-12T06:24:13Z"
---

# CTRAPS - CTAP Impersonation and API Confusion Attacks on FIDO2

**Speakers:** Marco Casagrande, Daniele Antonioli  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (37 min, 5,355 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Hello everyone. I'm Marco and today we will be presenting C-traps. C-trap client impersonation and API confusion on Fido 2. So I'm Marco. I'm a post doc at KTH in Sweden. I've done this work while I was doing my Ph.D. at Eurocom in France under Professor Antonioli which will talk with me as a co-speaker. My research is in security and privacy so I look at proprietary and standard protocols of IoT devices. For example, authenticators from Fido 2. I also look at mobile, so Android and transport very common ones like Bluetooth energy, Wi-Fi, NFC and so on. So now Daniele will have the stage for him. All right guys. Thank you for coming to our talk. My name is Daniele Antonioli. I'm an assistant professor at Eurocom, a university and research center located in Sofia Antipolis in the southern part of France. We do research in security and privacy. We cover topics including Bluetooth, electric scooters, Fido 2, web tracking and more.

Feel free to reach out to me after the talk if you're interested in doing a Ph.D. or a post doc in any of these topics. You can also check out my website for more information about myself and my research. This is the CTRAPS talk outline. So I will start by introducing Fido 2 and the CTRAPS contributions, attacks and vulnerabilities. We'll talk about some background information about Fido 2 and our threat model. Then Marco will take over and talk about really cool attacks with demos including the client impersonation attacks, the API confusion, man in the middle attacks. Then he will talk about the CTRAPS vulnerabilities, a toolkit that we developed an open source implementing the attacks, our experimental results and countermeasures and responsible disclosure we did with the Fido Alliance. Let's jump into the talk. So this talk is based on a research paper that we published in 2025 at IEEE Euro S&P called CTRAPS, CTRAPS, Client Impersonation and API Confusions on Fido.

Remember that we started this research in 2023 and it took quite a lot to get the paper accepted but when the paper got accepted then we also submitted the content of the paper to DEF CON to also showcase our results to a broader audience and not only to boring academics. So let's start with Fido to set the stage. What is Fido? Fido means Fast Identity Online version 2. This is an open authentication standard that was designed to be resilient against phishing. Phishing is a password compromise. It highly relies on the concept of authenticator. An authenticator can be like a USB dongle like this one.


## [03:03]


This is a YubiKey. It's quite popular. There are billions of authenticators like this one which are storing billions of credentials. These days you can also use your smartphone as a Fido authenticator. And then the authenticator is storing some authentication credentials which are cryptographically key pairs that you can use to authenticate. There are two typical use cases for Fido authentication. The first one is a second factor or multi-factor authentication where let's say you want to log in on Apple.com, you register with Apple.com, you register a Fido credential, and then whenever you log in, you enter your username, your password, and then you provide your authenticator to the backend. And this allows you to tolerate the password compromise attack. Because even if the attacker gets access to your password, it cannot get access to your USB authenticator, to your key pairs which is not phishable because this is a cryptographic key.

It is not a password that can be brute forced and so cannot impersonate you on Apple.com. More recently, Fido also enabled what is called passwordless authentication where you can log in on a website by using your username and only the passkey. And this is also known as passkey authentication. It's a single factor authentication while the other is a second factor authentication. These are the entities involved in a Fido authentication ceremony. There is the authenticator that you've already seen. Then there is a Fido client which can be a smartphone, a laptop, and there is a user which usually owns the authenticator and the client and wants to authenticate to a web service via the client like Apple.com, Facebook.com, Google.com. And so the user is contacting the relying party. Relying party is just some technical jargon using the Fido standard to indicate a service who is supporting Fido authentication.

And then Fido specifies two standard application layer protocols that are used to authenticate. One is called CTAP. CTAP stands for client to authentication protocol. And this protocol is used in the link between the authenticator and the user and can be transported using multiple technologies including USB and NFC. In fact, if you look at the authenticator, you have a USB port. You can plug in your authenticator in your laptop and then complete authentication. Or you can tap your authenticator to your smartphone. They are in NFC range. They exchange some CTAP messages and then you can complete authentication. Second factor or single factor. On the other hand of the scheme, there is WebAuthn. This is a communication protocol which is spoken by the client and the relying party, which is using standard internet-based technologies like TCPAP and TLS.


## [06:06]


And in CTAPs, we focus on the CTAP link, specifically on the security and privacy of the CTAP protocol. So the attacker is a protocol level attacker which is trying to find design vulnerabilities in the CTAP protocol. And we will see, we will focus on vulnerabilities enabling client impersonation and man-in-the-middle attacks. So with a client impersonation, the attacker is approaching the authenticator as a client and is trying to tamper with the client. In a man-in-the-middle scenario, the attacker has a man-in-the-middle channel between the authenticator and the client. And these attacks, they have critical impacts on the FIDO ecosystems. So here we talk about millions of keys and billions of credentials stored in these keys. And as an example of attack impact, you can delete credentials, you can track users based on their credentials, you can denial of service an authenticator, you can leak some sensitive data about the user, like the list of relying parties, the list of accounts, and so on and so forth.

And interestingly, while attacking CTAP, you are kind of agnostic to the link layer. So if you find the vulnerabilities at the application layer, then you can attack communications over USB, over NFC, over BLE, and so on and so forth. Plus, you can also affect, as a side effect, the relying party. So we mount attacks using CTAP messages, and without even sending one single web out and message, we can affect the relying party. Because if we delete the credentials in the authenticator, then the user will not be able to log in on the relying party. This is the list of contributions that we provide with the CTAP's work. After doing extensive security evaluation of the protocol, the application layer protocol, we found eight critical design vulnerabilities affecting even the latest version of CTAP. So up to CTAP version 2.2. And we show how to exploit these design vulnerabilities in 11 new ways.

So we present 11 new attacks, four of which are client impersonation ones, and seven of which are man-in-the-middle attacks, where we exploit a new attack technique in the context of FIDO that we call API confusion, which essentially means that the client wants to call APIA. We are in the middle, and then we play some tricks. We are able to bypass authorization and play some tricks to call API B on the authenticator and return an APIA OK message to the client. Then we also present a toolkit, which is on GitHub. It is open source. It is implementing our attacks. It also includes a virtual CTAP testbed that you can use to test your own authenticator without having to attack Apple.com or Microsoft.com. You can just test it locally, and then you can try the attack somewhere else.


## [09:09]


We also provide four attack clients that can be used on real devices against real devices to perform all of our attacks. We run an extended evaluation, experimental evaluation with real devices, where we compromise six of the most popular authenticators, including Yubico Authenticators, Faitian Authenticators, and open source Google Authenticators, and also 10 very popular reliant parties. So we are capable of compromising Apple.com, Microsoft.com, NVIDIA.com, and so on and so forth. And all the web services that are relying on their login mechanism as a piggyback. And then we also discuss eight fixes at the design level, because here we are focusing about the protocol. We are not talking about side channels, fault injection. We talk about the CTAP protocol. And we present eight design fixes, which are compliant with FIDO, too. All right. So now a very quick introduction about FIDO. So I talk a lot about FIDO credentials.

What is a FIDO credential? This is an ECDSA key pair, where you have a private key that you are using to sign something. You have a public key that you are using to verify something. There are two types of FIDO credentials. First type is non-discoverable. These are credentials where the private key, which is encrypted, is stored on the relying party. And this is historically the first type of credentials that was introduced by the FIDO standard. And something that might sound surprising to you, it was to me when I started working on this field, is that FIDO credential, the private key of your FIDO account, is not stored on the authenticator by default. But it is stored on the relying party, on the Apple backend. And it is encrypted, this private key, with a master key that is instead stored on the YubiKey. Then the second type of credentials is called discoverable. And these are credentials that are enabling the passkey authentication thing.

So the single factor authentication. And in this case, the private key is encrypted with the master key, and then it is stored on the authenticator itself. And a typical authentication flow with FIDO goes as follows. So you create a FIDO credential when you are interacting with a relying party. And then you use this credential whenever you want to authenticate using the client. So you use the client, you contact the relying party, you enter your username, your password, and then you authenticate with FIDO, or you only authenticate with FIDO. And the authentication under the hood is a challenge-response-based protocol, where the relying party is sending a challenge , the client is relaying the challenge to your authenticator, the authenticator is answering the challenge with a signed response using the private key corresponding to that credential, and then the relying party knows the public key and can verify your challenge and let you in.


## [12:10]


Now, within this authentication flow, there is a channel, the sit-up channel, which is providing communication means from the authenticator to the client using this sit-up protocol. This is a standard application layer protocol. As I told you, it is transported over USB, NFC, or Bluetooth Low Energy. And it is a request-response protocol. So the client is requesting something from the authenticator, like make a new credential, the authenticator is doing something, and then it is returning the credential. The first sit-up version was also known as UTF, or Universal Second Factor. And this first version of sit-up standardized in the FIDO specification two-factor authentication and provided what is known as an authenticator API. This keyword is very important because this is what we are attacking in the sit-ups slides later. The authenticator API is a way to manage, create credentials, generate authentication responses, and so on and so forth.

Then sit-up was updated to version two. The latest is 2.2. And this standard introduced password-less authentication with discoverable credentials. And it also extended the authentication API with features such as protection of discoverable credential. And remember that our attacks are focusing on sit-up two, and so they're effective even against the latest and most secure FIDO authenticators, clients, and their line parties. Specifically, we focus on seven core sit-up authenticator APIs, make credentials, get assertion, credential management, client pin, reset, selection, and get info. The get assertion API is the one used to authenticate. An API can have a subcommand. For example, the credential management API has enum RPIs or enum credential subcommands. And so you send a credential management API request with subcommand enumerate, please enumerate airline parties. You send this message to the authenticator, and the authenticator sends back the information.

And this communication is protected by two main authorization mechanisms. These authorization mechanisms are standard, so everybody's implementing this API. Everybody in the FIDO ecosystem must implement these authorizations. The first one is called user verification, and here is color-coded in blue. With user verification, whenever you use an authenticator, you set a pin. This pin is stored in the authenticator, and then you have to enter the pin in the client whenever an API requires it. For example, if you want to create a new credential, before being able to do that, you enter the pin on the client, on your smartphone, then the smartphone communicates to the authenticator. You are kind of authenticated because you know the pin, and then you can proceed. The second orthogonal authorization mechanism is called user presence, and here is color-coded in violet.


## [15:11]


This user presence is entirely managed by the authenticator, and it depends on the transport that you are using. If you are using USB for your authentication, your API call, as a user, you have to press a button on the authenticator. So you insert the authenticator, the client is asking something to the authenticator, and then the user has to press a button to confirm his presence. Instead, if you are using NFC, then NFC range automatically guarantees user presence. So you don't need to press any button. There is no user interaction required. You just have to put the devices in range. And this is the threat model, the CTRAPS threat model, just to understand the requirements of the attack before we talk about the attacks. We consider a scenario where a victim is owning a client device and an authenticator, like a smartphone and a YubiKey . The user is capable of performing user verification with the client and also user presence with the authenticator.

And the client is communicating to airline party via WebO10 and also to the authenticator via CTAP. And we assume that there is an attacker, protocol-level attacker, focusing on design issues on CTAP, which is either trying to spoof a client, so approaching the authenticator as a client, or as a man-in-the-middle position between the authenticator and the client. It is important to realize that we are not assuming any compromise on the FIDO devices. So we are not installing malware on the client. We are not installing malware on the airline party. We are not running a side-channel attack on the authenticator. Here we are just sending CTAP packets and we want to exploit the CTAP protocol. So this concludes my part and now Marco will continue talking about the client impersonation attacks and other really good things. So let's look at attacks and video demonstrations. We start with the client impersonation, but first we must have one single stop to understand what actually happens when you call a CTAP API.

So the user wants to call API A. They do this through the client. So the client understands if the API A requires user verification. If it requires user verification, the client starts the pin UV out protocol. This goes as follows. You perform a secure defilement in order to get a shared secret. Then the client sends encrypted a message containing the hashed pin for the authenticator that the user inputted and the permissions. The permissions give access control to the user verification token at the end of the process. Basically they restrict the token to specific API calls or end to specific relying parties.


## [18:20]


Now the authenticator checks if the pin is the one stored inside its memory. If it's correct, sends the encrypted message containing the user verification token. And then the client retrieves it and can use it for the whole session. So this token is reusable. You can use it as many times as you want without asking for another pin input prompt to the user. If you disconnect, you have to do this again. Now the client actually calls API A and attaches the token if it requires user verification. Then the authenticator understands if API A requires or not a user presence check. If it does, he does it. If it's correct, then the authenticator executes API A and sends an API A okay result. So this is how it works normally. Let's see how we tamper with it. So first the client impersonation attack that factory resets the authenticator. So we have on the left the authenticator and on the right the attacker posing as a legitimate FIDO client.

The attacker sends a reset command. It's one of the sit up APIs. This command requires user presence. So connects to the authenticator, sends reset, user presence check, passes, and then the authenticator deletes the master key which invalidates all credentials because without the master key, the authenticator cannot decrypt them. No matter if they are discoverable or non-discoverable. No credential is safe. Then it also resets the PIN and other data and settings on authenticator and finally sends the reset okay result. So let's look at the first demonstration. So let me open the link. So we have the FIDO authenticator put inside the pocket of a jacket just right now. Then I have my malicious app that connects over NFC to it. I'm trying to scan for it. Now it works. And we have the message here that... sorry I will show it better a little bit later. So you can see the message that the factory reset was successful.

And now I will show you that there are no credentials inside anymore. So I just use another function of my app to show that we connected and here it's supposed to print credentials but there is no credentials. So that's the factory reset. We have plenty more. So let's go to the tracking user from the authenticator credentials. So now I think this is very interesting because the get assertion API call is used for authentication but for us we use it to track the user by extracting identifiers.


## [21:28]


So get assertion usually requires user verification but if the relying party set the credential protection policy as off, it doesn't require user verification. And this is the case for big players like Apple and Microsoft. So here the attack goes as follows. The client, the attacker posing as a client prepares a list of insecure relying parties. What I mean by insecure is they have credential protection as off. So this skips user verification. Then the client, the attacker sends the get assertion API call. Here it is the client that decides if the user presence is required. So we control the client. We are the attacker. We are the attacker client so we put it as false and we don't need user presence anymore. And we also touch the list of relying parties that are using credential protection off. So the authenticator gives back a list of all credential identifiers from those relying parties and all user identifiers.

And these are the values that bind the online relying party user account to the credential IDs. Both of them are uniquely identifying the user. So we can put them in a fingerprint list and then track the user whenever he connects to our malicious client, our attacker, whenever we connect to it. So we have the demo. This is also over NFC. So this time we changed the piece of clothing. So we have the jeans if you were wondering if they work on every clothing. So now we are scanning the authenticator key and we can see no user input, no user consent. And we detected that there is one relying party, login Microsoft com, and we have two credentials inside. One, we have the credential identifier for the first one, 085C, and then the user identifier. And also same thing for the second credential. So that's how we can track users. Just to quickly add on what Marco showed, here we can track the user because the credential ID is unique.

And so this can act as a really strong fingerprint for a tracking attack. And so the first time we collect the data, we create the fingerprint. And then whenever the user roams around and then we scan again, the same YubiKey, we can track the user via its unique credential IDs. Yes. So we have four client impersonation attacks. I already shown you the first two. For the third one, we attack the client API.


## [24:28]


So here we just submit 10 incorrect pin guesses. So then the authenticator goes into a hard lockout mode that requires factory resetting. So you lose all of your credentials . And we also have another attack that profiles the authenticator given the hardware and software details. Now we go into the second attack technique . So in this technique, we have a man in the middle attacker between the authenticator and the client. So for example, a malicious USB hub, a malicious sit-up library, and so on. So first, if the client wants to call API A and it requires user verification, the man in the middle attacker runs the pin UV out protocol, forcing to create a user verification token that accepts all APIs and all parties. So we break the access control part of the protocol. So now that we can run any API we want on any rank party we want, we wait for the client to call API A with a token if it's needed.

And then we instead run API B on the authenticator. So we change, we confound API A with API B, which is the one the attacker wants to execute. Now, if API B requires user presence, we do the user presence check. Then API B is executed, a success message is returned, and the man in the middle attacker changes it to API A success to masquerade. We tested API Confusion with all combinations of the seven core sit-up APIs. You can see the seven APIs here. So let's give an example. Here we have on the left the APIs called by the user, and on the columns, so on the top, the APIs that the attacker wants to run, to execute. So for example, for Reset, it works that if the user calls MakeCredential, we can call Reset instead, because MakeCredential has compatible permissions, compatible user verification, user presence, compared to Reset. So we can use the user presence from MakeCredential to execute the user presence that Reset requires.

And you can see some one and choose. We have these requirements because if we want the attacks to be stealthy, in some cases we require NFC to have user presence, or we need credential protection to not perform user verification. But this is optional. This is just for having stealthy attacks. But we can run them anyway. Now, the first API confusion attack I will present is the Factory Reset.


## [27:30]


So the goal is the same as the client impersonation, but the technique is different. So now the client calls API A that requires user presence, because Reset requires user presence. For example, get assertion, selection, and so on. The middle attacker confounds API A into Reset. We do the user presence check, which the user expects. We delete the credentials, other data, and then we get back to the client saying API A was executed and not Reset. Then we have the second attacks that we are presenting for API confusion. So deleting discoverer credentials, which are the ones stored in the authenticator. Now we run the middle attack. We have the pin UV authentication protocol that forces the user verification for everyone. The client calls API A. We confound it with calls to credential management. And here credential management has different subcommands. So the first one you can see from the diagram is getCredentialData.

We see if there are any discoverer credentials inside. If there are, we enumerate all the parties tied to those credentials, and we get a list of the relying parties. So like Microsoft, Apple, Google, and so on. Then we use this to get, with the credential API call, the list of credential identifiers. And now we can delete one by one all the credentials stored in the authenticator. And then we tell the client that API A was okay. So here is a demonstration that we can use the user verification token for multiple API calls. And now, so here we have a setup that simulates. I'm in the middle over USB. So you will see a lot of traffic. Let me skip here. Yes, so you can see a lot of traffic here. So this is the client pin protocol that executes. Then we are enumerating the relying parties, enumerating the credentials. And at the end, you will see that we are deleting the discoverer credentials just in a little bit.

So yes, I will just wait two seconds. And you will see that the stored credentials are now zero. So here you can see deleteCredential. We deleted one, two, three, four credentials, and then zero remaining. So that's how you delete discoverer credentials using API Confusion. So we present seven API Confusion attacks. Some of them are similar to the ones I've shown you for the client impersonation. Other ones are like filling the discoverer credential memory space by using make credential API calls or denial of servicing authenticator using selection. So now as for the vulnerabilities and the toolkit.


## [30:33]


So we find eight CTAP vulnerabilities on the design of CTAP. So the fact that you can connect the CTAP client without authentication, the fact that there is no feedback about API calls, the fact that NFC range gives user presence, allowing us to connect without the user noticing, the fact that you can use destructive APIs without user authorization, trackable identifiers, issues with the user verification, user presence of reset and credential management API, and also the DOS over the selection API. So this is how the vulnerabilities map. Basically the first two are required for connection to the, for connecting impersonating the client and many in the middling, the traffic. The second one is for stealthiness and the other ones are specific to some or just one API. So then we released open source toolkit for connecting these attacks. We have three modules, a virtual testbed for CTAP experiments for other clients and the sectors which improved the sectors for CTAP.

So for the testbed, we have a virtual running party and CTAP client. We want to test, we wanted to test physical authenticators without breaking the actual running parties, without sending web authent requests. So this was very practical. We extended the Yubico testbed for this, the FIDO2 Python repository. We send raw CTAP packets with this so we can change them at a byte level. We can customize the configuration of the virtual client , changing the requirements and the curves, extension needed. We also offer templates imitating, simulating real running parties. So the correct running party ID, identifier, the real credential protection policy and so on. Then for the four attack clients, we have an Android app for the client impersonation over NFC. So it runs on the attacker phone like this one and we got up to two centimeters of range on my phone. Then for the Proxmark, we have a long range antenna that deploys the attack with a range of up to 6.5 centimeters.

So a better result. And also sends this custom CTAP commands over NFC. Also an Android app running on the victim phone that performs a client impersonation. And we are using our custom API instead of the Android NFC API. And finally, this Electron app running on desktop that can simulate API confusion over USB using the node-hid library. And performs that in the middle. So we evaluated six popular authenticators from Mubico, Feitian, Solokeys and Google. Some of them were open source. Some of them are closed source.


## [33:34]


They have different amounts of memory for discoverer credentials. And years are from 2018 to pretty recent 2023. Then also ten popular running parties. Among them are Apple and Microsoft that has the discoverer weak type here. Which means that they use the credential protection off on their discoverer credentials. Then we have sites like Adobe and the GitHub that use discoverable credentials. So they are stored in the authenticator. And then DocuSign and Facebook that use non-discoverable credentials. They are stored by their running party. So our attacks, they target the SITAP protocol. So they are working regardless of the hardware and software of the authenticators. So all of our attacks are effective on all the authenticators. Just one exception that four authenticators do not implement the selection API. So of course we couldn't implement the selection-based attack. Because there was no API there.

As Daniel already told you, we have impact also on the running party despite not sending any WebO10 command. So for the outcomes, we have the deletion of credentials. So you can't log in. You lose the credentials. You can't log into the account. So for example for Microsoft, this is for the Microsoft account. But you also use these credentials for the OneDrive, for the Office and even for your Minecraft login. Then we have the user tracking. So here we track the user and it doesn't work with non-discoverable credentials. So that's why we have the not applicable in our table. We only can track the user with discoverable credentials. And finally we have the service authenticator meaning that it doesn't work or it is busy. So you cannot use it for login, for authentication. So for the countermeasure disclosure, we have eight countermeasures fixing the eight vulnerabilities. So some of them are proposals for having a trusted sit-up client like PKI or attestation.

Then visual feedback for authenticator. For example, using the LED is one of the options. Then user interaction for the user presence even when doing NFC, when having the NFC transport. A different pin for destructive APIs. Rotating and credential protection on credential identifiers and user identifiers. Adjustments to the authorization of the reset and credential management. And rate limiting for DOS on the selection, for DOS prevention on selection. So for the disclosure, we informed the FIDO Alliance on November 2023. They answered us on May 2024 with nice feedback. And they also requested us to disclose our findings to the vendors.


## [36:35]


So we contacted Yubico, Soloveit and Google about the attacks. We also told Yubico that we found an implementation flaw in their firmware. And we got a CVE and an advisory from them. And now it is fixed for newer models, but not for the old ones. It is still there. And it involves data leaking. And then we also contacted parties using the credential protection of policy, which is Microsoft and Apple. So thank you for the attention. And if you want to meet us later, we have a live demo for most of the attacks. So feel free to come to us. Thank you. Thank you very much.
