---
title: "Can't Stop the ROP"
speakers: ["Automating Universal ASLR Bypasses for Windows-Bramwell Brizendine"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Can't Stop the ROP - Automating Universal ASLR Bypasses for Windows-Bramwell Brizendine.eng.txt"
sha256: "527ba9a5b248b926272eca2cabaf5a8f3c1e57d4a9762ae4a66652c383c402fb"
duration_seconds: 2561
words: 6554
text_chars: 35460
redacted_secrets: 0
converted_at: "2026-08-12T06:24:13Z"
---

# Can't Stop the ROP

**Speakers:** Automating Universal ASLR Bypasses for Windows-Bramwell Brizendine  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (42 min, 6,554 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


So I'm here to introduce our speaker, Dr. Bramweill Brunzedean, with his talk, Can't Stop the ROP, Automating ASLR. I am extremely excited for this talk, so would you all please help me welcome him. All righty. Welcome, welcome. Thank you for coming to my talk. The first talk in a while that I've actually had full control of my voice. The last few I've had severely affected vocal quality from my voice being ravaged. So in any case, my name is Bramweill Brunzedean. I am the director of the Vrona Lab at UAH and also an assistant professor and creator of various different tools, including the Shell Wasp, which allows us to utilize Windows Syscalls and Shellcode. The JopRocket, allowing us to do jump-oriented programming, and RopRocket for return-oriented programming, and an NSA-funded Shellcode analysis framework. SHARM, funded with about $300,000. A speaker at various conferences, DEF CON, Black Hat, Hack in the Box, Virus Bulletin, PAI on different grants from the NSA, and more recently, DLD.

I do have a PhD in cyber operations, which is a highly technical degree dealing with reverse engineering, malware analysis, and software exploitation. So now that we have all of that out of the way, why should you care? What is the big selling point of this talk? And really, the answer to that is, ASLR is the crown jewel in the arsenal for Microsoft in terms of defending against memory corruption bugs. So if you're using applications that are made in a non-memory-safe language, this is extremely, highly, highly relevant. So high entropy ASLR is hopefully supposed to defend and be very resilient against these attacks, but the reality is, it's not. It's a significant design flaw vulnerability that allows us to do some things that we probably shouldn't be able to do, but yet we can easily do so. In my testing, I've achieved 100% success on the different OS builds that we tested it on, and we expected nothing less.

What this talk is going to give us, we're going to have a mini-tool that we'll release, and actually released about 3 a.m., part of the ROP Rocket tool, and also have a demo, so some things to look forward to. So why did I do this work in the first place? Well, I saw that there was a bug bounty listed on Microsoft's website, valued at $5,000 to $15,000. I thought, well, you know, I could do this. I looked at the scope, what was in scope, what was out of scope, and it seemed like something that I could easily do, and so that was the motivation for this work. So a one-page slide crash course into ROP.


## [03:04]


Well, what is it? It's really a way for us to have bits of executable code chained together, allowing us to then overcome a mitigation, such as data execution prevention, or address space layout randomization. If we can do that, then we can then execute our shellcode, or achieve our arbitrary computation. So these are highly, super, super relevant ASLR. It's been around for a minute or two. So first came about in 2001 with PAX. 2004, we figured out, you know what, we need more entropy than 32-bit, because if we don't, then we can simply do brute-forcing attacks. It took a little while for Microsoft to make use of ASLR, but they eventually came around to it. More recently, what's been most fashionable in all the rage has been different side-channel micro-architectural attacks. So some really superb research on some of that right now. We're skipping through some of them, just in the interest of time.

And then the last there is this universal ASLR bypass, which is really an enhancer, or a multiplier to a pre-existing vulnerability. So some classic approaches to an ASLR bypass, and this is not 100% all-inclusive, but one could be a memory disclosure bug. So we might have something like a use-after-free. We have some particular value that's being displayed, maybe printed to a screen. We free that. We then maybe cause that particular memory location to be overwritten with something of the same size. And then when you go to display it again, hence the use-after-free, then maybe you disclose something like a function pointer. And then from that, you can extrapolate and determine the base address. Other things, page spraying, heap spraying, brute force attacks, those used to be highly effective, and then to a lesser degree. But with high entropy, ASLR, the entropy is just too great for those to really be feasible.

If you go back much further, it's simply just using hard-coded predictable addresses if you didn't have ASLR. So I guess that's not really a bypass. Our approach here is we need to have one existing vulnerability. So if you don't have something like, say, a memory corruption bug, then this is not going to work. And you need to have the ability to execute ROP gadgets. So if you don't have that, if you don't have the ability to use ROP, then this will not work. So we're targeting 64-bit windows, and in particular, high-entropy ASLR. And we can have force relocate enabled as well. And our goal is to get the base address of system DLLs. In this case, we then have effectively bypassed ASLR, and we now have access to those, and we can easily, readily use them to massively expand the attack surface.


## [06:14]


And we need to have a payload size of sufficient size. The benefits to the attacker, massively expanding the attack surface. So maybe you have a small application, and there's just really not enough to really do much. But with this, you now have the ability to potentially add thousands of additional gadgets, and that can enable you to do something more substantial. It is portable across binaries, meaning that this general approach, you can take it and apply it to, hopefully, any Windows binary. It's not tied to a specific application that happens to be vulnerable, hence the universal ASLR bypass, and the title of this talk. So this does make use of the process environment block, the PEB. I'm sure a lot of you are familiar with that. And it's something that's easily readable from user mode. There's some common ways to disclose that that we'll get into, but there's also some rather uncommon ways, as well, that I think will probably be surprising and interesting to most people, though not necessarily everybody.

So we'll go through all of them, and this technique can make use of any of them. But we'll start with the most, so this page right here, we see the PEB being displayed, the threat of environment block. And so that can be pointed to by the GS segment register, and then at offset 60 is going to be located the process environment block. So if we have the ability to access the GS register, then we can easily access that. And then if we don't, that's fine. There are other ways we could obtain that, as well. So the PEB is defined as a structure . One place that it's defined, as you can see, the Windows internal header file. You could also view that in tools like WinDebug and see some more of those reserved. In this particular thing here, we see reserved 1, 2, reserved 2, 1. But if you view that in WinDebug, you can see the actual titles of all of those. But in any case, our goal, our general approach here to this is, firstly, somehow we need to obtain the PEB.

There are many ways in which we can do that. So the most common easy way to do that would be the GS segment register at offset 60. We saw that that points to the process environment block. If we can do that, great. If we're doing it in a 32-bit, then that would be FS30 would hold the PEB. If we're doing WoW 64 and we want to access the PEB, you could do something called Heaven's Gate, where you transition from 32-bit to 64-bit mode, which is really very fun and interesting to observe. And in that case, the R12 would hold a pointer to the TEB, and then you could use that to then go and obtain the PEB. And some other more interesting ways, Windows syscalls, we can use them. We'll explore those. And then RDGS base is another less familiar way that we will explore.


## [09:14]


We need to have a way to reach the PEB LDR data. We'll check that out. And then we can choose one of three doubly-linked module lists. And then we'll follow the flinks a set number of times based on the general approach that we follow. And then finally, we will obtain the DLL base, which will be a field that will be available and accessible. So we'll go into great detail momentarily. But first, let's take a look here at an example of one way we could leak the PEB. In this case, it's a very ugly, kind of nasty-looking gadget. And we see sub-RCX, Q word pointer, GS, RSI plus some big numbers. So what we want to do is we want to have RSI be equal to 60. I already have a big number there. How can I do that? The answer is simple, integer overflow. So we just add another really big number so that once you add them together, they're equal to 60. RCX, we can set that to zero. And then the result will be a negative value in two's complement form.

And then we can then do neg RCX, which will then restore it to its original form. And that will be the PEB. We also have to make sure that the EAX register is set to valid memory. And then the POP and the RET10, we can easily compensate for those with filler. So going back more to the specifics of our approach here, we have nine different variant approaches. We're targeting three different DOLs. So in any particular Windows application, or almost a vast majority of them except for really tiny minified applications, but any real world application, you will always have these three DOLs loaded. And depending on the particular module list that you make use of, they will be loaded in a preset order. So we can manipulate and use that. And this is a table that I created. So we can have the number of different flinks that are indicated showing the steps that we would take. So these are doubly linked module lists.

So essentially we have a module list that will have a forward link pointer and then a backward link pointer. So if we take one flink, then that takes us to the next one, to the next one, et cetera. And then the offsets for the list and the DOL base, those can vary. You notice that all of them, you add them together, the sum is always going to be 40. Not that that's super relevant, but just kind of interesting. So what we're going to do is we're going to do a case study of one of these approaches and we're going to super great detail. And the others are similar, but just slight differences in terms of what you're pursuing. And then this is something that we'll also see that can be fully automated by the tool. So it'll generate all the gadgets and we'll see that momentarily. So here's a handwritten script to get kernel 32. And you notice these are all really basic, simple gadgets. We're not using anything unusual like move dereference, some offset.


## [12:17]


We can have one move dereference gadget and a lot of flexibility. And so it's really designed to be very simple and basic gadgets that are easily readily available on binaries. In terms of capturing the PEB, so in this case, we're doing it with a leak in the GS register. We can look in WinDebug and we see, okay, RDX, what is there? It's the PEB. We can verify that with the bang PEB. And then we need to add a distance of 18. That will then allow us to reach the PEB LDR. We can add that. In this case, we're adding, we're popping that into one register and then doing, adding the two registers together. If we don't have the ability to add registers, not a problem. We could also do something very simple. For instance, 18 increments, very easy, common gadgets to obtain. And then we can then dereference that. We get the LDR location. So we're basically traversing Windows structures here. And then we want to reach the init initialization modulus.

And so in this case, we're going to add 30. We could consult that little table that I showed you earlier. And once we do that, we can then obtain the LDR and initialization order modulus location. And then we can follow that. That'll be returned to us in RBX. So we complete those ROP gadgets that you see on the slide there. And then now let's follow in the flanks. So we get to our first flank. And what that is pointing to is simply another flank. And that's pointing to another flank. And so we can easily traverse that just by simply taking one, dereferencing it, getting that address, and then dereferencing it until we get to the location that we so desire. So we can, our initial one that was on the previous page can take us to NTDL flank. We take another move dereference, getting us to the kernel base flank. And then finally, a third dereference will get us to the kernel 32 modulus for an initialization.

Or I'm sorry, the entry, not the modulus. And then within that structure, there's a field called the DLL base. And so that is really what we're focused on. If we can obtain the DLL base, that gives us the base address. That means we can take that base address and that's the ASLR bypass. So we can then from that utilize simply offsets. So we're going to proceed forward and we see that once we capture that, we get that into RBX, we dereference that, we can check, we can do the LMF command and window bug and we see clearly, okay, this is kernel 32. We have obtained our ASLR bypass. Another view from the window bug there, we check in RBX and it maps out.


## [15:22]


So the kinds of gadgets that we can use. Again, this whole approach is we're taking, trying to take something and make it simple and easy to do with ROP. We're not writing shell code. We're not writing malware with inline assembly where we can just use whatever we want. We need to use it with commonly available gadgets. So we need some way to leak the PEB, some way to add offsets. It could be a series of increments and then some way to dereference. Now the registers being used does not matter, not important. And we can have a single dereference gadget. We can use it multiple times and it could also be XOR dereference or add or sub dereference. We can work with any of them. Some way to load a value, whether it's popping or integer overflow or negation. Lots of opportunities. So what are some alternative routes? So a lot of you may know about the GS register but let's check out some of these less common ones.

Because you know what, not every binary is going to have a way to leak the GS register. So can we proceed forward without that? Absolutely. So two different syscall roads or paths to get to the PEB. One is NT query information thread, which will give us GIF wrapped, the tab pointer. And NT query information process, which will then return to us the PEB. So we've got to set up those syscalls. So the first one, NT query information process. How specifically do we set that up? So we just simply follow the X64 system calling convention but with some minor twists that we'll get into. So into RDX we can provide zero, which will specify the process basic information. R8 can be any memory that is writable. Does not have to be... you can use multiple gadgets to do that. Could be the stack. Really doesn't matter. And then R9 we want to provide the size of the process basic information structure. R10 we want to do negative one.

So that's going to be 16 Fs. And then the syscall system number, the SSN, we will provide 19. And I'll talk about that momentarily. And then in some way to invoke the syscall, a syscall gadget. And then after that, we'll get into EAX or RAX, NT status code zero, which stands for SLK. And then we can then go and retrieve the PEB base address, which will be located from that structure we created. But it will be an offset eight. So the SSN, it's a Windows syscall. How can we deal with this? Because some of you know that with Windows syscalls, there can be challenges. You need to dynamically resolve the SSN because it can change from one OS build to the next. And that is super true. So we may have techniques like, you know, syswhispers, hellsgate, but if we do our information gathering, we determine, hey, we're targeting Windows 10 or 11, then in that particular case, it's completely stable.


## [18:33]


It's the same SSN for all of them. So we could actually ignore that step. But if you wanted to also target Windows 7 or other earlier versions, then that could be something extra that you could add to do that. So the structure for the process basic information, you can see that provided there for you. And then the one we care about is the PEB address or PEB base address at offset 8. So here's some inline assembly just to kind of demonstrate what we could do in order to obtain this. So we can set RDX to zero by Zoring it together. We provide a memory address, RSP 50. And then in R9, we're providing our size. And then R10, we are providing negative one. And you might say, well, the calling convention is you should do that in RCX. But because we're doing syscalls, what's going to actually happen is you have a native API wrapper, and that will just simply take the RCX value, move that into R10, so we can skip loading our RCX.

It's not necessary. We can instead just load the value directly into R10. And if we don't do that, it's not going to work. And there's also a fifth parameter, the return length. Guess what? It's optional. So we can just simply ignore that. And that's true for both of these syscalls. The SSN, we can provide that directly, and we have a syscall value or syscall instruction there. Once that's done, then we can simply retrieve that at offset eight from that structure that we provided. So this, again, we'll see this in a ROP form momentarily from the tool that will automate that process. We can check that out after we do it. We do the bang peb. We compare what we have in RAX. Does it match? Yes, it does. So totally works. In T query information thread, another syscall that can work. Very effective and also very similar. Some minor changes. So zero is going to specify the thread basic information structure.

Again, any writable memory is adequate for our R8 register. R9, we can provide the size of that structure. And then within R10, again, that is going to be the handle. In this case, it's going to be negative two, which is going to be the current thread, and we'll provide that in two's complement form. The SSN, we can provide that directly. And then the syscall instruction, we can do that cleanly with a nice syscall gadget. And once we get the result where we get the tab, we'll have a couple extra steps. We'll need to also then add 60 to that and then dereference that. The SSN, same situation, same story. We don't need to resolve it if you're doing Windows 10 or 11. If you want to include other ones, then you would need to utilize a technique to dynamically resolve the SSN.


## [21:37]


So we could check out the inline assembly form of that. We could set RDX to zero. We provide a buffer for our structure. We provide the size of that into R9, and then the SSN of 25. That's how we specify this is the syscall that we're using. We don't have to worry about resolving any addresses for syscalls. We just need the SSN directly. We have the negative two and the two's complement form there. We call the syscall, and then whatever location we provided as the memory, we can then go and retrieve that at offset eight. So again, very similar to the previous one. We can execute it, check it out in a WinDebug, and we get a particular value. We do the bang tab, and it matches. And then there at the bottom, circled in silver, we can see the PEB address. That's where we want to get to eventually. So we can simply add 60 to that, because offset 60 of the tab points to the process environment block.

So in this case, you can see there we're just doing RAX plus 60. Later on, you'll see that in ROP format generated by the tool. So another option, and this is one that probably most people are not familiar with, is RDGSBASE. It's an actual assembly instruction. So you could provide it RDGSBASE, it's going to return the address of the tab directly into RAX. So that would be very effective if you were writing, say, shellcode or malware, and you wanted to resolve the base address of a DLL. If you're doing it with return-oriented programming, we have to look at what are the gadgets that will be found. This may be too obscure to be commonly readily available as a use in 2024. We're going to check this out in WinDebug. We execute it. Very simple. It gives us the tab very easily. And then we can add 60 to that. And once we do that, then that gives us 2AC00. And that checks out as the peb there. So another option, what if those four things do not exist?

Could we still do this via ROP? And the answer is yes. We can effectively synthesize our own ROP gadget. For instance, you could synthesize RDGSBASE. So if you have the ability to write bytes to memory, let's say, for instance, virtual alloc and t-allocate virtual memory, I mean, those could potentially be blocked. But let's say we have some way to do that. Then we could write that to memory, and then we can then call that gadget and then make use of it. It's a technique that we made use of in our DEF CON talk last year on process injection via return-oriented programming. So let's check out the exploit demo.


## [24:38]


We have a little video here. So let's hope I'm sophisticated enough to operate this. Wrong video. Okay. There we go. So we have a leaked PEB right there. And we're going to verify it with a bang PEB command. And we can load the LDR offset there. And now we have the LDR address there. We can continue on. Now we want to set up getting the LDR and load memory. We got that. Now we're following different flinks. And at this point, we want to add 30. And that will then allow us to reach the DLL base. And we add that. Now we have the DLL base. We're about to get it momentarily. And we see that that is... looks like it... now we have the ASLR bypass. We've achieved it. So that's simple. And we have the bypass. So the reality is probably 99% of the time or greater this is going to work. If you have these ROP gadgets, you set them up. I mean, this is a design flaw vulnerability. It's almost certain that it will work.

I don't want to say 100%, but very likely. Do we need to leak the GS register? No. There are alternative ways as we explored. Do we always need or want to do this? The cases in which you want to do this is if you want to have it as a multiplier or as an enhancer. You have the ability to call ROP, but you need significantly more gadgets. You want access to some of these system DLLs. If you can obtain that, then that vastly expands your attack surface. You can do things that otherwise you may not have previously been able to do. So some possible mitigations. How can we fix this? How can we address this? How can Microsoft address it? Well, one is to randomize the LDR data table entry different, insert unpredictable offsets into that to break some of these fixed offset traversals. So you can't just use these hard coded offsets. So it's unpredictable gaps. This could be done on a per process basis or just set up once each time you load the operating system.

Either way would be effective. And so in that case, if you were to try to use one of these fixed offsets, then it simply would not give you the desired result and it would break this approach.


## [27:40]


We could also have privileged PEB access to that. So block direct segment loading and user mode. That could be an option. Maybe we have a new syscall that is a little bit more responsible in terms of how it provides access to the PEB or the TEB. And we could check things like what is the control flow graph that led to that? Did we follow a valid path or are we doing things that are kind of unusual? So that would be a major architectural lift, but it would eradicate this set of bypasses. Some other possibilities is pointer obfuscation or encryption. So maybe we have encrypted values and so if you go and obtain it, it's not enough because it's encoded. So you could use the Windows API. It already has some pre-existing things that could be used or just simply even something as simple as an XOR key. So an additional step that could be relatively simple to implement, in my opinion, but could be highly effective so that you can't just simply decode these PEB values on the fly.

We could also have approaches like fine-grained ASLR, dynamic ASLR. With fine-grained, we're going to shuffle functions and even basic blocks so that if you do obtain a bypass, it doesn't matter because the other things have been randomized as well. Dynamic ASLR where we have periodic rebasing. So it may briefly be effective, but then it's going to change and so it no longer is useful or relevant. So these would serve to complement not to replace ASLR. And we could also have execute-only. So you could have that implemented via hardware support, making PEB and loader pages non-readable. How would the access be granted? That would be something that would need to be carefully determined, but it could be viable. We could also simply enhance the existing exploit detection. So things like EAF and EAF plus have been around for a very long time. So we may add some additional heuristics to that or Microsoft could.

And so flag some of these abnormal list entry walks and it wouldn't prevent it, but it could stop it from being effective. All right, so let's dive into the ASLR mini bypass tool. So this is built off of ROP rocket. And what it will do is everything that we talked about, it can generate those complete ROP chains. So if you wanted to do this and not have to spend the time to write the code, you simply use the tool. So it does it in 64 bit and it will do all nine variants and it will output it as Python. Here we can see a complete Python exploit script. So it takes care of a lot of the mechanics there. And we will zoom in on that here. So one way is the PEB disclosure via the GS register. So we can set that up and obtain that. And it follows the various steps. And it does it by kind of a hybrid approach of semantic searching.


## [30:43]


So we may say, okay, I want to load a particular value into this register. What are the different ways in which we could do that? So it's not something that's just a simple hard coded recipe, but there's a lot of very rich flexibility that's possible. We can also do it via the RDGS base, the NT query information process and the NT query information thread. In this case, to obtain our writable and readable memory, we have it set to a particular location relative to the stack. We use emulation to then calculate the distance to that. And so we use that to provide the parameter. And then later on when we go and get that as our result and add offset 8, it automatically takes care of that and does emulation to precisely calculate what that's going to be. So this is actually a little bit more complicated than what I showed you. And again, it's fully automated. So it does it through those four methods, the GS register, RDGS base and then the two sys calls.

And if you get the ROP gadgets from it, then it should work. It should be totally reliable if it provides those gadgets. So nine bypass techniques are supported by the tool. We have kernel 32, kernel base, NTDLL, and for all three of the different module lists. And moreover, we implement them. So actually it'll generate not nine, but actually nine times four, which is 36. So we can do it for the GS register. We'll get nine variants. We can do it for the RDGS base, for the NT query information process, as well as the NT query information thread. All of them will work. And we'll see a demo momentarily. But if you're interested, go ahead and take a picture. It's going to be part of the ROP rocket. I updated it about 3 a.m. this morning, providing this additional functionality. So tool demo time. Okay, there we go. So here's the tool. We're running it. And right now it's generating all of those.

It's going, you know, it's 36 of them and it's very fast. Of course, this can vary from application to application and whatever the attack surface supports. And also at the same time, it's going to write it all to file. So obviously you don't want to copy and paste from a command console. So you can take one of them and make use of it. So a very effective tool. So the disclosure timeline for Microsoft discloses April 15th. I reported it to them. May 2024.


## [33:45]


Microsoft acknowledges the validity, decides to do nothing. That's kind of unfortunate. Microsoft in July, they asked to meet during Black Hat and then later I do meet with them. Today I'm releasing the tool. So I got an email from Microsoft, May 1st, 2024. They claim the report is valid, but that they prioritize vulnerabilities that are assessed as important, critical severities for immediate servicing. They don't consider an ASL bypass to be relevant if it doesn't cross a security boundary. What that means is the fact that it relies on a preexisting vulnerability. You know, but that is true. But the goal of this is to be a multiplier or an enhancer. And in terms of the patch history for Microsoft, we see that in 2013 there was another ASL bypass that was making use of some readily available address. And it was disclosed publicly, not to them, but Microsoft did actually fix that. This has been reported in April 2024.

They have not fixed it. They have not shown any interest in fixing it, unfortunately. It's sad that they don't care, but I guess that's the reality. So some context for them. Invited me to meet with them on July 11th. I didn't acknowledge them for a couple weeks, but eventually I did agree to meet with them on the 22nd of July. I got another detailed email on the 30th of July asking for detailed information. My response was, can you provide me more information on why you decided to pay $0 for the bypass? And my whole purpose of doing this was to hopefully obtain a bug bounty. I didn't get a response, but I did get a response in person when I did meet with them. So they did ask to meet and I met them on August 7th. We spoke about 50 minutes in detail regarding the case. One of their issues was they wondered how they could change the scope so that if somebody like me wanted to do an ASR bypass, then they would have legitimate grounds to not pay it.

My thinking was you have your scope clearly defined on the website and I followed that. I looked very closely at what was in scope, what was out of scope, and I think it would be better to honor their pre-existing scope, but I think that they had other ideas, which is fine. It's their choice. They mentioned in 2013 there was a post they made where they said, you know what, we had this vulnerability, which is the same vulnerability we talked about a moment ago, and we already knew about it, but if that person had reported that to us, that would have been really cool.


## [37:02]


We could have then given them a bug bounty, but they didn't. By the way, you should go and submit your vulnerabilities to us. So it's disappointing that they make statements like that and then they publicly contradict their kind of general stance on that. But in any case, they decided not to do that. Why does this matter for Windows malware? So we could go much further beyond simply what we have been talking about. So if you were to go and fix this for ASLR, that would be great, that would be cool, but it would be much more hugely impactful for the malware ecosystem in general. What that would mean is a lot of malware will utilize some of these pebwalking techniques in order to then go much further and dynamically resolve runtime addresses. And by doing that, you can kind of hide and conceal what you are doing. So if we were to fix that, then that would break it so that much of the current existing malware simply would not work.

It could work. In that case, you would have to use normal imports. That's something that could be easily detected from a static analysis standpoint. A tool like PA Studio could easily enumerate that. You could also do things like getprocaddress , a little library. And those are also very easily detectable things from a static analysis standpoint. So fixing this for ASLR would be much more hugely impactful in terms of securing the enterprise or securing just any Microsoft computer from the threat of malware. Forcing attackers to make their malicious functionality very obvious because if they were to resolve it by using techniques like pebwalking and then traversing later on the PE file format, those are things that are not easy to be detected from a static analysis standpoint. You would have to drop it in a sandbox or do some type of dynamic execution. So a massive, hugely impactful gain for defenders.

But I guess that's not important to them. Another alternative for them going in an opposite direction is it could force them to use Windows syscalls to then more, to obscure their activities. Now, syscalls can be much more challenging to utilize. They're a lot more complicated. It's not something that every malware author would be able to do just because of the complexity of that. And some functionality that could be easily implemented via Windows APIs.


## [40:04]


The same thing may not be true necessarily for Windows syscalls. So either way, it would be a huge win. So some takeaways here is we have these existing structures, the peb, the modulus. These have been a single point of failure. They've been existing in this form for decades. And, you know, it's an inherent design flaw and it should have been corrected a long time ago. But unfortunately it has not. And so we've seen how we could take this general approach to peb walking and then apply that to ROP to make it much more simple, basic, where we can use a few easy to find gadgets, even the same ones over and over again, to achieve it in a relatively simple way. And how we can even take that and then automate that with a tool so you can accomplish that in seconds. You could maybe have 30 seconds and then you have 36 different approaches of bypassing ASLR. So it would be great if Microsoft would patch it.

They've really shown no interest in doing so. And it's not really something that could be achieved with a patch. It would need to be more of an architectural lift. But if they were to do that, it would defend against ASLR and also a lot of malware that's currently existing that's native CC++ compiled would no longer work and force the attackers to implement that malicious functionality in a different way. Unfortunately, you know, Microsoft does not care. They've made that clear. But maybe someday in the distant future, we may see a more responsible approach that can preclude some of these attacks. But I don't see that on the horizon for the immediate future. So with that, I'd like to thank you. DEF CON is an incredible place. Very, very special place. And thank you for coming to my talk. But more importantly, thank you to all of you for coming to DEF CON. Feel free to contact me. You can email me.

You can check me out on GitHub. The address of the tool is there. Feel free to add me on LinkedIn or make contact, however you like. I won't take any questions now. But I'll step to the front there and then possibly outside. So feel free to come and chat with me. And thank you very much.
