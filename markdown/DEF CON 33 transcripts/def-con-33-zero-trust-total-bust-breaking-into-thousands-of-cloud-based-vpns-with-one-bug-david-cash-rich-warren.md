---
title: "Zero Trust, Total Bust"
speakers: ["Breaking into thousands of cloud-based VPNs with one bug-David Cash", "Rich Warren"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Zero Trust, Total Bust - Breaking into thousands of cloud-based VPNs with one bug-David Cash, Rich Warren.eng.txt"
sha256: "9fea54d4f28c811c2da9d19f49f24e5b80b99633b0aed8809a96505b3abeb0b7"
duration_seconds: 2320
words: 6181
text_chars: 34288
redacted_secrets: 0
converted_at: "2026-08-12T02:50:00Z"
---

# Zero Trust, Total Bust

**Speakers:** Breaking into thousands of cloud-based VPNs with one bug-David Cash, Rich Warren  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (38 min, 6,181 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


Good afternoon, DEF CON. Our next talk is Zero Trust Total Bust. Please give a big welcome to our next speakers, Dave and Rich. Hey everyone, thanks for coming to our talk about zero trust network access. I'm Dave, this is Rich. We both work at Amberwolf, which is a UK-based consultancy specialising in red teaming and bone research. We're covering a lot of bugs in this 45 minutes, but all of the technical details will be on our blog within the next few weeks, some of them straight after this talk. So everyone's heard of zero trust. The theory is that no user, device or request is inherently trusted. Everything must be continuously evaluated against a set of access controllers. So even if you've got valid creds, you won't get access unless all the criteria are met. There's some terminology that we'll be using in the presentation. If you think of SaaS as like checking whether the user and device are permitted to access a resource, acting like a traffic controller.

And then you've got steering, which is routing traffic based on conditions, so like ACLs. Identity providers feature in this. It's not their fault. And we've included a definition of privacy just as a reference. So the number one question we got asked after our last presentation was, what about Zscaler? And we've all seen adverts similar to this saying that legacy VPNs are dead, need to move to the cloud, zero trust network access. We also routinely see these products on red team engagements, so we thought we'd better take a look at whether they live up to their claims. So a picture of the scene. Everyone's panicking over the latest Advanti bug and they want to move to ZTNA. So what happened is that people just replaced their VPN with a ZTNA product but then just used it just like a VPN, so no granular access controls, just brokering connections into the internal network. And actually, even worse, we've seen a lot of this trusted corporate LAN concept used in the ZTNA world, but instead this time, you're sharing an IP space with every other customer of that ZTNA product.

So we see conditional access policies like this, where there's an exclusion for MFA from the ZTNA public IP ranges to reduce authentication friction. And this means that we can just bypass MFA by coming from those ranges. A lot of the vendors do have a concept of dedicated IPs, so if you do have to do this, then maybe look at that. But enough of the preamble, let's get into some actual bugs. So checkpoint ZTNA offering is called Harmony. We cracked open the Windows client and immediately saw a reference to an encrypted private key within an upload to SFTP function. Interesting. This function is called whenever a customer chooses to upload log files to the checkpoint supporting.


## [03:02]


And that decode resource function you saw, that just decrypts a resource from within the binary with a hard coded AES key and IV, and the result is your standard open SSH private key. The log files were also encrypted before being uploaded to the SFTP server, but the encryption key was predictable and brute forceable. The log files contained JWTs that lived for 30 days, so the impact is that any customer that had uploaded logs, um, was, could, could just be compromised because those JWTs can be used or, or straight to the, uh, ZTNA service. Um, read access to the SFTP file system was removed immediately upon reporting to checkpoint. That's pretty quick. Um, we've got some more bugs in this product as well, but they are still in disclosure at this point. When we start this research, we made like a little wish list of, uh, what are the best bugs we could possibly get, uh, in all of these products.

So we're going to keep this table updated as we go through the presentation. So next we're going to take a look at Zscaler, which is a very popular ZTNA product. Um, you see this a lot in red teams. And we'll start from a completely unauthenticated perspective. So the first thing that happens when you launch a Zscaler client is you put in your email address. As soon as you do that, the client's going to start making requests to the mobile API. So the first request it makes is, uh, it's encrypted here and, um, we don't have time to go through reversing the exact encryption routine here, but to summarize, essentially it generates a random AS key. It combines that with a hard coded key and then it encrypts that with RSA and adds it to an auth token header. And then that blob that you see there in the post request is encrypted with AS. So we'll just decrypt that now. So you can see here it sends a device type and a user email and then that uses that to fetch the cloud endpoint host name.

And then the next request is this get AEP. And again, we just decrypt this. Uh, and so before you put in a password, it pulls down this policy, uh, pre-auth, and it's called the pre-auth configuration for the tenant. So what does a pre-auth config contain? So this can include configuration details such as the authentication settings, such as whether the tenant is using IDP, or it can contain things like the cloud features that are enabled, whether they're using ZPA, uh, Zscaler private access, or just ZAA, uh, and what features in the ZCC client are enabled. So things that we might be able to potentially abuse once we get a shell for things like privilege escalation. So this has been fixed now, but the pre-auth config also used to contain a URL for the pack file. And this is something that we'd commonly see on red teams. So, um, they usually look something like this one here with lots of misconfigurations in, so it might be things such as like, misconfigured wildcards in domains, expired domains, uh, or abusable services like Azure.


## [06:09]


And what we're looking for here is domains that we can use to bypass inspection and we can use those potentially for C2. So another way that Zscaler can be configured without a, uh, the ZCC client is in this explicit proxy mode. So the idea here is that users traffic is steered to Zscaler's cloud proxies either by explicitly setting a proxy in their browser or via a pack file. And what happens here is the user's HTTP requests get pushed through the Zscaler cloud proxy. Uh, the proxy then checks for a gateway cookie. If no cookie is found, the user's redirected to their IDP to do auth. The user is then sent back through the proxy and then the proxy checks that gateway cookie and strips that from the request and forwards it back to Zscaler . So it might not be a surprise if we can steal the gateway cookie from a user's device, then we can also authenticate to the Zscaler gateway and make our traffic appear as if it's coming from a Zscaler IP.

But what about if we don't have a gateway cookie? Well, unfortunately we can't show you the next slide, because Zscaler asks us to remove it while they fix it. But the good news is, we've got a different bug to show you. A better bug. Uh, so to pick up where you left off on the Zscaler client flow, if a tenant is configured for SAML auth, the client makes a request to the CL star end point. This get request includes the domain parameter, a code challenge which is used for PKC, so this is essentially a client chosen sha256 hash, which the user has the plain text for. And then the server responds with a SAML form, with a SAML request relay state parameter, fairly standard SAML flow. This is then submitted by the client browser and redirected to the IDP. So once the user signed into their identity provider, the SAML response and the relay state are posted back to this SFC SSO end point. This then responds with a 302 redirect, redirecting the client back to the mobile end point test.html end point containing a JWT in the redirected URL CKE parameter.

This JWT can then be used to register a device. And that's summarized as a typical SAML login flow. But let's take a step back and have a look at that last step again. So the client sends back this SAML response to the Zscaler service provider. The server should then decode the SAML response, extract the user details from the SAML assertion and validate that the signature is valid for that assertion. For the one that's been configured by the admin in the portal. All fairly standard. However, what we found is that the signature on the SAML assertion was only checked for presence and it wasn't validated against the IDP's public key. So we could just sign it with any key we wanted or even one with an invalid signature. So this gives us an auth bypass for any Zscaler tenant using SAML for authentication.


## [09:13]


So we reported this to Zscaler on the 18th of July and they were very quick, fixed it within about four hours. On Tuesday the 22nd of July, we tried the POC again and it worked. So we reported this regression to Zscaler and they confirmed that they had to roll back the patch due to a more critical issue that was introduced. So this has now been fixed and there's a CB so it's a CB 2025 54982. And it's fully resolved, doesn't require any interaction by Zscaler clients. So we're going to show a demo of this now. So what we're going to do is we're going to run our script which is going to forge the SAML assertion. And this contains now, we've got our JWT back and we've got our code challenge which we need to redeem the JWT. And then we're going to use our Pyscaler tool which is just going to take that JWT and then register a fake machine in Zscaler. So you can see now in the admin portal this is our machine that we've registered using that JWT and you can see that the hardware ID that we chose to register with them.

So let's take a look at that wish list again. We can tick off off bypass Zscaler. So moving on. Another way that Zscaler can be configured is with this authentication mode called device token authentication. So this isn't enabled by default but it enables admins to allow a token to be distributed to machines or mobile devices that use, that can use Zscaler as the identity provider. This is documented in Zscaler's docs. So on Windows you simply specify the token and user as MSI arguments during install. But the problem here is that it only validates that the token is valid for that tenant. There's no association with a specific user. So if an attacker or a malicious user is able to extract that token from an endpoint, they can then register as a machine with any user name. Or simply by changing the user name on an existing device, they can potentially bypass steering policies by simply changing the user name and registering as anyone they want.

So where is the device token stored? So when installing with the MSI arguments, this actually ends up in the registry. So there's a device token that's stored in the registry in clear text and a user name parameter. For mobile devices, this is typically deployed by an MDM config. So we're going to show a demo now of device token extraction and replay. So the user, this is on the victim's machine, so we're going to extract that device token from the registry.


## [12:21]


And then on our attacker VM, we're now going to import that token. So we're going to pause that scaler. We've then imported those tokens and user name. Then we'll just kill that scaler so it picks up that new token. So you see now, we're logged in as user 1 with just the token. And if we try and access the internet, we can't get to the internet because this user doesn't have internet access. So let's try a different user. So again, we suspend it and we'll just go in and change the user name in the registry. So we'll try admin 1, they've probably got better access. Registers as admin 1. You can see now we've got the network admin policy. And we can access the internet. So move on to Netscape. We came across this on a red team engagement and it seemed quite architecturally similar to Zscaler. There's a couple of specific bits of terminology to talk about here. So we've got an org key. And that's assigned to your tenant.

It's immutable. So it's always going to be the same one. And we've got a user key which is assigned to the user. Also immutable. So always going to be the same for that user. Not revocable. Recon, again, very similar to Zscaler. The tenant name's sent. And if the tenant's configured to hand off to another identity provider, a SAML request is returned. And that contains the org key. So, you know, we've already kind of leaked that at that point. If they're not configured to use SSA, then there's a high chance that their kind of customer admin portal bit is. So if you go to like customer.goscope.com, there's the same kind of SAML request flow. And again, it leaks the org key. So I think we've established at this point that the org key isn't really a secret value. Recon, again, very similar to Zscaler. You can use this org key to pull parts of the tenant configuration or the pack file. And this is before you supply the password.

There's actually more information that's leaked to Netscape than there is through Zscaler. And you can see some of the end points there. And you can kind of get the steering config and everything before you've even supplied credits.


## [15:22]


We're going to go through the flow of a publicly disclosed authentication bypass in Netscape to set the scene for kind of what comes afterwards. So here we start with our first request, exchanging our knowledge of the tenant name for a SAML request. And remember that SAML request contains our org key. As is convention, the next step sends the SAML request to the identity provider. And the SAML response is received. So it's all fairly normal so far. That SAML response is exchanged for JWT. Again, all fairly standard. Then the org key and the email address are exchanged for a user key. And as we said, that user key is just a long immutable string that's tied to the user. Uh, but hang on. This request has got no authentication at all. We can just see that we're supplying the org key that we have already leaked, uh, and an email address. So in actual fact, we can skip that whole SAML IDP stage and go straight to pretending that we're whoever we want, providing we know the org key and the valid email address.

So we're Mclover. The authentication flow then continues by exchanging the org key and that user key for a configuration . And finally ends with exchanging that same information for a certificate that's used to set up the tunnel. And remember, this is for Netscape's private access solution, which in the scenarios we came across was, uh, equivalent to VPN access to the target network. So we got our exploit working, exploited the bug on the red team, network access, um, report the issue to Netscape, point out that it's already been reported, fixed, and even had a CVE advisory. Uh, so firstly, big props to Sander DeWitt for being the original discoverer of this issue. Um, we're a bit confused because if it had been fixed and had a CVE, why were we still exploiting this a year on? Um, I suppose the important takeaway is that Netscape's recommendations here are use secure enrollment, uh, and make sure you use, sorry, make sure you use device compliance, um, so that red devices can't connect.

So we'll just talk through the fix for that quickly. So, uh, according to Netscape's advisory, this issue was first reported to Netscape, um, by the original discoverer on the 18th of April, 2024. Uh, they released an advisory on August the 26th, 2024, and assigned CVE 2024 7401. Uh, so this describes a new enrollment method called secure enrollment, and this was first introduced in release 11610. So interestingly in this advisory that they released, it also states that the issue was exploited in the wild by bug bounty hunters. Uh, so despite a CVE and advisory, we're still running this mode in, um, February 2025 when they made secure enrollment the default. Um, and even now in August 2025, we're still finding organizations who don't have secure enrollment enabled.


## [18:30]


So what about secure enrollment? How does that work? So this uses a secure enrollment token, and this has to be deployed via MDM to user's devices before registration. Uh, as a side note, we're not really sure why they didn't go for just validating the JWT that you could get in, in the offload, but we'll go through it now anyway. So we can see they've added an authorization header here, and this authorization header contains a JWT, and we're still specifying an org key in the URL, but we've now got this signed JWT in the authorization header. So how is this JWT made? It's signed with the secure enrollment token, and it contains the UPN and the user key, uh, along with the org key and, um, in the JWT as claims, uh, which you can see on the right here. As mentioned, this JWT is signed using secure enrollment token with HS256, but there's also an org key in the URL. So which one will the server use?

The one in the JWT or the one in the URL? There's only one way to find out. That's right, get parameter wins. The server only checked the JWT was signed correctly for the org key within the JWT itself, but it actually uses the org key in the URL when it enrolls into the tenant. So if we supply a valid JWT for any Netscape tenant, and then we can tamper with the org key in the URL and begin secure enrollment process for any other tenant. This negates the need to know the secure enrollment token for the target org, as long as you have an enrollment token for an entirely different org. And we can specify the target user in the signed JWT. We even found leaked enrollment tokens online, so you don't necessarily need to be a Netscape customer to exploit this. So what about the fix? We reported this cross-tenant bug to Netscape on the 14th of March, 2025. They released a server-side fix in version 126 on May the 12th.

This fix now checks that the org key in the URL matches the one in the JWT. Unfortunately there's no CV for this vulnerability, and despite Netscape being a CNA, they don't issue CVs for server-side fixes. So we're going to show a demo of the cross-tenant exploit now. So the attacker is going to run a script and they're going to provide the target tenant details, the EPN for the target tenant, and the org key for the target tenant, which remember can be leaked pre-auth. They're going to provide the org key and the secure enrollment token for the source org, and then this gets back a cert and a branding file. Then on the attacker's VM, they can then just import this, the branding file and the cert. So now we just restart Netscape.


## [21:34]


And now we have MPA access. So that's Netscape auth bypass completed. So we've seen how secure enrollment can be bypassed, but how should it work? As Rich said, it's usually kind of rolled out via MDM, like an in-tuned install argument or SCCM or something. The token's then stored in the registry in this location, and it's encrypted using DPAPI. But it's encrypted using flag 4, which equates to local machine, which in fact means that any user on the machine can decrypt it. It does have a little bit of entropy, and that's hard-coded as this is global entropy string, and yeah, that really is the entropy. We'll just show a quick demo just to show you that, yeah, you can actually extract that string as a low-pref user, and what you end up with is the decrypted enrollment key. That's what an enrollment key looks like. So what does all this mean? If you're an XSCAPE customer and you're still using IDP mode, there's still a complete auth bypass for enrollment.

In secure enrollment mode, this has been fixed, but if your secure enrollment token is leaked, for example, from someone compromising your in-tuned environment and getting the install arguments, then you can still auth as any user and bypass string restrictions, and it's still possible if you've got an auth key, which is very easy to obtain to, to do loads of recon on a target environment. So we covered, covered the auth bypasses. What if you've already got a shell? If you're up against EDR, we might want to spend as little time as possible on a monitored endpoint, and actually a good option is to steal the ZTNA config, move it to our machine, and then we can access the internal network from a non -monitored endpoint. So both Zscaler and Netscape can store their configs as DPAPI encrypted blobs. Only Zscaler actually does this by default. But in theory, zero trust should stop you from stealing the config.

The posture check should fail. The hardware ID should mismatch. But as we'll show, all these checks can be spoofed. So we'll have a quick primer on the Zscaler config. It's stored in a file, and it's encrypted, well, it's XORed with a fixed key after some kind of convoluted XOR shuffle.


## [24:34]


But to dump the config, you do require a system , and that means we need at least admin privileges to read it. Luckily, we've got two privesks in Zscaler as well, one of which we reported in 2023 and still isn't fixed. We can actually do, like, a whole other talk on the privesks, but we don't have time to, like, cover all the technical details in this bit. But we do have some demos to show you in a second after we've explained how they kind of roughly work. But if we go back to our table here, we can tick off config theft. So about these privesks, all of the ZTNA products we looked at shared a very similar architecture. They've got a low-privileged system tray and a high-privileged service or broker. And those two kind of communicate via IPC. That might be TCP socket and then pipe, RPC, but they all have one thing in common. They failed to properly validate the calling process. There's some attempts to do this by checking signing or path-based checks, but you can bypass that by injecting it into a relevant process.

And there's also some kind of tamper proof stuff, but we bypass that, too. But just because you can call IPC methods, that doesn't automatically give you a system shell. But luckily for us, every product we looked at had a vulnerable IPC method that we could exploit to run code or system, whether that was directly via , like, process starting or via file writing. So I'll show a couple of quick demos. Here's our Netscape LP. You can see the attack is using a custom IPC client. Injects a TCP proxy into a legitimate process to bypass the path signature check. And then after this, the client actually enrolls Nacho VPN style, if you've seen that, into a malicious server, and it gets delivered an MSI update, and that MSI update gets run as system, and ping, we've got a system shell. So next up we've got Zscaler. This is version 4.7, which I think is the latest version. Again, using process injection to bypass the signer check and then trigger a vulnerable IPC method to load some malicious code.

And then once we've got our system shell, we can dump the user's config, as we previously discussed. And then finally, we use a script to dump the various source values needed to spoof the hardware ID, which will be important later on when we discuss, um, hardware ID spoofing. So back to the wish list. We can tick off, and this is starting to look quite scary now, but let's carry on. Cool.


## [27:35]


So you've stolen the config and even rolled, now what? So we mentioned at the start of the talk that zero trust involves verification of the device as well as the user. Uh, and one of the ways that this is achieved is through processor checks. Uh, so these have been around for decades as, in SSL VPNs as well, and they're really no different in, um, in the ZTNA space. Uh, so the specific checks that are carried out vary vendor to vendor, but, um, they typically involve the ones we've listed here. Uh, in the ZTNA products that we assessed, the processor checks were retrieved over HTTP from the ZTNA server before being processed locally via the ZTNA client, and then the results were returned back to the, over HTTP to the server. So Netscape bypassing these checks was really simple, we just had to, uh, put an intercepting proxy between the client and the server and change status equals false to status equals true, and we could bypass the processor checks.

Whilst this was mildly amusing, in practice, having an intercepting proxy between the client and the server is impractical due to things like DTLS and mutual TLS. Uh, so we took a different approach here, and instead of, we injected a DLL into the client and then used API hooking to spoof the checks at a Windows API level. So another piece of information that we had to fake was the hardware ID. The hardware ID is essentially a hash of information that's derived from various components in the operating system and hardware, um, and then this is sent during registration and later on during keep live or device assessment requests. Uh, it's supposed to be unique per user and per machine, um, and if the hardware ID mismatches, the server might refuse the connection or class it as a new device and force re-registration. Uh, in reality, all of the values that make up a hardware ID can be spoofed and we can generate the correct expected hardware ID.

Uh, in the game cheat world, this would be called a spoofer and it's really no different for ZTNA. Uh, so we quickly realized this would make a great generic tool for tricking software that evaluated these various operating system components to, to trick these compliance checks. Uh, so we made this configurable D, DLL that could be injected into the ZTNA process and allow us to hook these API calls. Uh, for hooking the Windows API methods, we mainly use MinHook because it's a simple library and it's widely used. Um, for DLL injection, we mainly use the app, app init DLLs, um, method for DLL injection, but we also have the option to use a sign driver. So for most checks, a simple Windows API hook was all that was needed. Uh, however, to spoof some other checks, things that are slightly harder. Um, so one of the things that normally makes up the, the, uh, hardware ID check is CPU ID. So they use this as a way to get a unique fingerprint of the hardware.

And the way this works is they'll execute a CPU ID instruction to extract the processor info and the feature bits, um, by saying EAX to one and then executing the CPU ID instruction. Uh, so we can't hook this because it's an instruction. It's not a, um, a method. Uh, so instead what we do is we use a page guard hook.


## [30:35]


So the way this works is we search through the, the, the memory and looking for the CPU ID instructions. And then we find the, the page that they're in. We set the page as page guard. And then when that instruction gets executed, it triggers an exception, which we can catch with a custom exception handler. And then we can swap the, the register values out just after the CPU ID instruction gets executed. The downside to this is it's slightly slower than an inline hook. Uh, another option would be to use a custom hypervisor to spoof this, but in reality, we didn't need to do this. So we then created this small GUI application to control the hook settings, allow us to spoof all these various values dynamically. Um, and this allows us to import the config and it'll automatically adjust the spoofing settings to the posture profile inside the config. Um, although some vendors offer the ability to perform cryptographically verified compliance checks or server side checks, they're not turned on by default.

And some vendors don't actually support this feature at all, meaning that compliance checks can always be bypassed in this manner. Um, yep. Uh, also we got a side note here that, um, we called it red scaler, but it makes more sense in British English than American English. Otherwise, I guess we would have called it rescaler. Uh, we're going to do a demo of this now. So this picks up where we left off. So we, we privest, we stole the config and this is what the config looks like in plain text. So you can see it contains all the posture checks. So now we're going to import that config into our red scaler GUI. This is going to re-encrypt the config and put it in the right place. You can see here, it's picked up some of the posture checks and asked if we want to apply those automatically. Then it restarts red scaler and we're logged in. But we can't get to the internet. Oh, it's okay. We forgot to spoof BitLocker.

So we'll just spoof that. Just to show you the posture checks are working. You sunk my battleship. That's a full house for all the products we looked at. Bar the, uh, checkpoint side quest. So let's have a couple of minutes on this privacy thing. So due to the way these products work, they have to install , uh, a root CA in order to inspect less traffic and provide you some of the security benefits that, that they offer. Um, I quite long lived here, like 2065, it's quite a long time for a set.


## [33:40]


Um, the way these are pushed down typically either with the policies or they're installed with the client. It's not really particularly difficult, uh, different to a traditional VPN appliance or, or a web proxy. Um, but the difference here is that the zero trust engine is, is very much an unknown. Your traffic enters there, it's decrypted and processed , that leaves again. You're trusting that it's routed correctly, um, that nothing else happens. Um, and we're sure this hasn't happened, but the compromise, a ZTN provider would allow some additional processing to be performed on the deep decrypted traffic. Um, and you would never know. So we'll talk through a couple of, uh, defense and mitigations here. So the most important one is if you are an Xcode customer and you're not using secure enrollment, please enable it now. Um, also ensure that clients are always updated, even if there's no CV published.

Just because there's no CV doesn't mean there's no bugs, so make sure you're always up to date. Enable cryptographically secure checks or server side validated checks where they're supported. Um, as far as we know, there's nothing for Netscape, but, um, ZTN do support this since version 4.4. Uh, we've also shared some general hardening tips here and some mitigations to help limit the impact of config theft, for example, requiring re-authentication with a, a small re-authentication period. And to minimize the attack surface for privilege escalation, disable client side access to features such as debugging and verbose logging. Um, and finally, if you do use app based or domain based steering bypasses, ensure that these are appropriate for, and narrowly scoped, and they're reviewed on a regular basis. To detect malicious behavior, you can look at vendors provided logs from newly registered devices, devices that perhaps quickly switch from passing failures and fa- passing posture checks and failing posture checks, and make sure that you're ingesting these logs into your scene.

You might also see devices coming from unexpected locations or, um, from unexpected operating systems, so for example, if a user normally uses a Mac, and then suddenly enrolls a Windows device, uh, you can also use EDR to detect activities on end points, such as reading sensitive registry keys or files, uh, and some of the techniques we've discussed require process injection, for example, bypassing those signature checks. Uh, equally, some vulnerabilities might cause a ZTNA process to spawn an unexpected child process, so using, uh, EDR to detect on unexpected child processes can be useful, too. So, there's a couple of techniques that are good for detecting token and config theft. Uh, Google have this brilliant blog that, uh, explains how to configure auditing from deep, for deep happy events. Uh, this is actually aimed at detecting browser cookie theft, but also applies here. So, if the data is, uh, flagged as auto, auditable during encryption, then the, the 4693 event will be raised during decryption, or when an attacker attempts to decrypt the value on the, on the machine.

But, unfortunately, none of the vendors actually flag the data as auditable when it's created, uh, so we can't use this 4693 event type.


## [36:45]


However, we can use the very, very memorably named Microsoft Windows Crypto Deep Happy Log if we turn on debug locking. And this is also detailed in the, in the Google blog. And then this gives us a new event, event ID 16385, and we can use this to correlate the data description and the caller process ID to identify any potential misuse. Uh, so in the case of Zscaler, you can see here, uh, when we installed Zscaler config earlier, uh, you can see the data description is client configuration, uh, which is the description that's set by Zscaler during encryption. Uh, another option for audit, auditing access to sensitive registry keys and config files is to configure SACLs, uh, which will raise, uh, another event in the event security log, so this is, uh, event ID 4663. Uh, and you can see here the event that was raised when we exported the secure token from, um, from the registry for Netscape.

So in conclusion, well it turns out there are no magic said TNA beans. We've got the same old bug classes reimagined for a new technology stack. Rather than zero trust, we're actually putting a lot of trust into these vendors and to process our data securely. And we expect them to clearly and effectively communicate when they do fix vulnerabilities. Even if those bugs are server side, so clients can make informed decisions regarding risk profile. When the bugs exist only in one place though, at least patching them is efficient. But even in that case, if the patch still requires you to change a setting, we think that this could be communicated more effectively. Vendors are still putting far too much trust into client side components as we saw with authentication bypass and posture profile bypasses, which doesn't really fit the zero trust principle. In summary, we found that in practice, rather than being never trust, always verify, we found it was more always trust, never verify.

So thanks for watching our talk. We hope you enjoyed it. We've got a link here to our blog. We'll be publishing write ups over the next few weeks and days. And you can come up and speak to us and we'll take some questions.
