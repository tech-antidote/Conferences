---
title: "Orion - fuzzing workflow automation"
speakers: ["Max Bazalii", "Marius Fleischer"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - Orion - fuzzing workflow automation - Max Bazalii, Marius Fleischer.eng.txt"
sha256: "d309449a23ae779b254d813623b8acd4b6e69ab3fac06eb6275d16aabae65744"
duration_seconds: 2654
words: 7219
text_chars: 39921
redacted_secrets: 0
converted_at: "2026-08-12T06:24:14Z"
---

# Orion - fuzzing workflow automation

**Speakers:** Max Bazalii, Marius Fleischer  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (44 min, 7,219 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


All right. Hi, everyone. Welcome to DEF CON. Today I'll talk about Orion, a system we built to automate the entire fuzzing workflow, from picking up what to fuzz, to generate harnesses, seeds, even creating fixes. This work is a collaboration between me and Marius. We both spent many years creating authentic security tooling, so Orion is our way to make fuzzing smarter, faster, and frankly, less painful. All right. Let's talk who we are. I'm Max Bazali, I lead an authentic security team at NVIDIA DriveOS. My main focus right now is discovery of vulnerabilities, using AI tools, formal methods, and general security research. Before NVIDIA, I was doing a lot of research on Apple operating systems, creating jailbreaks for iOS and watchOS. Actually, I present very first Apple watch jailbreak here at DEF CON 25, many years ago. During the time, we even discovered first Pegasus spyware on iOS and published the white paper about tried and exploit chain using that attack.

Let me hand it off to Marius. I'm Marius Fleischer, I'm a security engineer with NVIDIA's authentic security team for DriveOS. I primarily focus on applying AI to security challenges. Before NVIDIA, I was at UC Santa Barbara, working at the security lab with Giovanni Vigna and Chris Krugel. I was primarily looking at how we can do automated vulnerability discovery for operating systems. Cool. So, most of us spend a lot of time, probably more than we wanted, on creating harnesses, managing fuzz and infra, and do all this, like, glue around fuzzing. So, that's why we created Orion, to make, like, take the painful parts out of fuzzing and automate them. Let's actually see what exactly we are doing. And for that, we need to understand the core loop behind the fuzzing. That's how all the modern feedback doing tools like IFL or the fuzzer works. It all starts with a, like, seed corpus, initial inputs, the fuzzing tool takes it, mutates it, run to the target through the test case.

All the test cases executed through the harness, that's, like, a very important step. And if, like, during the run time, we basically can collect the code coverage to understand what is going on with our target. If our inputs, you know, like, explore the new path, hit the new branch, that is great. It gets added to the seed corpus, and basically that's the way how the fuzzing tool can understand where to dig deeper over time. If our inputs find a crash, that's great. We potentially found a bug. Most of the bugs currently detected by ASAN UBISOFT, great sanitizers detecting stuff at the run time. So, what's the problem with the loop? Basically it's hard to make it work properly. A lot of moving parts. We need right targets, we need the solid harness, we need the good seeds, and basically a good fuzzing tool.


## [03:05]


If anything goes wrong, basically our fuzzing campaign most likely will fail. So, that's where Orion comes in. We take the, like, time-consuming parts out of this loop and automate it one by one. Let's talk about LMs a bit . I hear they're very, very popular right now, and surprisingly we use LMs at Orion. Let's talk a bit why we think LMs are a good fit for this kind of task. Actually, LMs, like, we know neural networks, training huge corporate text and code, that's all good, using, like, transformer architecture under the hood, which unlock really nice capabilities, like, reasoning through the code, generating code, spotting some risky patterns. That's why we think it's a great fit for security automation like fuzzing. Speaking of the risky patterns, LMs can spot bugs, they can occasionally even introduce the new ones. So, that's why we should never trust them, never trust them blindly, and always run through the validation loops, and we will talk in a bit how exactly that works.

Okay, just a few words about agents. Basically, this is how LMs can evolve into something more capable than just, like, passive tools, we ask, they respond. With agents, we can give LMs a task, we can give it access to tools, and a way to, basically, understand what's going on, refine and correct if needed. Very, very similar to what humans would do. What does it mean in practice? We need to generate, let's say, fuzzing harness, so the agent can, you know, first scan for any dependencies, generate the code, try to compile the code. If compilation fails, okay, let's make another attempt. So, that's what we call the agentic loop, it's like act, observe, and refine. Again, very similar to what the real humans would do. This loop is important, this agentic loop is important because that's a way how we can trust these agents and basically make this automation possible in the first place. Cool.

We're going back to our fuzzing loop to understand, like, what exactly we're automating. The very first question with fuzzing to understand what we want to fuzz, right? And that may be tricky because in the large code bases, like we got in Drive IS, maybe millions of lines of code, thousands of functions, probably a lot of them are really interesting from security perspective. What we really care is, like, high risky interfaces, code that's parsing, untrusted user input, stuff like that. That's usually the very manual work to identify them, there is no easy way, like we need to do some threat modeling, look into the code, look in architecture, like, all of this. That's why we create an agent that can help us do this work, analyze the code, understand how this code can be triggered, run the function and basically say, hey, this is what makes sense to fuzz.


## [06:12]


Next one, even if we know what to fuzz, we need to trigger the code that we want to fuzz, basically we need to create the harness, this glue between fuzzing tool and the target. If our harness is bad, like, incomplete, you know, we miss some memory location, it will just crash or, like, do nothing and waste a lot of fuzzing cycles. So, if I have a bad harness, we'll find zero bugs. That's why we basically create another agent in Orion that can analyze the code for us, understand what kind of dependence is needed, do the proper setup, proper, like, teardown if needed, memory location, basically creating all this context for harness. And we will look really soon how exactly that works. Cool. What next? We know what to fuzz, we know how to fuzz. We not really go far without initials inputs, because we remember that's the very first thing that fuzzing tool takes and mutates. If our inputs are bad, it will be rejected by a target or, even worse, it can just crash the target.

So, we create another agent that can help us analyze the code, understand how the inputs are handled, what are the checks, are there any magic bytes, are there any string constants that we need to take care. And basically create an input that makes sense from target point of view. So, we not waste time on fuzzing the wrong thing. All right. Let's say we collect it all together, we run, and our fuzzing finds some crashes. So, what's next? Are these crashes the real bugs? Maybe. We don't know. We don't know until we can reproduce them. So, if we can't reproduce the bug, we can't triage it, we cannot fix it. And usually, the process of reproducing it may take time. So, we need to take inputs, like run it in the same environment where the crash happens, like many, many steps. We create an agent that helps us with that and basically look into what happened, why it happened, what sanitizers trigger a crash, and basically create some minimal reproducer.

So, some small program that we can run over and over to understand if this bug still exists or not. And finally, the final challenge that we are automating after we detect the bug and reproduce it. So, we want to fix it, ideally. And fixing, it's maybe a problem, because where the crash happens, maybe not the root cause, where the actual bug is at first. Second, our fix, like, it can introduce the new security bugs, like, more than we are fixing. So, we really need to understand why this happened, what the root cause. It's usually the manual work. So, we create an agent that first rely on the reproducer from the previous step, creates us a minimal patch, verify this patch using the reproducer, and if everything is cool, we send it for a human to review. So, we still, we don't want LLMs to, you know, push the code uncontrollably into our release branches, so we still want to review these kind of things.


## [09:20]


All right, this is how everything comes together, like, it's the high-level design of Orion. We create it to automate the entire fuzzing workflow, from identifying what to fuzz, to creating patches if there are any crashes, right? It's separated into different phases, like, each phase is dedicated for specific agents that are doing this kind of work. So, we start with one agent, the output, the first agent is input to another one, and so on, so on. Think of it like an assembly line, but for fuzzing. We start with interface analysis agents, like, analyze the code, say, OK, this is what makes sense to fuzz. Switch to the harness. Harness generation agent creates the harnesses that we can compile for all the targets we selected. Next, seed generation agent, then we just run our fuzzing campaign. Depending on the tool we use, depending on, like, what we want to fuzz, it may take some time. If there are any crashes or findings, the agents will pick it up, basically create the reproducers, create the patches if possible, and basically give it for humans to review.

So, with this approach, basically we can run all the agents together, we can run all the agents one by one. If anything fails in between, let's say we create a harness, we can't compile it, that's OK. The agent will make another attempt with updated information from the compiler, for example. We use it to scale fuzzing across NVIDIA, and since we deployed this project, we found more bugs compared with, like, manual fuzzing campaigns. All right. I want to talk a bit about feedback loops and oracles. Basically, that would make this agent reliable. Because we don't trust LLMs, they will hallucinate, do all this kind of, like, interesting stuff. So, but what we can do, we can take a tool that we can trust, like some deterministic tools, and use them to validate the output. For example, we generate the code, we run it through the compiler. We create a harness , we try to execute the harness and see if it crashes, if it's really generating any code coverage for us.

This kind of stuff. So, these agents, they are part of this loop, we call it validation loop for every agent. And until the agent can pass all the validation checks, it's not moving forward. That would make it reliable and basically all this automation possible. All right. A few words about infrastructure. Because it's all this stuff not going to be possible without proper infrastructure. Because by default, LLMs have no idea how our code is structured, what interface are internal, what are external, where the data types are defined, all of it. Without that, agents will be guessing, hallucinating, like, all kind of non-fun stuff. So, we have to create infrastructure.


## [12:20]


Basically, we have to parse the code, we have to understand dependencies between functions, we have to create the call graphs, basically create some minimal context, basically, like, where all the data types or dependencies are defined, that we can pass an agent to do the proper work for analysis or code generation. All right. Next, we will talk about benchmarking. Benchmarking is another very important part of building a system like Orion. It really helps you understand how your individual improvements improve the overall performance of Orion. We chose to have a very simple, reliable benchmark suite by using human annotated ground-truth data as the base. And then we can use a few different ways, a few different aspects of our system's output and analyze that based on that data. The simplest way, of course, is to just directly compare to your ground-truth data. That may work especially well for integers.

In some other cases, when you're ranking target functions, you may care more about the relative ranking of these individual functions more than the actual concrete score. So you may look for pairs and how they relate to each other. If none of this works, you can always fall back to an LLM judge where you just have an LLM review the system's output. In our case, because we have ground-truth data, we can get a very reliable judge in that way. For our targets, we choose, of course, the NVIDIA GPIO driver for QNX, which is a user-space driver. Because of that, we choose two additional open-source user-space applications listed on our slides. So let's have a look at our actual agents now. The first one is about finding the targets we want to fuzz. We really want to find functions that are likely to have bugs that a fuzzer can find. So that means we define a set of metrics that you can individually evaluate against the functions.

And then you can combine those scores for the final ranking. One example of such a metric would be dangerous expressions, so we're looking for pointer dereferences or pointer arithmetic or similar things. The output of that agent might look something like this. We have individual scores for all the metrics, and we combine them and rank on the final combined score to have a holistic picture. Once we have the functions that we actually want to fuzz, we can go on to the next phase, the harness generation. The first step in the harness generation is what we call the dependency analysis. So in this step, we have an agent that goes to the code base and tries to understand which functions depend on which others, and also how do you clean up after calling your interface. So you may have to deallocate some objects. The output that we get here is essentially a list of functions to call before our target function and a list of functions after, and also the reason why we have to do that.

With this information, we can now actually go and create a harness. So this harness generation agent has to create this piece of code that sits between the fuzzer and the actual target interface.


## [15:27]


So that means it has to be compatible with the fuzzer, has to be able to understand those inputs, transform them into whatever structure the target understands, and also set up and tear down the target interface correctly so that we can repeat this over and over again. In our case, we're fuzzing with lip fuzzer and AFL, so we use the harness format that is compatible with that. It's also important to have all the right header files included, otherwise your harness won't compile, and we do need to call the target function in the end. Now, because LLMs may or may not produce correct code, we do need to do some verification here. Of course, we can just give it to a compiler, ask the compiler is this code correct, and if it's not, we'll give it back to the LLM to fix that. We also have another LLM with some guidelines to review the harness to make sure there are some more semantic components that make sense.

If anything goes wrong here, like on the right side here, we would just take the error and take the harness that produced the error, give it back to the harness generation agent, and ask it to fix that problem. If not, we're happy and we can move on. Now, next step is the seed generation. So now, we want to find those inputs that are good in the sense that they will produce good coverage, and they will also hit a few of the edge cases, maybe help the fuzzer overcome some of the challenging aspects like checksums or matching values that you need to hit. So we have an agent that goes through a code base, looks for all these things, and then tries to output a good seed corpus of multiple inputs that together cover all these cases. For a function that has, say, three arguments that might look something like this, for each individual argument, we will have a part of the seed that corresponds to that, and because LLMs are not great at outputting binary data, we either get a structured output from it, or we can also have it generate, for example, a batch script that if you execute, actually gives you the binary seed that we can provide to the fuzzer.

At this point, we have all the artifacts that we need, the harness, the seeds, and we can start fuzzing. So at NVIDIA, we have a cloud infrastructure that you can just provide these artifacts to, and then this infrastructure will go wait for a runner to become available, set up that runner correctly so that it's the correct fuzzing environment, it will run the fuzzing, monitor what's going on. Once we have results, it will collect that and provide it back to Orion. The purpose here is kind of twofold. One, of course, it executes our fuzzer, but it also kind of works like a second feedback loop. If there's anything unexpected during a fuzzing campaign, say, less coverage than we expected, or some crashes that shouldn't have happened, we can give that back to Orion, to the harness or seed generation agent, and ask for an approval again. Of course, we may also find bugs. In that case, we would then go and start our patch or bug analysis pipeline.


## [18:30]


The first step here is to analyze what went wrong. So we're looking for the root cause with this first agent. It will essentially go look at stack traces, look at sanitizer logs, also have a look at the source code, and try to figure out what went wrong. Once it has an idea, it will also generate a minimal reproducer to prove that it was right, which is, again, another feedback loop. Once we have that reproducer, we have something that we can actually give to a human that makes it easier to understand what went wrong, and we also have what we need for the next phase as a reproducer, as a feedback loop. The next phase being the patch generation. So here now, we want to generate a patch that's meaningful and that actually goes to fix the root cause, not just block symptoms or maybe even just removes the vulnerable function. It should apply cleanly to the code base and it should compile. If it matches the coding style of the whole code base, that's also great.

So the output is essentially a diff file that you can apply. We will, of course, give that to a compiler as a feedback loop. We will also use the reproducer that we just generated and have that as a second way of verifying that we actually patched the bug that we wanted. And as Max previously already mentioned, we will not just push this to main, we will actually give this to a developer to make sure that this is correct. At this point, you might ask, does this actually work? And to give you an idea of whether or not this works, we brought some of our benchmarking suite data. So for a little bit of context, we started the development of these agents with GPT 4.0, but we have since switched to 4.1 just because it's better. And all the data that you will see on the following slides and on this one will all be from GPT 4.1. The LLM judges that we have as part of our benchmarking suite, they use open source models.

So Lama, Nemotron, these kinds of things. In terms of the interface analysis, overall we have seven different metrics that we evaluate. And if you average across these metrics and across all the three different targets that we have in our benchmarking suite, you will see that in 95 percent of the cases, we actually evaluate the metrics correctly, which is pretty cool. The two metrics that you can see here on the slides are actually the two worst metrics out of the seven that we have. All the other ones are better than what you see here. The metric we seem to be struggling the most with is dangerous expressions. So what we're looking for here is essentially, are there pointer references? Is there pointer arithmetic? Maybe there's also some bit operations. As far as we know, the problem here is that some of our targets use macros heavily or have some other aspects that make it hard to resolve some of the symbols.

So the LLM doesn't really fully understand the code. And that makes sense, but also makes it very hard for the LLM to figure out what's going on. The other metric on the slide here is about sync functions. So we're looking for functions that are commonly seen in the context of vulnerabilities.


## [21:33]


For example, memcpy for a buffer overflow or things like that. And that seems to be doable for the LLM. In terms of harness generation, this task seems to be a little harder. The performance is a little worse, but still pretty good overall. We have nine different metrics that we evaluate here in our benchmark. The two here, I think, might be the most interesting for you. The one in the middle is about whether or not our generated harness passed all the verification steps. So that's the compiler, that's also our other LLM that verifies if everything is correct. And we have a pretty good performance here, I would say. The metric on the right is the one that I find actually more interesting and more impressive, even though the number is lower. This metric is about finding out whether or not in the harness there's all the functions that we need to call. So all the dependencies, the actual target function, and also the cleanup after.

The reason why I think that's the more impressive number is because this performance is largely dominated by the dependency agent. And the task of this agent is to go through the code base without any documentation and understand where are the dependencies in this code base. I think that's a pretty hard task, even for a human, and the fact that this agent is doing so well is actually quite impressive. Now I'll pass it back to Max for the time-savings. All right. Thanks, Marius. Basically, let's take a look at the time-savings. We compare how much time we spend doing this work manually compared with the automation. And that's the real numbers from our internal deployments of Orion. We split it in three sections, like interface analysis, harness generation, and patching. For interface analysis, we get a lot of time-savings, because if you remember, that's work that we need to do manually, like look into the code, understand if it makes sense to fuzz or not, if it makes sense how we can fuzz it, and so on.

So with agent doing this work for us, we can save days, sometimes even weeks, of the manual work. Next one is harness generation. This is where we see most of our savings. And the reason for that, doing harness is taking a lot of time. It's hard, right? Because we need to think about input, we need to think about any set-up, tear-down, globals, all this kind of stuff. With agents that can do this work for us, we see most of the savings. Again, let's say we need to create harnesses for a hundred functions. It may take days, it may take weeks, again, depending on the code base. So that's a really, really good help. Finally, the patching. This category is maybe not as impressive as harness gen, and the reason for that, we still want to review patches that LM generated. We don't trust LM that much to uncontrollably push the code. Still, like, it can create us, the reproducers, it can create the patches that pass the validation checks.


## [24:39]


It's a huge, huge help. All right, I know it's a Friday night, guys, so let's take a look at how we run works in practice. So what we will do now, we'll run Orion on the real target. It's a C -lib, it's an open source C package manager. We take the latest version of C-lib and we run Orion on it. And let's take a look. So we split the screen in two parts. Left side is show the current flow of the agents, what's going on. The right side shows the output of an agent. And we start with the target selection. Basically, we scan the code and look like what makes sense to us. It may take some time, because we run each function. Okay, so we've got like 11 functions that make sense to us. And we jump to the next phase, which is seed generation. Basically, creating the seeds for C-lib package install. It takes time, analyze the code, analyze all the input arguments, and we jump into harness. So the reason why we do seeds and harness, we want harness to support our seeds first.

With harness generation, we have to take a look at the dependencies, set up, tear down stuff, globals. So it will take time for an agent to figure it all out. Create us or generate us the code that will pass to the compiler. Basically, that's what happens on the right side. So we see some intermediate code generated by an agent. And let's see if it can be compiled. All right. Again, depending on the size of the code, it may take more time. Well, in our case, the compilation failed, which means the code generated by LLM was not great. But that's okay. The agent will make another attempt to self-correct, basically, and create us a new harness. All right. So essentially, we took the warning that the compiler gave us and give it back to the harness generation agent. It will generate a new harness, and it will then first pass it to the LLM verifier to make sure everything's okay. And only if it passes that verification, we will actually go on and go back to the compiler.

Only if both of those pass, and we can see here, the LLM passed, only if both the verifications pass, we will actually use this harness for the further processing. So we're through the first stage. The LLM thinks this is okay. Now we're waiting for the compiler. That may take a second. And the compiler also thinks this is okay. So now we can actually start the fuzzing. We will use the seeds that we just generated with our harness, and we just run the fuzzer. Now we had very good seeds, so we very quickly found a bug, and we kick off the patch generation.


## [27:46]


So the patch generation now goes through the code base, and we'll start modifying the code base to try and patch the bug, and it will then also invoke the compiler and the small reproducer to see if this is patched. You can see here, it was actually successful. So we did generate a correct bug. It compiles, and the reproducer doesn't trigger anymore. Thank you. All right, let's talk a bit about findings. So we tried to run many open source targets. One of them is C-lib, so we just found a stack overflow. We tried on H3, and we deployed it internally at NVIDIA. So at C-lib we found many security bugs. C-lib was fuzzed for years by OSS fuzz, but they still are missing. We will not share all the details of the bugs because they are not fixed yet. We want to give maintainers a bit more time to fix them. If you use C-lib in the production, and you are concerned about that, sure, let us know, we can talk.

Internally we deployed at NVIDIA drive stack, maybe like 18 months ago, we found 100 new bugs, mostly in C and C++ code, memory corruption, the main source of the bugs. With the help of agent that can help us filter out if it's a real bug or not, of course, understanding if it's a crash or a real bug helps a lot. Anyway, that's for the findings. Let's talk a bit about where the LM can fail. And as we already mentioned, LMs first of all don't know your code base. So they will be guessing without the proper context. If you don't provide all the information, they will hallucinate and do all the other type of stuff. So this infrastructure is the mass that creates at least the minimal context that we pass to an agent for analysis for code generation. The next one, even with the proper context, LMs still can be unreliable. They can hallucinate, invent stuff, do all the other kind of things. For example, very first iteration of our patching agent was just removing the code that crashed it and say, hey, old tester, pass it, like all good.

So to mitigate this kind of stuff, we need to use oracles, like tools that we trust, compilers, fuzzers, sanitizers, any tool that can be used to validate the output of an agent or LLM. Otherwise, it will hallucinate. Finally, sometimes LLMs are not the best answer, right? Like Python on Barscript, if the Python on Barscript can do exactly the same work as the fancy multi-step agent, maybe just use the script, it will be faster and definitely cheaper. Let's talk a bit about the tools. So we hear discussion, are the LLMs going to replace all the tools, like what the future is?


## [30:47]


First of all, we don't think so. I think each side doing what they design it to do, so the tools are great combination with agent, with LLMs, because sometimes they just belong to the models, like models probably not going to do the great work generating call graphs, generating ISTs, where there's some deterministic tool can do it very reliably. But tools can be great oracles. So as I mentioned, if you have a tool that can use for validating the output, definitely use it. If you can use it, if you can do it in the way basically without like any probabilistic guessing that LLMs do. So for example, I really want to believe this is my compiler not hallucinating to me. Finally, tools are great for providing context for an agent. So generating call graphs and generating dependency graphs, all this kind of stuff, it's really helps and really boost agents to do the proper work for analysis or generation.

In terms of benchmarking, some of the lessons that we learned is that, well first of all, LLMs and LLM agents, they're still software. So all the things that we learned about how to benchmark software, that still applies here as well. You want to look for coverage, you want to hit all the edge cases that you can and so on. It's always of course the best if you can verify your agent's output using an actual tool without any LLMs, that's most reliable. But that's not always going to be possible and LLM judges can be very effective in this scenario. You do want to think very closely about the task that you're giving your LLM agent though, because the more simple that task is, the more reliable your agent will be. If you don't do this, you may end up in a situation where you now have to worry about your LLM judge's performance, so you kind of need a benchmark for your benchmark. That's not a good place to be in.

So keep the task simple, maybe have some ground truth data that it can compare against and it'll be fine. In terms of infrastructure, we already talked about the fact that it's very important to have good infrastructure. The reason here is that essentially you need all the right context, but only the right context for your LLM. If you have too much, it's going to be overwhelmed. If something's missing, it will hallucinate. So it's really important to spend a good amount of time on your infrastructure to get it right, and the return on that investment will be great. In that context, be ready to build a lot of wrappers and glue code around existing tools. Their interfaces unfortunately don't always fit exactly what you need, and you will just have to accept that you will need to spend some time on that. Another thing is that agents and LLMs do benefit from many different kinds of context. It can be documentation, but it can also be previous examples of the agent's own performance that were very good.

So if you have some sort of persistent memory where you can store very successful executions of the agent, and you can then use that for future iterations, that can also be very beneficial.


## [33:50]


Cool. Let's talk about open source. I have many questions about what we're going to do with open source, and the short answer is we're working on it. The long answer is it's complicated, because it's really coupled with our NVIDIA infrastructure, like the way how we generate the context, the way how we do fuzzing, the way how we build our stuff. Unfortunately, as of today, there is no drop-in replacement for the context creation. I mean, the Codex and Cloud Code are great tools. They're not 100% replacement yet, so the coupling will take time. Alternatively, we're thinking just release some modules like logic that generate the harnesses or generate patches, so we can just take it and plug it in your own setup. And if it's going to be released, we don't plan any hosted version, which mean if you want to use Orion, you will have to provide your own LLM and fuzzing infrastructure. Well, that's pretty much it.

That's how we built Orion. We use it internally to fuzz our C and C++. And by the targets, we are happy to go deeper into agents, oracles, future plans, so let's open it up for questions. There's a microphone over there where you probably want to line up. Yeah, that's a good call. Yeah, please, please use microphone in the room. Hi, guys. Thanks, Tal. That was really great. Is Orion ready to do anything besides C? I mean, it depends on the fuzzing tool you use. If the fuzzing tool, I mean, we support IFL and LiveFuzzer. If the language you want to fuzz is supported by these two tools, Orion can do it. Can I get this from GitHub or something? Yeah, let's talk. That's basically what we want to announce. If you're interested to contribute, if you want to try it, we'll be happy to connect and chat. Yeah, absolutely. Fantastic. Thanks again. Is there any reason you chose GPT as your foundational model for the agents?

Well, good question. I think both models are good, both clouds and GPT are good. We just see some better results in hardness generation with GPT. But really, they're really, really close. So, I mean, right now the new models get dropped, like GPT 5 and 4.1. We have not updated our benchmarks yet. So, GPT 4.1 was the latest and greatest by the time of when we were recording this demo. So, you did test other models and see if they had less hallucination in the code generation? Correct. In our test cases, both 4.1 and Cloud 4 was really, really good.


## [36:53]


But again, it will depend on Oracle. So, both models will occasionally hallucinate. How good and how trustable your Oracles are really will define the output. Thank you. Hi, really great talk. I'm very happy someone is integrating AI into the fuzzing space. Now, my question, do you think it would be possible to use this kind of a tool on exclusively local hardware? Or is the, for lack of a better term, horsepower need so great that you really need to use the cloud? Yeah, great question. Right now we use it on the cloud models mostly, but we're working on fine-tuning the smaller models that we can open source to so you can run it locally. It's not done yet, work in progress, most likely like the second iteration after we open source Orion. So it should be doable. Hello, great talk. I have two questions on the evaluations. The first one, what acceptance rate did you see on your generated patches, including how many did you actually send to other maintainers and how much do the maintainers actually like revised and were accepted?

Yeah, well, we actually, we run it mostly on our internal targets. So, and we compare, we have the ground truth data. So we know how this bug should be fixed because we run through the manual fixes first. And I think acceptance rates, it's around like 70 something percent. I mean, it's eventually like hallucinate, but as we show, we run it through the validation loop first because it doesn't mean the first patch generated by a model will be good. So we generate the patch, we run the reproducer. If the reproducer still can, you know, record the bug, that's a bad patch. Yeah. And after that, maybe around like the final version, like around 70% were good. When we start, it was maybe like 30%. As I mentioned, models will just remove the code that caused the crash and say, yeah, we fixed that. So definitely like some, some additional work required to detect this kind of things. Yeah, that's a good number.

So what percentage actually passed the validation loop? I don't think I have data on like what's passed and what's not passed because we right now, we only look on the patches that pass at least all the validation loops and how many of them are valid, right?


## [40:00]


Because it's, it may pass the validation loop, but still the fix is like stupid or something like that. So it was around 70% on our internal targets and on the C-lib, at least on the demo, I think it takes like first attempt. Right. So it was like, it was a not very hard bug, but the first attempt for C-lib, it was able to generate a valid patch. Okay, thanks. And for the second question, like you said, you have a seed generation agent. How much performance gain do you see from the seed generation? Like alternatively, I think you can also patch the patch of the harness so that for the fuzzing mode, it can pass the magic value checks. So what's, yeah, how much performance boost did you see? Well, I think we measure in terms of like fuzzing cycles. So it's really good question. I don't think we measure without seed because basically we never measure to run the fuzzing without seed. So we always generate the seeds and if the seeds and we do the small fuzzing runs and we collect some of the code coverage.

So that's the minimal validation steps that the seeds are valid, correct and don't crash the target. But that's a good question. I think we can collect this data and, you know, try to run everything the same without the seeds, probably to waste tons of fuzzing cycles. And then we can, you know, give you the better answer how much time it's saving just on the seed optimization. Yeah, I guess it might be better like to patch the harness so that more running are valid rather than just get the magic value only giving that to the initial seed because the mutation might invalidate some of the like checksum or something. Yeah, that's exactly what we're trying to avoid to make fuzzing tool brute force it over time. We're trying to give it like as much valid input as possible. So that can at least hit many, many branches from the beginning. Cool. Thanks for answering my questions. Sure. Thank you.

Hi, I was wondering if you've done any financial cost benefit analysis on automating fuzzing? Well, I think we can calculate it from the time savings. I mean, I haven't done the financial analysis. We've done only like time and how much time, how faster our fuzzing campaigns now can be. Again, I think that's something that we can collate, like how many engineers need to work on that, you know, manually and then see how much money we save. But as of out of my head, I don't have the data. Thanks, this was really great. I was wondering what your experience with the quality of the generated fuzzing code was.


## [43:03]


Like, can humans maintain it over time and read it when they need to debug it and so forth? You mean the actual harnesses that we generate? Yeah. Yes. So what we do, we provide some initial templates. So we don't want agents, you know, to be very creative in the harnesses. We provide the template for the specific tools. For example, if we know that we're going with LiveFuzzer, we provide the template and LiveFuzzer format and very specific instructions how the harness should look like, which make it like pretty maintainable. Well, occasionally once in a blue moon, agents, of course, can be very creative and add a lot of unneeded code. But we figure out how to detect it. As Marius mentioned, lm judge that can, you know, we're not just passing the compilation checks, but judge look at the generated code and say, like, hmm, it shouldn't really be here, right? And right now we have to pass the judge checks.

So overall, if it's all good, validation pass it, it's pretty maintainable code. Okay, thanks. Thank you. All right, guys, if you have more questions, we'll be around near the speaker corner, room 227. Thank you all and, yeah, enjoy the rest of DEF CON.
