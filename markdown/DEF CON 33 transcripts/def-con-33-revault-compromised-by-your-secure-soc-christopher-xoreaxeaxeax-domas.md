---
title: "ReVault Compromised by your Secure SoC"
speakers: ["Christopher 'xoreaxeaxeax' Domas"]
conference: "DEF CON"
conference_full: "DEF CON 33"
edition: "33"
year: 2025
source_type: "transcript"
source_transcript: "DEF CON 33 - ReVault Compromised by your Secure SoC - Christopher 'xoreaxeaxeax' Domas.eng.txt"
sha256: "7f421e90793935f687b5866f6b9749d01adf7b7693827de5a93c72d6cbd85555"
duration_seconds: 2516
words: 7749
text_chars: 41002
redacted_secrets: 0
converted_at: "2026-08-12T06:24:14Z"
---

# ReVault Compromised by your Secure SoC

**Speakers:** Christopher 'xoreaxeaxeax' Domas  
**Conference:** DEF CON 33  
**Source:** automatic speech-recognition transcript (41 min, 7,749 words). Wording follows the recording and may contain recognition errors; timestamps anchor each section back to the video.


## [00:00]


I'd like to introduce Philippe who is going to be talking to us about secure systems on a chip systems. And I'm really excited for this talk and I hope you are too. Philippe? Thank you. Hi, everyone. So, yeah, so today I'm going to talk about revolt and how you can get compromised by your secure system on chip. So, hi, I'm Philippe. I'm a senior software engineer. I work on Windows systems. And basically what I do is to look for vulnerabilities in third party software. And we find stuff, we report the stuff to the vendors and when everything is fixed, we publish reports and publish the research as I'm doing today. So, that's what it is about. What can you expect from today's presentation? It's a 45 minute talk. I'm going to go over some of the bugs we've reported to Dell and Broadcom like in the beginning of the year. Targeting something called control vaults. And all the bugs sit together, leads to like a full compromise of the system and we'll see like how that works.

So, if you've never seen that before, it's a laptop, right? And here you have a fingerprint reader, an NFC card reader and a smart card reader. And all of those peripherals connect into a control vault. And if you look into the system panel in Windows, you can find this mysterious device called control vault. And you don't really know what it is, but you know, that's the point of this presentation. And if you open the laptop and remove the battery, you can find the control vault board like over here. You can see it's like covered in tape and you have just like a little like flash module that sticks out. And the various peripherals are like connected onto it. So, now you can be like, okay, control vault, what the hell is that, right? So, technically it's control vault 3. It can be found in like 100 models of the laptops. Mostly like the business lines, so like latitude and precisions. And that the board is there.

It's a mix of software, firmware, hardware. You have Windows API, Linux API. You have like a firmware that like it's a real time OS that's running on this ARM chip here. And the chip is like a Broadcom 58202. Cool. And it's meant to be a secure storage of secrets. So, that little like flash module here is encrypted and it contains like the firmware and potential secrets. So, yeah, technically it's called USH for Unified Secure Hub. And the idea is like you connect like the peripherals I was mentioning before. Yeah, it's control vault 3. There is also the newer control vault 3 plus. You find that in some of the Dell Pro machines. It's also vulnerable to like all of my findings. And before that, you had control vault 2 and control vault, you know, like no number. The first control vault was 2010 and from my reversing, it looks like at least some of the vaults that I've reported were also present then.


## [03:07]


And yeah, control vault 3 and 3 plus estimate is in the tens of millions of devices that you can find like in Dell laptops. And there is no documentation. So, we're going to have to do some work here. So, step one, what can you do? Google stuff, right? And this one pops up eventually, which is the Linux implementation of the host APIs for the control vault device. And what is cool about it is that they compiled it with debug symbols. So, you can import the host API in IDA, Ghidra, whatever. And you will get a lot of data structures, enums, status codes. So, with all of that, it's really helpful for reversing because the same data structure that are used on the host are also going to be processed by the firmware itself. And the other one is kind of a weird one. It's a certification for the system on chip for like it's cryptography module. It doesn't give you much information, but kind of hints at the internals a little of what it can do.

And it also gives some information about the values acronyms that are being used. For instance, here, SBI, for instance, stands for secure boot image, which is an extension of the boot ROM and should be trusted. Okay. So, why did I want to target that? You know, okay, it's hundreds of models of Dell laptops, you know, probably millions of devices affected. But really, like the beginning of that project were way more humble. I was looking at that, which is just, you know, process manager to see, you know, what's running on my system. And that one caught my attention. So, I was listing the stuff that have like ASLR enabled. And, you know, in 2025, everything should have ASLR. So, that's address space layout randomization, which is a common security mitigation to make, you know, like exploitation much harder. And it should be enabled everywhere. So, when it's not, that's kind of worrisome. And same like some of the DLL that are being used by that, also not ASLR enabled.

And all of that is in the Broadcom services that talk with control box. So, I got really excited, you know, I started like healing, you know, through my windows. You know, it's like, let's freaking hack the planet, guys, you know. So, I was like really excited. And next step was like less exciting, which was like looking at files. So, extracting the installer and trying to understand what's in there, like what is control box, you know. And there's like so many files. You know, you have a driver, like USB driver, and then you have like all the peripherals of the drivers as well. There's like Windows services, there's like DLLs that nothing use, so like I don't even know why they're there. And then there's a firmware folder like full of files. And there's like a few that kind of like look interesting. So, we have like that famous like SBI that I was mentioning three slides ago. The secure boot image, which is like, you know, part of the boot process.

And that was in clear text. So, you can import it in either, like whatever you want and look at it. And then there's that one called like Citadel, which turns out to be the application firmware.


## [06:11]


So, that's the main stuff you talk with like when in normal operations. But unfortunately, this one is encrypted. So, it's pretty sad that we have more work to do. But that's going to be fun, you know, challenge. Before we go too far, like just to give an idea of the system architecture, here we have the main firmware. That is like the boot ROM. That's the SBI. That's the main application. And on the left side, you know, it talks with all of those hardware peripherals, you know, the flash, the NFC reader, et cetera. Using, you know, like various buses. And on the right side, you have the host. So, like on our case, Windows. And it talks over USB with all stuff. And then inside, in the Windows side, you have like a USB driver that runs in kernel mode. And then user land, you have some control APIs and value services and other stuff that talk with the driver. Okay. So, if we want to talk with that board, what can we do?

The way it works is the USB driver will create like a file device on Windows. And user land, like any regular user on Windows can open that device and send comments to it via IO control commands. And eventually, the driver will send USB packets to the firmware. But because it's mega tedious to do that by hand, you know, they have like a high level DLL that implements many high level functions. Those are called like CV commands. And you have like CV open, CV closed, CV create object, delete object, get object, you know, tons of that. I think there is like 150 different commands. So, it's a huge attack surface. And they need a session handle and you get that with like CV open. And on the other side of that, on the firmware, you have like a handler that's going to receive those commands and dispatch them to the proper implementation. So, it's called CV manager or CV manager SBI. And all of that is clearly like an attack surface you want to mess with, you know.

But before we do that, let's, this is like a quick little example on how to communicate with the firmware like by hand. Kind of. So, it's like a little Python script I wrote. Here, you just use C types to load the API DLL. And then you define the function that you want to talk with. In particular, like what are the arguments and the return value. And for that, you can use the symbols you got from the Linux version of it. And then you just call it. And for instance, the result is that. This 20 lines of code is you will get that. So, with 20 lines of Python, you can talk with these devices in your laptop that does some crazy stuff. So easy. I was going to publish a tool about how to do that better, but I ran out of time. So, for now, you only get that one slide. Sorry about that. Yeah. Now, we know how to talk with it. We want to like mess with it, right? So, a little of that modeling. Back to the model, the system model.


## [09:13]


I see that kind of like four main attack surfaces that I was considering. One is the main firmware stuff, you know. So, the SBI and the application firmware, you know, they have a USB implementation. They receive like this high level CV commands. And then there's more interaction with like all of the services. So, all of that is like super interesting to target. Then, the firmware itself lives in something called like secure boots, which is meant to prevent, you know, unauthorized tampering with the firmware. And for that, you know, you're going to have like some secure firmware update mechanism, some signature checks. And that's something we also want to look at. Then we have all the Windows services here. So, originally I was just like, oh, I want to mess with that driver to maybe get an elevation of privilege on Windows. But, you know, that's one angle. But then you have like all the services, you know, all of that is really interesting.

And finally, something that I'm not going to talk about today, but I wanted to highlight it. There is that side where like all the physical peripherals talk with the firmware as well. And so it's kind of a funny question to ask. Like, could a malicious, you know, smart card reader or fingerprint reader could mess with the firmware? Or could a malicious firmware mess with the fingerprint reader or smart card reader? Those are really exciting questions. But trying to answer them in that project would like 10X the scope of it. And I was like, okay, I don't have time for that. But, you know, it's fun to explore. And just to double down on the secure boot like architecture, like that's kind of like a really high level overview of how that works. You have the boot from that verified SBI's and then the SBI gets some key material from something called a secure code descriptor. And then it will be used to like start the application firmware.

And during a firmware update, all of that gets kind of like, can be modified and get, need to be verified. And we kind of see like, how does that work? And, okay, we want to hack stuff. So let's talk about mitigations to like make it harder for us. So have we seen, the Broadcom services don't come with ASLR. But they come with DEP for data execution prevention. So basically you cannot just like jump on the stack and execute everything. You know, it's not the 90's. And there is like something called stack cookie to make stack overflow exploitation harder. And in that case, like sometimes it presents, sometimes it's not. So it really depends on the complexity of the function. It's kind of already weird because, you know, you should put it everywhere and, you know, but not the case here. And on the firmware side, there's like one really cool feature. It is the, the firmware is executed in place in the flash chip and it is encrypted.

So you can, you know, decrypt and execute the firmware like in one go, which I think is really cool. And then you also have potentially the secure boots between quotes. So, you know, a bit of foreshadowing here. And then there is no ASLR, no stack cookie, meaning that if you can get memory corruption, like a stack overflow or something, it's going to be really easy to exploit stuff.


## [12:17]


DEP is a bit of a question mark. If you try to jump on the stack, for instance, it's going to crash. But there's also read, there and then jump there and it's done. So if you can get memory corruption bugs, it's pretty much like done from there. Okay. So now we have an idea of the system. Let's try to like reverse stuff a little. The point was the application firmware is encrypted and we want to like decrypt it. But we are only angle here is like looking at the SBI first. So, yeah, as I said, we want to look for firmware decryption routines. And for that, we can look for strings. And that things pop up, which is like a lot of like sad failure about decryption. But the failure of that thing is our success because that points us to where like decryption happens. And in practice, the way it works, like doing firmware updates, it's like a three step process. And you can find out like if you reverse the upgrade service on Windows or like one function in the Linux version.

And so, the host is going to send a field upgrade start command. And with that, it's going to provide an encrypted header for the firmware that's going to get decrypted and verified like cryptographically verified. But if there is no key present, it's going to use some hard-coded credential defaults, hard-coded keys. And then it's going to generate new key material for the storage of the firmware like on the flash chip. So every time you reinstall the thing, it's regenerate those keys. Then the second step is like the update command that keeps sending blobs of encrypted firmware. And that gets decrypted. The one gotcha here, it's using a weird IV for the encryption mechanism. And we'll see that in a few slides. And finally, when the whole firmware has been sent, it calls, like the host calls the complete command that will verify the signature and commit everything to flash. Or what remains to be committed.

In practice, it looks like that. In certain situation, the key material is going to be loaded into that SCD stuff I was talking about. It's just like default key and default IV. And that's going to be used to decrypt, to be fed that USH decrypt function. And if we look into the USH decrypt function, it turns out it's just a wrapper around the cryptographic module APIs. So we don't really know what it does. But you can see here it's using the key we've seen just before. But it's not a big deal that we don't know which algorithm it does because there's only so many. So what we can do is a bit of trial and error by taking the encrypted header and just trying to decrypt it with different stuff. And it turns out like ASCBC with the actual default key and IVs we've seen, will get you something decrypted. And the one gotcha here is like the SCD may have different endianness so the byte order might swap depending on stuff.


## [15:24]


So it might be a problem sometimes. So we decrypted the header. Now we really want to decrypt the whole firmware , you know, just not just the beginning. And for that, that's the update command. And we can see here you have that while loop that's going to keep decrypting, you know, like blobs, like junk of the firmware. And it's like the same stuff except that it's using this like custom like IV command. So the vector, initialization vector of the, for the ASCBC. And the function looks like that. So you can just copy paste that either in Visual Studio or like Python and this way it generates the proper IV for you. And the idea is it will take the address, the mapping address of that chunk of flash. So, you know, if it's loaded as whatever address in firmware that would be the input for that function. And so we implement the whole stuff and we run the algorithm ourselves and we successfully decrypt the application firmware.

And I just run strings on it to kind of showcase that. Cool. So now we can import the application firmware in IDA and start looking for vulns. But what vulns do we look for? As I said, all the CV, most of the CV commands except, expect, sorry, a handle, like a session handle. And so you have to open a session. And that's a function that does it. But in practice , that's firmware side. What the firmware does when you call that is it's going to allocate memory. Going to write like a session tag, like, you know, the letter S-E-S-S in that memory. And then just return that pointer as a session handle. So it's kind of really funky already because straight out of the gates, like, the API is leaking like heap addresses back to the user. You know, they could have used like random number or something to identify a session . But here is like straight out the heap address of the decision is the handle.

So that's curious but, you know, sure. And then what we can do also, you know, you open the session, you're a good boy, you close the session too, you know. And for that you provide the handle which is a pointer. And it's going to validate the session. And to do that, it's going to make sure it's within the range of the heap, you know, because that's memory address that you're sending. And then it's going to look for the session tag. And that's pretty much it. There's a few extra checks that are like irrelevant. And when you pass the check, it erases the tag and free the memory. So far so good. But, you know, if you like to play CTF, you know, you have a bit of CTF brain rot, you might be like, it looks like fragile, you know. And, you know, my question was like, do we have like an arbitrary free here? Because really the only check is if the first four bytes of whatever pointer you provide is SESS.

So if we can place that in the heap, you could free something that's not a session.


## [18:25]


But can it be done? Yes, it can be. So you have that function, CV create object that take, you know, a blob of data, a size, and it will put it on the heap for you. That being said, you don't control all of the metadata of those objects. And also, you get a handle to that object. And that one is not the address like for the session, but it's like some random value that, you know, ties to the object. So we can place data on the, on the heap, but we don't know quite know where it is. But still, you know, we have arbitrary data on the heap. So now we want to locate that data. And for that, uh, I picked one of the CV function. Uh, CV get random that, you know, meant to return random data from the cryptography module. But in our case, it takes the session, check if it's valid or not, you know, using the same function as before. And whether or not it's valid, we'll return either like invalid session or do it stuff.

But with that, we really have a session oracle. Like we can just, you know, ask CV get random, here's a pointer to the heap. Is it a real, real session or not? And it will tell you there's no side effect. And it takes like, I don't know, like 30 seconds to swipe the whole heap. So with that, we can locate objects. So to kind of summarize that, CV create object lets you place it on the heap. Get object and set object, uh, not on the slides, but lets you read and write your object data. Get random lets you find, you know, your object on the heap. And CV close lets you, uh, free, um, something that looks like a session. So with all of that, yes, we do have an arbitrary free in the heap. Uh, so are we going to do some cray cray, uh, exploitation, heap exploitation? That's, that's me when I have to learn, you know, a new, uh, heap implementation. Uh, so I was like, okay, let's, let's put a pin on that.

And let's just find more bugs, right? Um, and so let me introduce you a different function called, uh, secure bio identify, which is also a CV command even if it doesn't have CV in the name. So you can reach it from, you know, user land. And it takes a handle to an object. And then what it does, uh, copy the properties of the object or which of them and then mem copy, uh, one of those property into this data to buffer and data to is really just like a stack variable. So here I was like, oh shit, do I have like a stack overflow, like just like that. Um, so to do that, we would need to be able to control that specific field. Uh, and it turns out, no, you can't. Uh, at least there is like some protection and you don't have the control of that, at least in the easy way. But that does not matter because we've just seen we can mess with the heap and so we can mess with the objects on the heap. And so to do that, we're going to, uh, overlap two objects and use one to modify the other.

So here's kind of like a beautiful diagram I made. Um, it's not a one to one representation but to give the idea. So there will be like one of those objects created by, uh, CV create object where we already put the session tag in it.


## [21:29]


And purple is like what you can control and blue is what you cannot control. Not one to one but just to simplify. Uh, we can call CV free, CV close to, you know, free that session and it's going to poke a hole inside the object. And then we can allocate a smaller object that, you know, is meant to fit inside, you know, the hole we just poked. And by the way the heap management works, it's going to be placed, you know, inside the, uh, bigger object. So we kind of like overlapped two objects which, you know, not meant to happen. And with that, oh yeah, what we can do, you can see here purple is, you know, something we can control and blue is something we shouldn't be able to control. But because they overlap we can, um, we can modify the content of the large object which really is going to modify the metadata of the small object which we're not supposed to do. And then we're going to feed the small object into secure bio identify and this time we actually trigger the stack overflow.

And because I said there is pretty much no security mitigation, it means we really can now execute code, uh, inside the firmware. But the question is what do we want to do now? Like what kind of stuff would we want to execute? And so I have like kind of two, uh, cool ideas. One is you can just, you know, run your exploits and any data that you would want your exploit to return to you, you can, uh, make it return inside the large object, you know, in a different area of it. And then you can use CV get object to retrieve that data. And so this way you have kind of a two way communication with your, um, with your exploit. Uh, which is pretty neat because with that you can, you know, make it dump RAM content. So like if there's like key material in the RAM you can retrieve that. The boot from that's, you know, like kind of critical to the security of it. It's pretty fun. But there is a function, uh, you're not supposed to be able to access it, but because we can run code we can now.

It's called SOTP read key. And it basically, uh, retrieve key material from the fuse of the system on chip. And that's for like key material. And clearly like retrieving secret key material from our chip is something that sounds cool. Uh, so that's the one angle. And the other that, uh, won't be developed further today, but, um, we can, uh, patch memory addresses, um, that are like function pointers, you know, like V table for like some object and devices and so on. And with that idea we can redirect a code execution flow from, you know, a legitimate function into some payload we would have placed in memory. And this way we can tamper with the firmware in a baby steps toward like firmware modification. And it's, I'm just not talking about it today because I have better coming up. Uh, overshadowing again. Um, so yeah, first demo, uh, I'm just, uh, going to run the, uh, the attack to leak the keys as I, as I've described.

So hopefully you guys can see. Uh, the process is, uh, first we use the CV get random to locate existing sessions because, you know, the board is being used.


## [24:30]


And then we forge a new one and we free it. We overlap the object, uh, set up the, the exploits to call, uh, SOTP red key. And actually we do it twice. And with that we, uh, we leaked an HMAC key and an AES key. Uh, those are like for my device. So, you know, it's okay if I put them on the slides, you know. Uh, but those are like pretty important. And that's why you should ask like keys. What kind of keys do you talk about? So, thank you. Uh, so there is like seven, um, fuses, like key slots on the chip. And two of them are the one we just leaked, uh, that are used to, uh, secure storage in the flash. So when I said, you know, flash is encrypted, could be with that. And it's also used for, uh, the secret code, the secure code descriptor that, you know, we've seen in some of the slides early. The secure code descriptor contains an RSA key that is used during firmware updates to verify the signature of the firmware updates.

And also the AES and HMAC key that were used for the execution in place, uh, but that one gets updated every time you do a firmware update. And finally, as I've said, uh, there's also the hard-coded defaults, uh, that are actually being used if the SCD is, uh, missing. And now a question you can ask is like, how do you lose that SCD stuff that you keep talking about? Well, there's a very, uh, simple explanation. There's that file called, uh, clear SCD dot bin. And, uh, I don't know if you can see, but it's like zero one two F a bunch of times. And it's used legitimately during firmware updates to reboot in, uh, secure boot image mode. So basically the host flashed that file in, over the legitimate SCD and reboot. And, uh, to flash it, it's using that beautiful function called CVFlashUpdate that once again any user can call. And you reboot and then the SBI is kind of like that, like confused.

And we're like, okay, we need to wait for, you know, proper firmware updates. But the thing that's crazy here is that CVFlashUpdate is literally giving you an arbitrary flash write, like from user land. You don't need any privilege to do that. The one gotcha here is like everything should be encrypted. And so, you know, but we leaked the keys that are used to encrypt it. So that's pretty cool in a way, like a good spot to kind of like asking the question, how secure is the boot process now that, you know, we have leaked critical keys, you know? So revisiting the secure boot process was a little more like details. The way it works, the boot ROM verified the SBI's and then the SBI's get the keys, you know, the fuse keys from OTP memories and decrypt the secure code descriptor that contains the RSA keys, AES, HMAC, et cetera. Populates the execution in place and starts that. But critically, there is no signature check of the application firmware during boot.

The whole security of it depends on, you know, that being secure and therefore that being secure.


## [27:30]


But we just broke that and we can mess with that. So that's, you know, a little worrisome. So yeah, with that we can do some, can we do some firmware modification? So we can dump the device keys. We can retrieve the SID content and decrypt it. But then that's why it gets a little messy because to modify the execution in place code, we'd have to forge, you know, to encrypt flash blobs and also sign them with HMAC key and et cetera. And it's a good way to break your device, you know. So it's like, I don't want to try that, you know, a little risk adverse. And instead, I was like, okay, we can be a little more crafty. We can forge our own SID, you know, and also encrypt it and HMAC it properly. But we're going to put our own RSA key in it so that we can sign our own firmware image updates. And it's going to get accepted because it's using, you know, all key material. And yeah, with that, did it work?

No, it did not. So as I said, you know, I fucked up the NDS of the SID. So I flashed, you know, kind of the wrong byte order and it just, the device just stopped working. And here it's like me trying to fix it with a flash programmer. I was, you know, I knew that could happen so I made a backup of the flash before doing it and that proved to be useful. But then, you know, I put the computer back together like it was restored and I flashed the right SID that, you know, with the right byte order and it worked. This time, you know, like, I don't know if you guys can see but now it's calling itself Control Pwned, you know, because I'm 12. And that's, you know, sent from the, the firmware over USB to Windows. So that's cool. The application firmware was actually modified and that one, you know, it's a persistent modification that even if you reinstall Windows, you do whatever, like it's modified and, you know, you'd have to reinstall the firmware to change it.

So, yeah, that's opened. Thank you. So, so that one, yeah, opened the, the door to a payment implant. So, that's cool. But what can we do with that? You know, how is the unified secure hub, how secure is it? So I have this cool cute demo. There is that function, uh, CV fingerprint identify that is used by, uh, Windows Halo when your computer is locked and but you've set it up so that you can, uh, unlock it with your fingerprints. The, the Halo is going to talk with, you know, some driver, driver or whatever, which will talk with the firmware and call that function and ask, oh, does the fingerprints, you know, on the reader match any of those recorded handles that we have? And so the question is like, can we make it say yes every time? You know, that would be really nice, right? So I tried that. I modified the CV fingerprint identify function, uh, and it looks like that.


## [30:31]


Uh, and just for context, that's a, a plastic finger that I got at Defcon last year, like at the 503 party. So I wanted to shout out to, uh, Magneto and, uh, Azriel that made the, the finger, the finger and just like, didn't know I would use it for that, but, uh, it's probably a fun usage. Uh, so it's like fingerprints, uh, plastic finger and a piece of green onion taped to it to, to trigger the organic detection. And you run it, it's just like, also shout out to Trader Joe for the green onions. And, uh, yeah, you unlock the, uh, you unlock the device. Thank you. So, yeah, that's cool, but can we do more? Can we mess it further? Uh, so that's like getting like a little in the weeds. Uh, I need to talk about how data is passed between the firmware and the host. And so the idea is like the data from the host is encapsulated. And you got, you're going to have, uh, some kind of TLV, like tag length value type of thing.

And the encapsulation is described as that. So for instance, that one, when data gets de-encapsulated, it will just like straight out copy the data into a buffer. While that one will be split into a size that goes one way and the buffer that goes the next one. Sure, like why is this so complicated? I don't know, but, you know, and the trick here is the host, uh, going to prepare the arguments and the size both ways for what it's sending to the firmware, but also what it's expecting back from the firmware. The whole thing, holy shit, the whole thing gets serialized, sends to the host, uh, the firmware. And upon return, it gets deserialized and, uh, the data get back, copy to the user. In practice, we see we get random, uh, it's little tiny, so I got to go fast. Prepare the arguments, call the function, and, uh, deserialize and copy back to the user. Uh, but the problem is the, the system shows the firmware too much.

And drink, that's like inside the firmware. It's, uh, the type of encapsulation and the size of the, the parameters are redefined by the firmware before sending them back. And the host shows that, which is not good because, you know, we've shown that we can be malicious. So when everything gets processed, you get actually like overflows or type confusion bugs, which those will corrupt the host. So, you know, with malicious data from the firmware, you can mess with Windows. And to do that, like a quick example, uh, I'm going to backdoor, uh, CV get random. So the function I was talking about before that return random value. Uh, and from the screenshots, I wanted to highlight some kind of deserialization or decapsulation where you're here, you're going to have like the integer that's meant to receive, uh, the size that was, that was being returned by, uh, the firmware and the data. And that's one of the specific encapsulation type.

But if we switch the encapsulation, we can make it so that, uh, yes, one second.


## [33:32]


We can make it so that, like the, the integer is going to be confused with the buffer itself. And we just straight out overflow that. And, you know, to not just destroy the CV get random function, I made it so that it will only happen if you ask for a specific size. So the firmware like checks, oh, if it's like magic size, it will send like the crazy payload. And if it's not, it will just do its normal job. So that's a different demo. Uh, on the right side, like the video is kind of eh, but it's a toy binary I made that I just called CV get random. If there is no argument on the command line, it just, uh, calls it. And if it's, if there's an argument on the command line, it will send the special size that triggers all the, uh, the nastiness. And so you just run it a few times. I think that, yeah, like here I'm just showing that there is no, uh, shenanigans going on. It's just calling the function.

And on the left side, I'm going to call it like twice with like no arguments so that it just do its normal job and return some random data. And it's cool. And the third time, uh, it's going to return, um, a malicious payload that corrupt the PLN thing and exploit a stack overflow. And I run it and suddenly, boom. Pop calc. So pretty happy with that. It's like we can actually corrupt Windows, you know. Uh, but the point is like that's just a toy binary and I was really looking for places that would use that function but I couldn't find any. So I was like, okay, we need to be a little more serious and we're going to go, you know, for the kill. We won't like mess with the system service. Problem is like we need to find something, uh, that has a high privilege, use that control vault DLL that's, you know, like unsafe. And, uh, in a function that doesn't have a stack cookie. So it's a bit of a tall order.

But, you know, targets, Broadcom services, uh, third party applications, I couldn't find any that use that. But in the past, I think it used to be but not anymore. Or Windows hello stuff, so the, what handles the fingerprints that we've only been messing with. So I spent so much time, uh, looking at the stack of so many functions. So that's me, uh, you know, when spending hours, uh, matching and be like, is it going to work? No it doesn't. But eventually I found that. Uh, whoop. Uh, that's the, uh, it's a function, it's a DLL that's used, uh, to interface with the Windows biometric framework. So when you have a fingerprint reader, like that's kind of the glue code in between the Windows services and the firmware. And what that function does is takes a handle to an object. Retrieve, uh, the head of that object. Using like, pretty much CV get object. And, uh, yeah, the handle is here. And it turns out the, the, that object is not even in the stack frame of the function.

It's in the next one somehow. I don't know how that works, but, but the parent stack frame doesn't have stack cookie either. So this is the best target because we've seen that, you know, we can, uh, overflow that type of stuff. And here is as a straight line to corrupting the return address and, you know, starting a rope chain. Uh, and just to like, summarize, that is being used with the Win biometric service, which run a system.


## [36:38]


Uh, and you can interface with that stuff with some kind of IO control like interface. And that any user can do. And so that's kind of like the cold, cold trace of the thing. So we can exploit that function that will corrupt all of that. And that is still within, you know, the Broadcom storage adapter. So we can overflow that and corrupt the stack. And I apply the same methodology where I backdoor the CV get object function so that if you request the object, uh, 1-3-3-7, uh, it will, uh, return like the whole nasty payload that triggers the bug as opposed to, you know, any legitimate object. So, uh, the demo here, um, just FYI, I already flashed the firmware with the malicious code. And we just going to trigger the vulnerable code pass and requesting that, uh, object. So, on the right side, you have the, uh, the Windows biometric framework running on those SVC hosts. So we run the exploit and we see here, uh, suddenly it pops Netcat, uh, to start a reverse shell.

And from our own stuff, you know, we connect to this reverse shell and, uh, suddenly we get a system shell. So that's cool. Um, so yeah. So far, what have we done? Uh, we got code execution on the application firmware, permanent modification of that application firmware, bypass of the login screen, you know, like if you have some green onions. And we got, like, uh, system privilege. Can we do more? Why, when am I going to stop, right? Uh, and the other is just a cute little, uh, demo. Uh, the stuff I was talking about, like, you know, that, actually that board, it's so tiny in real life. It's, it has like a ribbon cable that has some USB wires to it. And I was like, can I just plug it into any computer to, like, mess with it physically? Uh, and yeah, that's sort of that contraption. You know, most of those, uh, cables are just, you know, 3 volt or 5 volt. But 2 of them are, uh, USB, uh, data and whatever.

And so we can just, like, connect them. Uh, I was going to do a nice, super sleek, you know, circuit board and, you know, I ran out of time. So instead we have our, uh, beautiful setup here. But in practice, you know, that's my setup. Every time you touch a cable, everything breaks. Uh, but, you know, that's why it's, like, you know, ready to be plugged. But when you plug that cable, a new, uh, uh, Dell control, uh, control device pops up here. Uh, because there's a new one that we just connected physically with. And just here to show that, uh, you can actually communicate with it for real. And the, the previous one, uh, so I disabled it for the demo. But anyway, so with that, what, what that means is all of this talk about, you know, oh, malicious user or something, someone compromised, you know, mess with the board. Really, like, it's also someone can open your laptop, you know, physically unplug the cable from the motherboard and plug to their own thing and do the attack as well.


## [39:45]


So it's a whole new, like, physical attack vector that got unlocked here. So, yeah, pretty quick, right? So, uh, similar, like, cute, you know, overall diagram. There's, like, two main attack scenarios that I wanted to highlight. One is, like, as I said, like a post-compromise, which is the most likely, like, uh, you know, user gets compromised by some threat actor. And if the threat actor is aware of this type of stuff, could modify the firmware and, um, and then, you know, use it to have, like, a Windows compromise and kind of an implant in the system. And then a physical attacker can also do that and, you know, either, like, bypass the fingerprint or, uh, you know, uh, compromise Windows. So, uh, reports and stuff like that. Uh, we reported that to Dell, you know, like, early this year. An advisory was released in June. We just, I think, released some of these, like, uh, reports, uh, on the Talos website.

Uh, many CVs and, uh, a few more are going to come, like, later, like, in the year. And there are, like, two, uh, our two blog posts about it. Like, one level, a high-level overview that was released early this week. And the technical deep dive of, like, 5,000 plus words, uh, that was released today. Uh, and shout out to all the people who helped me get that one published because that was a lot of work. Um, and yeah. So, what can we say? I think the firmware security is, it's not new, but it's, like, so important. I think the, um, we need to understand the security exposure of, like, you know, the things that are, like, inside our systems. That board, like, it exists since, like, 2, 5, 15 years, you know. And as far as I can tell, there's no research about it. There's barely any documentation about it. So, I think it's either, like, us hackers, you know, can, you know, reverse that, document it, share, like, our knowledge.

Or maybe, like, what, uh, Nick was saying yesterday, uh, you know, maybe calling for, like, more transparency, you know, from the vendors and so on to, like, you know, be more open. So, I don't know. I think that's, uh, pretty much all I have for you today. Thank you. Uh, you can reach out, uh, to me on Twitter and what not. And, uh, otherwise, like, I have a few minutes for questions, I think. Thank you.
